# Design QA — evidence-first action brief

Date: 2026-07-23

## Comparison input

- Selected reference: `design/reference/action-evidence-result.png` — 1424 × 1104
- Release-candidate capture: `.audit/positioning-redesign-2026-07-23/12-release-candidate-1424x1104.jpg` — 1424 × 1104
- Side-by-side comparison: `.audit/positioning-redesign-2026-07-23/13-reference-vs-release-candidate.png`
- Evidence-fix capture: `.audit/positioning-redesign-2026-07-23/14-release-candidate-evidence-fix-1424x1104.png` — 1424 × 1104
- Final evidence-fix comparison: `.audit/positioning-redesign-2026-07-23/15-reference-vs-evidence-fix.png`
- State: demo result, action-first, all detail sections collapsed

The reference and implementation were inspected together at the same viewport. The implementation preserves the reference's defining trust mechanic: full source on the left, ordered actions on the right, and matching numbered/color-coded evidence at both ends. It intentionally adds a compact product header, provenance badges, separate detail disclosures, and a persistent completion/action footer without weakening the source-to-result relationship.

## QA passes

1. Desktop layout — passed at 1424 × 1104, 1180 × 820, and 760 × 900. No cropped primary controls, horizontal overflow, or broken heading layout.
2. Mobile layout — passed at 390 × 844. Source and action panes are both reachable; selecting a term stays in the explanation pane, while its evidence card deliberately returns to the source pane.
3. Evidence mapping — passed. Clicking source evidence opens or focuses the corresponding action, term, or explanation; clicking a result evidence card returns to the exact source quote.
4. Understanding layers — passed. Ordinary words and professional terms have distinct labels. Process context renders “这是什么 / 为什么要做 / 你该怎么做” and retains the original trigger evidence.
5. Verification states — passed. Pending, retrieved, failed, and explicitly verified states are visually distinct. A retrieved official page is presented neutrally and never receives verified styling by default.
6. Result ordering — passed. Action-first is the default; translation-first expands and moves the complete translation before the action path, while secondary details remain after both primary sections.
7. Interaction and recovery — passed. Reply draft, copy actions, recapture, reanalysis, cancellation, responsive pane switching, disclosures, settings access, and completion details are reachable.
8. Accessibility and runtime — passed. Buttons expose names and pressed/expanded states, evidence marks are keyboard operable, the reply dialog traps focus and closes with Escape, and the final preview produced no browser warnings or errors.

## Fixes made during QA

- Added responsive source/action pane controls so mobile users can reach both halves of the evidence map.
- Kept term selection in the action pane on mobile; source navigation now occurs only through the explicit evidence card.
- Stopped truncating action groups and separated material requirements from the first action.
- Derived privacy copy from result provenance instead of the current settings selection.
- Replaced ambiguous “verified” wording with neutral retrieval receipts unless an explicit semantic assessor confirms support.
- Added visible GOV.UK query approval when discovery has a safe minimal query but no candidate URL.
- Preserved owner-specific exact quotes when one source highlight supports multiple results, while keeping a single uncluttered source anchor.
- Added deadline and verification evidence to the same bidirectional source map; deadline headlines now require an explicit action relationship.
- Rechecked the evidence fixes against the reference in one 2848 × 1104 side-by-side image; source anchors, action cards, disclosures, and footer remain visually clear without clipping.

final result: passed

## 2026-07-24 usability follow-up

- Real Electron action-first capture: `.audit/usability-2026-07-23/31-final-action-first-readable.jpeg` (1180 × 820 CSS window; 1106 × 768 captured pixels).
- Updated comparison input: `.audit/usability-2026-07-23/32-reference-vs-real-window-readable.png` (selected reference and real-window capture shown together at equal rendered height).
- Core action, evidence, translation, terminology, process, warning, and verification text was raised out of the previous 8–10 px range. The reference's numbered/color-coded source-to-action relationship remains dominant and readable.
- Translation-first now means full translation → action path → secondary details. Action-first means action path → full translation → secondary details.
- Persistent evidence focus now keeps unrelated text readable instead of reducing it to 42% opacity.
- Small-screen order controls retain text labels, and the result pane opens on the selected action/translation content while exact evidence links still switch to the source pane.
- The provider setup was separately verified in the real Electron app as a four-step flow with explicit save state, metadata test gating, and disabled full-analysis activation before a current successful test.

follow-up result: passed for the fixture-backed interface and setup interactions; live provider ActionBrief generation remains a separate release evidence gap.

## 2026-07-27 result-order preference follow-up

- Audited action-first and translation-first at 1280 px and 600 px, including deterministic first-save failure and retry success.
- Replaced CSS-only ordering with matching DOM order so keyboard and assistive reading order now follows the visible preference.
- Renamed the narrow result pane from the fixed “行动与解释” label to “翻译与行动” whenever translation-first is selected.
- Failed preference persistence now keeps the current selection visibly pressed, explains that the requested order was not saved, and offers a targeted retry; successful retry removes the alert and applies the requested order.
- Verified the 600 px state at 600 px document width with no horizontal overflow. Evidence: `.audit/result-order-preference-2026-07-27/05-order-save-error-recovery.jpg`, `06-order-save-retry-success.jpg`, and `07-translation-first-narrow-fixed.jpg`.

follow-up result: passed for preference semantics, responsive labeling, and save-failure recovery.

## 2026-07-27 source-correction follow-up

- Audited the completed-result state for the user task “I found an OCR or paste mistake and need to correct it.” The previous footer offered reanalysis, recapture, and clearing, but no path back to editable source text.
- Added “修正原文” to completed results. It opens the existing capture surface with the complete source focused and explicitly states that the previous result remains in memory until corrected analysis succeeds.
- Added “先返回上一份结果” so entering correction is reversible. Returning restores the result headline focus.
- Deterministic first-regeneration failure now proves both recovery layers: the previous result reappears with an honest service warning, and “继续修正原文” reopens the exact corrected draft rather than the older source.
- A successful second regeneration replaces the result source and clears the temporary edit draft. A draft belonging to an older result cannot carry into a newer source.
- Verified the correction surface at 1280 px and 600 px, with textarea focus, visible return action, and no horizontal overflow. Evidence: `.audit/editing-source-2026-07-27/01-result-no-edit-path.jpg` through `05-edit-source-narrow.jpg`.

follow-up result: passed for reversible source correction, failure recovery, draft isolation, focus, and responsive layout.

## 2026-07-27 informed-submission privacy follow-up

- Audited the capture panel with a populated source under local Ollama and online OpenAI at wide, 600 px, the real 520×680 capture window, and 390 px.
- The previous responsive rule hid the only explicit processing-location label below 920 px. In the online 600 px state, the generate action and local-retention footer remained visible but “the full source will be sent” did not, creating a direct informed-submission failure.
- Added a persistent disclosure immediately above generate. Local mode says analysis stays on this Mac; online modes name the receiver and state that the complete source is sent. Both distinguish model processing from separately approved official-source lookup.
- Added “更改处理方式” beside the disclosure. It opens settings with the current location selected and keyboard focus restored to the settings return control.
- Replaced the old fixed footer, which could imply local-only handling, with provider-aware retention and network copy.
- Tightened short-height layout so the disclosure and primary generate action are visible together at 520×680. Verified 520 px, 600 px, and 390 px with no horizontal overflow.
- Evidence: `.audit/privacy-boundary-2026-07-27/01-local-wide-visible.jpg` through `10-online-default-window-submit-disclosure.jpg`.

follow-up result: passed for local/online informed submission, provider switching, semantic labeling, and supported capture-window reflow.

## 2026-07-27 processing-and-cancellation follow-up

- Audited the sequence from an informed submit through waiting and cancellation under local Ollama and online OpenAI, including a first analysis with no prior result and corrected reanalysis with a preserved result.
- The old waiting state removed the exact processing-location explanation immediately after submission and exposed only an icon-only “取消处理” action, so users could not see whether their source had left the Mac or what cancellation would preserve.
- Added an always-visible active destination: local analysis states that the source remains on this Mac; online analysis names OpenAI and states that the complete source has already been sent to the selected service.
- Replaced the ambiguous cancel control with “取消并保留原文” for a first analysis and “取消并返回上一份结果” when a valid result exists.
- First-run cancellation now confirms source retention and restores focus to the textarea. Reanalysis cancellation restores the previous result, announces that it remains, and returns focus to the result headline.
- Verified the default 520×680 and narrow 390×680 layouts with no clipped action, hidden destination, or horizontal overflow. Evidence: `.audit/processing-cancel-2026-07-27/01-ready-local-before.png` through `10-loading-online-narrow-after.png`.

follow-up result: passed for persistent local/online processing disclosure, explicit cancellation outcomes, retained-state confirmation, responsive reflow, and focus recovery.

## 2026-07-27 cancellation-acknowledgement follow-up

- Re-audited the cancel action as a trust boundary. The previous handler invalidated the visible task and announced “处理已取消” before `LLM_CANCEL` acknowledged the request; a rejected stop was swallowed, so an online provider could continue processing while the interface claimed success.
- Cancellation now has explicit pending, failed, acknowledged, and completion-won states. While pending, the task and destination stay visible. A failed online stop warns that processing and fees may continue; a local failure omits the fee claim. Both offer a destination-specific retry action.
- Acknowledged first-run cancellation returns to the retained source and focuses the textarea. Acknowledged reanalysis cancellation restores the previous valid result and focuses its heading.
- If a result arrives before cancellation acknowledgement, the result remains visible with an honest completion-race notice. If cancellation cannot be confirmed and the task later completes, that result also remains visible with a separate notice.
- Verified deterministic cancel failure, retry success, and completion races for online OpenAI and local Ollama; covered first analysis, preserved-result reanalysis, 520×680, and 390×680 states. Evidence: `.audit/cancel-acknowledgement-2026-07-27/01-cancel-ready-before.png` through `10-cancel-failed-local-after.png`.

follow-up result: passed for acknowledged cancellation, failure recovery, online cost disclosure, race-safe result preservation, focus recovery, and responsive layout.

## 2026-07-27 active-task settings follow-up

- Audited the user task “change model or privacy settings while analysis is still running.” Previously, the settings icon immediately replaced the processing surface with a full settings page that contained no active-task status. Changing provider could then invalidate the visible task without confirming that the online request stopped.
- Added an accessible decision dialog that leaves the live task visible behind it, distinguishes local from online consequences, and states whether a confirmed stop will retain the source or return to the previous result.
- “Stop task, then open settings” now reuses the acknowledged cancellation state machine. While pending, settings remain closed. A failed stop keeps the task visible and changes the retry action to “retry stop and open settings.”
- If completion wins the race, the result remains visible and the app explicitly says settings were not opened. After a confirmed stop, settings open with a persistent status explaining whether the source or previous result remains available.
- The dialog traps focus, closes with Escape, makes background content inert, and restores focus to the settings trigger when dismissed. After a successful first-run stop and settings round trip, focus returns to the retained source; preserved results return to their result heading.
- Verified online OpenAI, local Ollama, first analysis, preserved-result reanalysis, cancellation failure, cancellation success, completion after failure, 520×680, and 390×680 states. Evidence: `.audit/processing-settings-guard-2026-07-27/01-processing-before-settings.png` through `11-settings-guard-narrow.png`.

follow-up result: passed for active-task visibility, informed settings navigation, acknowledged stop-before-switch, failure recovery, retained-content confirmation, keyboard focus, and responsive layout.

## 2026-07-27 long-source recovery follow-up

- Audited the capture task at and above the 10,000-character limit for a manually entered complete source and an imported clipboard prefix. Previously both states reused the same generic warning even though one retained every character and the other had already discarded the tail.
- Manual overflow now says the complete source remains in the textarea, reports the exact excess, confirms that nothing has been sent or analyzed, and offers “定位超出部分,” which focuses the source and selects only the characters after the limit.
- Clipboard and OCR prefixes now say only the prefix was loaded, report the original and missing counts, and explicitly state that the missing tail is not in the input. Clipboard recovery selects the retained prefix for complete manual replacement without modifying the clipboard; OCR recovery returns to a smaller complete selection.
- Added a visible live count and associated the blocking explanation with the textarea through `aria-describedby` and `role="alert"`. Recovery confirmations use a live status message and keep the disabled submit state truthful.
- Verified 10,073-character manual input, exact 10,000-character readiness, 10,137-character clipboard input with a 137-character missing tail, focus and selection ranges, the default 520×680 capture window, and 390×680 with zero horizontal overflow.
- Evidence: `.audit/long-source-recovery-2026-07-27/01-manual-over-limit-before.png` through `08-clipboard-prefix-narrow-after.png`, plus `09-manual-before-vs-after.png` and `10-clipboard-before-vs-after.png` for same-viewport comparisons.

follow-up result: passed for retained-vs-missing source truthfulness, exact counts, actionable recovery, assistive association, focus, and responsive layout.

## 2026-07-27 unsaved connection draft follow-up

- Audited the user task “replace an API Key or model, then leave settings before saving.” Previously the field reported “有未保存的更改,” but Return and Escape immediately unmounted settings; reopening showed an empty field with no warning. Provider, local/online path, and basic-translation changes used the same silent draft-clearing path.
- Added one shared leave guard for API Key, service address, custom credential, and model drafts. Return, Escape, provider changes, local/online changes, and the basic-translation fallback now name the requested transition and require an explicit choice between continuing to edit and discarding only the unsaved draft.
- The dialog distinguishes the unsaved draft from already saved configuration, never displays the secret, gives initial focus to “继续编辑,” traps focus, makes the background inert and hidden from assistive reading, closes with Escape, and restores focus to the initiating control.
- If the user requests an exit while Save is still pending, the destructive action is disabled and the dialog waits for the authoritative result. A successful save continues the original transition; a failed save keeps the draft and converts the same surface back to an explicit edit-or-discard decision.
- Verified direct Return, direct Escape, continuing to edit, explicit discard, OpenAI → Anthropic switching, first-save failure, a deterministic slow save, 1280×720, and 390×680 with zero horizontal overflow.
- Evidence: `.audit/unsaved-settings-exit-2026-07-27/01-unsaved-api-key-before-exit.png` through `10-save-failure-keeps-draft-guard.png`, plus `11-return-before-vs-guard-after.png` and `12-lost-before-vs-preserved-after.png` for same-viewport comparisons.

follow-up result: passed for connection-draft retention, explicit discard, save-race truthfulness, keyboard behavior, assistive isolation, focus recovery, and responsive layout.

## 2026-07-27 active connection-validation exit follow-up

- Audited the user task “start full-analysis capability validation, then leave settings.” The validation can take 10–60 seconds and an online provider may charge a small amount, but Return and Escape previously unmounted settings immediately without explaining whether the request continued or where its result would go.
- Added one wait-or-stop guard for Return, Escape, local/online path changes, provider changes, and the basic-translation fallback. The live validation remains visible behind the dialog, and the safe initial choice is always “继续等待.”
- Provider cancellation now distinguishes requesting an abort from confirmed task settlement. The main process waits up to two seconds for the tracked validation task to settle and returns `cancelled`, `not-running`, or `still-running`; the renderer never leaves settings on an unconfirmed stop.
- A missing stop acknowledgement keeps the dialog open with a destination-specific retry action. If validation naturally completes while the dialog is open, the successful or failed result remains in settings and the dialog changes to “验证已经完成” instead of completing the original navigation silently.
- The dialog traps focus, makes the background inert and hidden from assistive reading, closes with Escape when safe, restores focus to the initiating control, disables dismissal while cancellation is pending, honors reduced motion, and stacks its actions at 390px.
- Verified deterministic confirmed stop, missing acknowledgement, completion race, Return, Escape, local/online path switching, 1280×720, and 390px. Evidence: `.audit/connection-test-exit-2026-07-27/01-before-testing.png` through `08-narrow-active-guard.png`.

follow-up result: passed for informed validation exit, acknowledged cancellation, stop-failure recovery, result-preserving completion races, keyboard focus, assistive isolation, and responsive layout.

## 2026-07-27 application quit-safety follow-up

- Audited the boundary between hiding the macOS window, closing it, and actually quitting the application. Previously the tray quit item and Cmd+Q entered `app.quit()` directly; the main process aborted requests, but the renderer never had a chance to explain that a draft, result, or settings edit existed only in memory.
- Added a source-bound main/renderer quit handshake. The renderer reports only a boolean risk summary to the main process; source text, results, credentials, and service addresses never enter the quit-status payload. Repeated or stale decisions cannot confirm another renderer's request.
- Quit consequences now adapt to the current session: source drafts, completed results, active analysis, official lookup, provider capability validation, clear-undo windows, unsaved connection fields, and settings persistence each receive specific copy. Active online work names the possibility that the provider already received content or incurred a small fee.
- Kept ordinary macOS window close as a hide action, so background analysis and verification continue with the existing privacy-safe menu-bar status. True quit blocks new assumptions, brings a risky hidden session back into view, and waits for an explicit decision.
- Found and fixed a same-frame race between a settings change and quit. The main process now always requests renderer acknowledgement instead of trusting a cached safe state, and the renderer waits for two animation frames before deciding whether a confirmation is needed. A save in progress disables destructive exit; once it settles and no other risk remains, the original quit intent completes automatically.
- The dialog uses an alert-dialog role, safe initial focus, Tab containment, Escape cancellation, inert/assistively hidden background content, focus restoration, disabled pending actions, reduced-motion handling, and stacked 390px actions. This is keyboard and DOM-semantics evidence, not a full VoiceOver certification.
- Verified a risk-free session (no interruption), draft cancellation with textarea focus restored, explicit destructive confirmation, completed result, online analysis, unsaved API Key draft, active provider validation, settings-save race, Escape, 1280px, and 390×844. Evidence: `.audit/quit-safety-2026-07-27/01-before-draft-at-risk.png` through `08-narrow-draft-quit-guard.png`.
- Limitation: deterministic browser fixtures cover renderer behavior and the main-process lifecycle is protected by source-level regression checks; a real packaged macOS Cmd+Q/tray smoke test and full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for consequence-specific quit confirmation, source-bound decisions, save-race safety, task/cost disclosure, keyboard focus, assistive isolation, and responsive layout within the stated evidence limits.

## 2026-07-27 same-window interruption recovery follow-up

- Audited the user task “continue after the interface unexpectedly reloads or crashes.” Previously, a renderer reload erased a 355-character draft or completed result even though the same window remained open, with no explanation or recovery choice.
- Added one bounded current-window recovery snapshot for source drafts, correction drafts, completed results, and the latest valid result during an interrupted retry. It expires after 30 minutes, is removed on restore, discard, explicit clear, or window-session end, and never becomes application history or sync data.
- Whitelisted the snapshot fields. Settings drafts, API keys, service addresses, custom prompts, provider configuration signatures, and official-verification approval IDs are excluded. Corrupted, oversized, future-dated, expired, version-mismatched, and kind-mismatched records fail closed and are removed.
- Recovery is always explicit. The alert dialog gives safe initial focus to Restore, traps Tab, keeps Escape non-destructive, makes background content inert and assistively hidden, and states the privacy boundary before either choice.
- Interrupted analysis and verification are stopped and never restart automatically. Restored results cannot reuse prior official-source approval; the interface tells the user to reanalyze and approve again. Restored results focus the conclusion heading rather than the destructive clear action.
- Verified draft and result loss before the change; draft, result, and interrupted-task recovery after the change; explicit discard with no repeat prompt; explicit clear with no recovery; Escape; 1280px; and 390×844 stacked actions. Evidence: `.audit/session-recovery-2026-07-27/01-before-reload-draft.png` through `11-narrow-recovery-prompt.png`, plus `12-draft-loss-vs-recovery.png` and `13-result-loss-vs-recovery.png` for same-viewport before/after comparisons.
- Evidence limits: deterministic browser fixtures cover renderer reload behavior, focus, and responsive semantics; source-level regression checks cover main-process renderer-crash cleanup and reload limits. A packaged macOS forced renderer-crash smoke test and a full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for explicit same-window restoration, privacy-bounded temporary storage, task/approval non-resumption, discard and clear semantics, keyboard focus, assistive isolation, and responsive layout within the stated evidence limits.

## 2026-07-27 clipboard-monitoring consent follow-up

- Audited the user task “automatically analyze text I copy” as a privacy, cost, and state-truthfulness boundary. Previously, the settings switch enabled monitoring with one click; in OpenAI mode, every new non-empty clipboard text could then be sent automatically without a local consequence explanation or confirmation.
- Enabling now opens a destination-specific alert dialog before any settings write. OpenAI and other cloud paths name the receiver, request/fee risk, and sensitive copied text; free translation names Google Translate/MyMemory and rate-limit risk without claiming a user fee; Ollama states that processing remains on this Mac while warning that unintended sensitive copies can still be analyzed.
- The safe initial action is “Keep off.” Escape cancels, Tab stays contained, background settings become inert and assistively hidden, and focus returns to the monitoring switch. At 390×844 the actions stack without clipping or horizontal overflow.
- A successful enable exposes a persistent destination-specific status card and direct off action, and refreshes the support summary. Failed enabling leaves monitoring off inside the same dialog; failed disabling leaves monitoring visibly on and states that automatic processing or transfer continues. Both use one targeted retry path, with no contradictory generic retry capable of applying a later privacy-sensitive change.
- Verified OpenAI enable/cancel/retry, reload-after-failure, Ollama confirmation and active state, free-translation copy, disable failure and retry success, support-diagnostics reconciliation, 1280×720, and 390×844. Evidence: `.audit/clipboard-monitor-consent-2026-07-27/02-online-monitor-toggle-silent.png`, `03-online-monitor-enabled-without-confirmation.png`, `04-online-monitor-consent.png`, `08-online-monitor-active-refined.png`, `10-online-disable-failure-refined.png`, `11-local-monitor-consent.png`, `12-online-consent-narrow.png`, `13-online-enable-failure-refined.png`, and `14-before-vs-consent.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover renderer behavior, focus, persistence failure, and responsive semantics; source-level regression checks cover the real clipboard event gate and settings persistence contract. A packaged macOS clipboard-monitor smoke test and a full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for informed destination-specific consent, persistent active-state visibility, truthful save-failure recovery, keyboard focus, assistive isolation, and responsive layout within the stated evidence limits.

## 2026-07-27 clipboard-monitoring visibility follow-up

- Audited the user task “leave settings after enabling automatic clipboard analysis, then continue ordinary work or hide the window.” The enabled and disabled capture surfaces were previously almost identical: only the textarea placeholder changed, with no persistent automatic-transfer state or direct stop action.
- Added a persistent destination-specific status above the active task surface. Online paths say that new copies are sent to the named provider; Ollama says analysis remains on this Mac. The status remains visible while processing, and its direct off action does not require returning to settings.
- Closing monitoring stops only future clipboard triggers. If analysis already started, the task remains visible and continues; the focused confirmation says so instead of implying cancellation. A failed settings write keeps the online/local active state visible, changes the region to an alert, and offers one targeted retry.
- The default 520×680 window uses a compact one-line status so the primary action remains visible. At 390×844 the destination and off action remain visible without horizontal overflow; longer consequence copy stays available at larger or taller layouts.
- The macOS menu now exposes monitoring on/off state, the active destination, and a direct off action even while the app window is hidden. During an active task, that action explicitly says the current task continues. Tray-driven changes stop the monitor, refresh the menu, and push safe settings back to the renderer.
- Verified enabled/disabled visual parity before the change, OpenAI and Ollama states, direct close, focused close confirmation, deterministic first-save failure and retry, close during a slow task, completed-result visibility, 1280×720, 520×680, and 390×844. Evidence: `.audit/monitor-visibility-2026-07-27/01-monitor-on-hidden-state.png` through `13-monitor-visible-on-result.png`, including `12-hidden-vs-visible-comparison.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover renderer behavior, focus, save failure, active-task semantics, and responsive reflow; source-level regression checks cover native tray presentation and renderer synchronization. A packaged macOS menu-bar interaction smoke test and a full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for persistent automatic-processing visibility, destination-aware stop control, truthful active-task semantics, save-failure recovery, hidden-window menu state, focus, and responsive layout within the stated evidence limits.

## 2026-07-27 consecutive clipboard monitoring follow-up

- Audited the user task “copy a second passage while an analysis or unread result is still present.” Previously, the second monitor event immediately changed the visible source even while the first request was still running; this made the screen describe one passage while the background request still belonged to another, and a completed result could be replaced before the user chose to leave it.
- Current work is now protected. A monitored copy received during a task, official lookup, draft, source correction, recovery prompt, error with retained source, or completed result enters one bounded renderer-memory waiting slot instead of mutating the visible source or scheduling another transfer.
- The waiting card names the consequence, previews the latest passage, states that no history is written, and offers `处理新文字` or `忽略`. Processing stays disabled while work is active; after completion, accepting starts the next task and preserves the previous valid result as the cancellation/failure fallback.
- Repeated copies replace only the waiting slot and increment a visible count. The menu bar receives only a boolean/count status, shows a dot and `显示等待内容`, and never receives the waiting text. Turning monitoring off discards the waiting item without sending it; app quit now includes the memory-only waiting item in its loss warning.
- Focus returns to the persistent monitoring status after `忽略`, and live regions announce arrival, replacement, acceptance, ignore, and disable outcomes. The waiting item is not written to session recovery, history, settings, diagnostics, notifications, or the native menu.
- Verified the mismatched-source failure, protected processing, protected completed result, ignore, explicit next-task acceptance, and monitor-off discard. Evidence: `.audit/clipboard-collision-2026-07-27/01-before-visible-source-replaced.png`, `02-after-current-task-protected.png`, `04-result-protected-with-waiting-copy.png`, `05-ignore-preserves-result.png`, `06-user-confirmed-next-source.png`, `08-before-after-comparison.png` for the same-viewport comparison, and `09-final-preview.png` for the clean final state. `03-after-default-window.png` was rejected because the browser viewport override cropped rather than reflowed the existing tab.
- Evidence limits: deterministic browser fixtures cover renderer state, focus, bounded waiting behavior, explicit acceptance, ignore, and disable outcomes; source-level checks cover native tray copy minimization and quit-risk wiring. A packaged macOS menu-bar interaction smoke test, real system-clipboard burst test, and full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for current-task/result protection, latest-only waiting behavior, user-controlled replacement, memory-only privacy boundaries, menu-bar discoverability, and keyboard/assistive state communication within the stated evidence limits.

## 2026-07-27 active capture shortcut follow-up

- Audited the user tasks “press Option+C for another passage while analysis is running” and “press F2 while analysis or official verification is running.” Previously Option+C changed the visible source immediately while the old request continued, and the screenshot IPC handler aborted analysis and verification before starting frame selection.
- Option+C now uses the protected waiting state even when clipboard monitoring is off. The current source, result, and task remain visible; shortcut copy is labeled separately from automatic monitoring, stays memory-only, and requires an explicit process-or-ignore action. A shortcut-selected waiting item takes priority over later automatic monitor events.
- F2 now opens an inline decision state with `停止后截图` and `继续当前任务`. Choosing stop reuses the acknowledged cancellation paths for analysis and official verification. Screenshot selection begins only after the active task stops or finishes first; a failed stop leaves both the task and a targeted retry visible.
- The main screenshot handler independently rejects capture while analysis or verification is active and no longer aborts either controller. Pending screenshot intent is included in quit-risk disclosure and contains no image or source text.
- Verified source ownership during slow analysis, protected completed results, explicit next-text acceptance, F2 ignore, confirmed analysis stop, confirmed verification stop, and deterministic stop failure. Evidence: `.audit/active-capture-collision-2026-07-27/01-before-option-c-replaces-source.jpg`, `02-before-f2-replaces-source.jpg`, `06-option-c-before-after.jpg`, `07-f2-before-after.jpg`, `12-full-option-c-waits.jpg`, `13-full-f2-waits.jpg`, and `14-full-confirmed-screenshot.jpg`.
- Evidence limits: deterministic browser fixtures cover renderer state, user decisions, cancellation races, and DOM semantics; source-level checks cover the main-process busy guard and quit-risk wiring. The in-app browser's high-DPI full-page capture returned only the top-left physical quadrant, so accepted full-state screenshots 12–14 use a temporary 0.5 audit-only render scale that was removed immediately afterward; DOM geometry was measured separately at the real 1280×720 layout. A packaged macOS global-shortcut smoke test and full VoiceOver pass were not automated.

follow-up result: passed for active source ownership, explicit capture replacement, acknowledged stop-before-screenshot, stop-failure recovery, and assistive state communication within the stated evidence limits.

## 2026-07-27 capture shortcuts while Settings is open follow-up

- Audited the user task “press Option+C or F2 while reviewing or editing Settings.” The main task panel remained mounted but visually hidden, so shortcuts could previously enter capture logic without any visible response, race provider capability validation, or surprise the user after returning from an unsaved connection draft.
- The hidden panel now retains capture intent without starting it. With no settings work at risk, the app returns to the task surface and starts the explicit shortcut action. Unsaved API Key, endpoint, or model input reuses the existing edit-or-discard dialog, with capture-specific consequences. Active provider validation reuses the acknowledged stop flow and does not start capture until the stop settles.
- Choosing the safe action keeps Settings open and exposes a persistent “request retained” status; returning later reveals the existing process/start-or-ignore controls. Empty Option+C input produces an in-settings alert and changes neither settings nor task state. First-use guided setup keeps the request pending until the user enables a processing mode instead of bypassing the setup gate.
- The main screenshot IPC guard now also rejects provider capability validation, covering renderer races without cancelling the validation. Pending text and screenshot intent remain memory-only and continue to participate in quit-risk disclosure.
- Verified ordinary F2 handoff, ordinary Option+C handoff, unsaved OpenAI credential drafts for both shortcuts, defer-with-visible-status, active provider validation, confirmed stop-before-capture, empty clipboard feedback, and guided-setup retention. Evidence: `.audit/settings-capture-shortcuts-2026-07-27/01-reference-settings-without-capture-feedback.png`, `03-after-unsaved-draft-guard.png`, `04-after-deferred-screenshot-banner.png`, `05-after-provider-validation-guard.png`, and `06-reference-vs-guard.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover renderer transitions, visible decisions, focusable actions, and capture sequencing; source-level checks cover main-process provider-validation rejection and one-time request wiring. The reference image reproduces the previously observed no-feedback Settings surface at the same viewport after the original behavior had been confirmed in DOM state. A packaged macOS global-shortcut smoke test and full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for visible Settings ownership, draft retention, acknowledged provider-test cancellation, deferred capture discoverability, empty-input feedback, guided-setup safety, and main-process race protection within the stated evidence limits.

## 2026-07-27 foreground capture decision follow-up

- Audited the user task “press F2 while I am correcting source text or deciding what to keep.” Previously, an unsaved source correction could disappear from the foreground immediately, and screenshot processing could replace it without any confirmation. The same missing boundary covered recovery, the saved-term library, settings confirmation, app quit confirmation, ordinary source drafts, and the clear-undo opportunity.
- Added one foreground ownership rule. Modal decisions remain inert and in front while screenshot intent waits behind them; editable or time-bounded states expose a visible request card without changing the source, result, focusable task, or undo snapshot.
- Source correction and draft actions name the replacement consequence. Clear undo offers `放弃撤销并截图` or `保留撤销机会`; if the ten-second window expires first, those stale labels change to an ordinary start-or-keep choice.
- Closing a modal returns focus to the waiting request but does not start selection. The listener now subscribes once and reads the latest handler through a ref, preventing development fixture replay and removing the production event gap caused by state-driven unsubscribe/resubscribe cycles.
- Option+C inherits the same foreground-decision protection through the shared capture context, so a clipboard shortcut cannot replace visible work while recovery, library, settings, quit, or clear-undo decisions are active.
- Verified the source-edit replacement failure before the change; protected correction with draft preservation and focus return; quit confirmation priority and stable post-cancel choice; explicit approved screenshot start; clear-undo protection and truthful expiry; and saved-term library priority with stable post-close choice. Evidence: `.audit/foreground-capture-guard-2026-07-27/01-before-source-edit-at-risk.png` through `07-before-vs-protected-source-edit.png`.
- Evidence limits: deterministic browser fixtures cover renderer state, focus, modal isolation, timing, and one-shot shortcut delivery; source-level checks cover every foreground reason and App quit wiring. A packaged macOS global-shortcut smoke test and a full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for foreground task ownership, explicit destructive capture choices, stable one-shot shortcut delivery, truthful timed-state copy, focus return, and modal isolation within the stated evidence limits.

## 2026-07-27 foreground clipboard decision follow-up

- Audited the user task “press Option+C while I am correcting, drafting, restoring, or deciding whether to undo.” The waiting card previously said only `处理新文字`, so a user could not tell that success would leave and replace an unsaved correction or draft; clear undo and modal ownership were equally generic.
- Option+C now reads the shared foreground reason. Recovery, saved terms, settings, and quit decisions remain in front and keep processing disabled. Source correction, unsent drafts, and clear undo receive consequence-specific process actions and safe actions that name what will remain.
- Keeping the current work removes the waiting text without changing the source, result, or undo snapshot and returns focus to the textarea, undo button, monitoring status, or result heading. Explicit processing still preserves the prior valid result as a failure fallback and replaces the foreground only after the new task succeeds.
- Verified the generic replacement ambiguity before the change; recovery-modal priority; protected source correction and draft; explicit draft replacement; clear-undo protection and safe focus return; and 390px action layout without horizontal overflow. Evidence: `.audit/foreground-clipboard-guard-2026-07-27/01-before-generic-action-hides-replacement.png` through `07-after-clear-undo-protected.png`, plus `08-before-vs-contextual-source-edit.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover renderer state, focus, modal isolation, timed undo behavior, explicit replacement, and responsive semantics; source-level checks cover every foreground reason and the stable clipboard event subscription. A packaged macOS global-shortcut smoke test, real system-clipboard input, and a full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for foreground ownership, consequence-specific clipboard replacement, safe focus return, modal priority, undo preservation, explicit replacement, and narrow-layout clarity within the stated evidence limits.

## 2026-07-27 global shortcut readiness follow-up

- Audited the user task “use Option+C or F2 after launching Slipstream, then recover if macOS rejected the shortcut.” Previously, Settings displayed only editable accelerator strings, so a saved value looked healthy even when macOS registration had failed. Startup failures could be emitted before the renderer was listening, and a conflicting change surfaced only as a distant generic save error.
- Registration now has one structured source of truth for both shortcuts. Startup publishes the retained state after the renderer loads; the task surface names the unavailable shortcut and links directly to the shortcut section; Settings focuses the first failed field and shows per-shortcut enabled, unavailable, or checking status with a targeted retry.
- Shortcut changes are atomic across registration and persistence. A conflicting or invalid accelerator is caught without crashing, all configured shortcuts are restored before the failure returns, the attempted value is removed, and the field-level message names both the failed combination and whether the previous shortcut is still working. Ordinary interface capture buttons remain usable throughout.
- The application-status card and preview-before-copy diagnostic summary now include only configured accelerator text and registration booleans. They refresh after successful recovery or rollback and never add clipboard text, source text, screenshots, credentials, or automatic transmission.
- Verified startup conflict visibility, direct focused navigation, successful alternative registration, synchronized diagnostic recovery, alert removal on return to the task surface, conflicting replacement with old-shortcut rollback, and two simultaneous conflicts at 390×844 without horizontal overflow. Evidence: `.audit/shortcut-readiness-2026-07-27/02-before-shortcuts-are-unverified-fields.png`, `04-before-failure-gives-no-shortcut-recovery.png`, `05-after-main-surface-conflict-visible.png` through `10-narrow-shortcut-recovery.png`, and `11-before-vs-real-status.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover renderer routing, focus, recovery copy, rollback presentation, diagnostics, and responsive semantics; source-level checks cover invalid accelerator handling, main-process registration rollback, preload boundaries, and the startup status event. A packaged macOS shortcut-conflict smoke test and a full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for truthful startup readiness, direct focused recovery, atomic shortcut rollback, synchronized privacy-safe diagnostics, usable button fallback, and narrow-layout clarity within the stated evidence limits.

## 2026-07-27 direct shortcut recording follow-up

- Audited the user task “change Option+C or F2 without knowing Electron accelerator syntax.” Previously, Settings exposed editable strings such as `Alt+C`; modifier-only input could be misdiagnosed as another-app conflict, and the two capture shortcuts could be set to the same combination without immediate local explanation.
- Shortcut controls now enter a direct recording state. Visible key chips use macOS-facing names, modifier-only and unsafe unmodified keys receive inline guidance, Escape cancels with a retained-shortcut announcement, and Tab cancels while moving to the next focusable control.
- Candidate accelerators are canonicalized in shared renderer/main-process rules. The interface blocks a duplicate capture shortcut before saving; validation failures remain distinct from macOS registration conflicts; the main process also rejects direct IPC duplicates; and atomic rollback keeps the previous working registration and saved value.
- Verified the old technical fields and misleading modifier-only error, direct recording, modifier guidance, duplicate explanation, successful Option+Shift+C registration, external-conflict rollback to Option+C, Escape cancellation, Tab focus escape, support-summary synchronization, and 390×844 layout without horizontal overflow. Evidence: `.audit/shortcut-recorder-2026-07-27/01-before-technical-text-fields.png` through `08-narrow-direct-key-recording.png`, plus `09-before-vs-direct-recorder.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover renderer recording, copy, focus, rollback presentation, diagnostics, and responsive semantics; source-level checks cover accelerator parsing, validation, duplicate protection, main-process registration reasons, and rollback. A packaged macOS shortcut-conflict smoke test and a full VoiceOver pass were not automated in this follow-up.

follow-up result: passed for no-syntax shortcut editing, immediate guidance and duplicate prevention, truthful invalid/conflict recovery, keyboard exit, synchronized diagnostics, and narrow-layout clarity within the stated evidence limits.

## 2026-07-27 runtime provider recovery follow-up

- Audited the user task “my saved online provider stopped working while I still need this document.” Previously, a rejected credential produced the same small `重试 / 重新截图 / 检查设置` row as a temporary outage. `检查设置` opened the top of Settings with focus on Return, no retained-task confirmation, and no direct validate-and-return path.
- Credential, model, connection, and transient failures now map to different primary actions. A DeepSeek credential rejection offers `更新并验证 API Key`; temporary outages keep retry primary; model and endpoint failures focus their relevant control. The current source and previous valid result remain the fallback throughout.
- Settings opens with the exact recovery control focused and a sticky `当前任务已安全保留` notice. Replacing an online credential revokes the prior full-analysis readiness, requires the fixed-fictional-text metadata and structured-output check again, and leaves re-enablement as an explicit user decision.
- Returning restores the complete 355-character source, clears the obsolete failure, focuses the source/result surface, and leaves one deliberate retry. The successful result reports `在线模型 · DeepSeek · 会发送原文`; the demo fixture no longer inherits the local Ollama label.
- Historical gate (pre-prompt-v2): a live DeepSeek run passed model metadata and the then-current representative structured action-brief check used by Settings. The reusable smoke command reads `DEEPSEEK_API_KEY` only from the process environment and prints no key, prompt, or raw response. Empirical model output also removed a false negative in that compatibility gate: valid exact source substrings may include surrounding text or omit terminal punctuation, while counter-tests still reject evidence that omits the submit or reply action.
- Verified the generic failure before the change, reason-specific credential failure, focused Settings entry, credential replacement, full-analysis validation, explicit re-enable, preserved-source return, and successful retry at the in-app browser's fixed 1280×720 viewport. Evidence: `.audit/provider-failure-recovery-2026-07-27/01-before-submit.png` through `08-after-retry-result.png`, plus `09-before-vs-focused-recovery.png` for the same-viewport comparison.
- Evidence limits: the live check used only a fixed fictional source and one process-scoped credential. The in-app browser did not expose a real viewport resize for this follow-up, so no new 390×844 screenshot is claimed; the existing Settings responsive rules remain covered by prior narrow-layout audits. A packaged app credential-rotation smoke test and full VoiceOver pass were not automated.

follow-up result: passed for reason-specific failure recovery, task retention, focused settings repair, fresh capability validation, truthful provider disclosure, the historical pre-prompt-v2 live DeepSeek compatibility gate, and return-to-retry continuity within the stated evidence limits.

## 2026-07-27 first-use translation fallback follow-up

- Audited the user task “I do not have an API key or local model, so I choose basic translation and later consider upgrading.” The first-use and capture screens promised translation only, but the development processing fixture returned the full action brief, including actions, a deadline, reply guidance, and colored evidence. That contradiction made the product appear to infer capabilities the user had explicitly declined.
- The fallback fixture now creates a real `translation_only` brief. Terms, contexts, deadlines, materials, next steps, verifications, and evidence are empty; two visible warnings explain that the result has not analyzed action fields or run official verification. The result presents the complete translation in source order and one `配置完整分析` path.
- The upgrade path now keeps the panel session mounted behind Settings. A sticky retained-task notice leads with the local-versus-online choice. If the user selects a provider but leaves before saving, validating, and enabling it, the same source and translation return with a specific incomplete-setup warning and one `继续配置完整分析` action. Processing remains disabled until configuration is complete; after enablement, the preserved source offers `用完整分析重新处理`.
- Verified the first-use choice, translation-mode capture disclosure, the contradictory pre-fix result, the truthful translation-only result, focused upgrade entry, incomplete provider setup, preserved-result return, and continue-setup re-entry at the in-app browser's fixed 1280×720 viewport. Evidence: `.audit/first-use-translation-fallback-2026-07-27/01-first-choice.png` through `06-incomplete-upgrade-preserves-translation.png`, plus `07-before-vs-truthful-result.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover the renderer journey, semantic boundaries, focus, and task continuity. The historical pre-prompt-v2 live DeepSeek capability check covered the then-current provider validation contract, but this follow-up intentionally did not resend the retained task or persist a credential. A packaged first-launch smoke test and full VoiceOver pass were not automated.

follow-up result: passed for truthful fallback capability, visible transfer and verification boundaries, task-preserving upgrade entry, incomplete-setup recovery, focused continuation, and full-analysis rerun readiness within the stated evidence limits.

## 2026-07-27 saved credential exit follow-up

- Audited the user task “I am using DeepSeek full analysis but want basic translation for now.” Previously, the fallback control said only that future source text would go to Google/MyMemory. After one click, the DeepSeek credential area disappeared even though the encrypted key remained stored, leaving the user unable to tell whether the secret had been kept, deleted, or transferred. Direct credential deletion was also a single-click action.
- The fallback now names the selected provider credential and explains both destinations separately. The user chooses `保留 DeepSeek 凭据并改用` or `删除 DeepSeek 凭据并改用`; keeping explicitly states that the key stays on this Mac and is never sent to Google/MyMemory. Deletion requires a second confirmation and preserves the current source and previous result.
- The settings transition first pauses setup, then optionally clears every selected-provider connection field, changes the backend, and finally completes translation-only mode. A partial write remains paused with one same-choice retry. Unsaved connection drafts and an active capability test still resolve through their existing guards before either path runs.
- Direct API-key deletion now requires a safe-default confirmation. Escape cancels deletion, restores focus to `删除凭据`, and keeps Settings open; this parent/child keyboard boundary is covered by the credential-exit regression check.
- Verified configured DeepSeek disclosure, explicit keep/delete actions, retained-key return, destructive confirmation, direct-delete keyboard cancellation, completed delete-and-switch, and a fresh DeepSeek setup showing `尚未保存`. Evidence: `.audit/credential-exit-2026-07-27/01-online-analysis-active.png` through `07-after-delete-no-credential.png`, plus `08-before-vs-explicit-credential-choice.png` for the same-viewport comparison.
- Evidence limits: deterministic browser fixtures cover renderer decisions, settings writes, focus, retention/deletion state, and task continuity. The historical pre-prompt-v2 DeepSeek compatibility request had already been exercised with the fixed fictional source in the preceding provider-recovery audit; this follow-up did not resend any user task or persist the chat-supplied credential. A packaged app keychain smoke test and full VoiceOver pass were not automated.

follow-up result: passed for explicit credential ownership, no secret transfer to the translation provider, confirmed local deletion, safe Escape behavior, task preservation, and partial-write recovery within the stated evidence limits.

## 2026-07-27 free-translation outage recovery follow-up

- Audited the user task “basic translation is temporarily unavailable, but I still need this text.” Previously, the error offered `验证当前服务`; that action opened Settings with a retained-task notice but no Google/MyMemory validation control, because the account-free translation endpoints have no API key or user-configurable connection. The user had been sent from an actionable retry into a dead end.
- Free-translation network, timeout, rate-limit, service, and generic processing failures now have their own recovery plan. The error names Google Translate and fallback MyMemory, avoids account-quota wording for the unauthenticated path, preserves the source and previous valid result, and keeps `重试` primary.
- The secondary action is now `改用本机或在线分析`. It opens Settings with the retained-task notice, explains that basic translation has no account setting to validate, focuses the local/online choice, and allows a direct return without changing or losing the retryable source.
- A one-time deterministic outage proves the primary path as well: the first request fails with the new explanation, the same source retries, and the second request reaches a truthful translation-only result without inferred actions, materials, deadlines, evidence, or verification.
- Verified the translation-ready state, misleading pre-fix action, pre-fix Settings dead end, actionable post-fix error, focused processing choice, return-to-retry continuity, and successful transient retry at 1280×720. Evidence: `.audit/free-translation-failure-2026-07-27/01-translation-ready.png` through `06-after-retry-success.png`, plus `07-before-vs-actionable-recovery.png` and `08-dead-end-vs-focused-choice.png` for same-viewport comparisons.
- Evidence limits: deterministic browser fixtures cover renderer error classification, focus, retained task state, alternate-processing navigation, and successful retry. They do not prove real Google/MyMemory availability, third-party retention behavior, a packaged macOS network-loss smoke test, or full VoiceOver output.

follow-up result: passed for provider-specific outage truthfulness, direct retry, dead-end removal, focused alternate processing, source/result retention, and transient recovery within the stated evidence limits.

## 2026-07-27 free-translation fallback integrity follow-up

- Audited the ordinary user task “translate a passage without configuring an account.” The advertised MyMemory fallback reused Google's `auto` source value, but MyMemory rejects `auto` with status 403; Google downtime therefore made the fallback unavailable during the default automatic-language flow.
- Automatic direction now keeps Google's native detection while inferring a concrete `en|zh-CN` or `zh-CN|en` pair for MyMemory. Missing or malformed fallback payloads fail closed instead of being treated as a successful translation.
- The same walkthrough exposed a privacy-copy mismatch: before submission the interface named only Google Translate even though failure would cause a second complete-source transfer to MyMemory. The submit and in-progress disclosures now state the order and condition explicitly.
- Verified MyMemory's invalid and valid language-pair behavior against its official endpoint with fixed fictional text, then forced Google failure and completed live English-to-Chinese and Chinese-to-English translations through MyMemory. Deterministic regression checks cover both direction selection and the actual fallback URL.
- Verified the user journey at 1280×720: empty capture, truthful destination disclosure, safe fictional sample, in-progress transfer disclosure, and a clearly bounded translation-only result. Evidence: `.audit/free-translation-fallback-2026-07-27/01-translation-entry.png` through `05-translation-result.png`.
- Evidence limits: live checks prove MyMemory fallback behavior with fixed fictional text, while deterministic browser fixtures prove renderer copy and state. They do not establish continuing third-party uptime, provider data-retention behavior, packaged offline behavior, or full VoiceOver output.

follow-up result: passed for functional fallback, explicit second-provider disclosure, translation-only boundaries, safe sample control, and stable desktop layout within the stated evidence limits.

## 2026-07-27 cancellation settlement integrity follow-up

- Audited the user task “stop a slow official-source lookup without losing the current result.” The renderer previously treated a rejected cancel call as failure, but ignored a resolved `false` acknowledgement. That left the lookup permanently showing `正在停止…`, removed the usable cancel action, and gave no truthful network-state explanation.
- The production main process also returned `true` immediately after calling `abort()`. It did not wait for analysis, official verification, provider validation, or screenshot capture to settle, so the interface could claim that work stopped while the underlying task was still finishing.
- Every cancellable task now owns a one-shot settlement signal. Cancellation captures the tasks active at request time, aborts them, and returns success only when all captured tasks settle within a bounded 2.5-second wait. A timeout returns `false`; it never converts an unresponsive network or model task into a successful stop.
- Official-source lookup now handles both rejected and explicitly unconfirmed stops. The `正在停止…` state clears, a visible notice says the lookup continues, and `取消查找` becomes available again. When settlement is confirmed, the preserved result and same approval plan return with an explicit stopped notice and focused retry action.
- Verified result preservation, approval preview, running lookup, the stuck pre-fix state, the recoverable unconfirmed state, and the confirmed-stop state at 1280×720. Evidence: `.audit/cancel-settlement-2026-07-27/01-result-before-lookup.png` through `06-after-confirmed-stop.png`.
- Evidence limits: deterministic browser fixtures prove renderer state, retryability, focus, and copy. Unit checks prove multi-task settlement, idempotence, timeout, and rejected-task semantics; source-level checks bind the main IPC handler to the settlement wait. A packaged app test against a deliberately abort-resistant real provider and a full VoiceOver pass were not automated.

follow-up result: passed for truthful stop acknowledgement, bounded failure recovery, preserved result/approval state, retryable official lookup, and stable desktop layout within the stated evidence limits.

## 2026-07-27 official-source action follow-up

- Audited the post-retrieval task “open the official page or copy its link.” Before the fix, both controls kept the same label after activation; a copy had no visible confirmation, and an external-open rejection had no user-facing recovery.
- Opening now exposes a pending state, reports success only after the main process confirms handoff to the default browser, and explicitly says that handoff is not semantic verification. A failed handoff leaves the result unchanged and turns the control into `打开失败 · 重试`, with copy-link named as the fallback.
- Copying now exposes pending, success, and retryable failure labels, uses an assertive error announcement, and reuses the guarded clipboard-clear authority. Clearing resets the copied button state and never deletes newer clipboard content.
- Historical gate (pre-prompt-v2): a real DeepSeek check used an ephemeral environment-only credential and passed both model metadata and the then-current representative structured-brief validation with `deepseek-v4-flash` in about 14.2 seconds. The credential was not written to source, settings, logs, or audit artifacts.
- Accepted browser evidence: `.audit/official-source-actions-2026-07-27/01-source-approval.png` through `09-final-copy-confirmed.png`. Deterministic fixtures cover one-time open failure and persistent clipboard failure; DOM inspection confirmed named buttons, busy states, polite success announcements, assertive errors, and guarded clearing.
- Evidence limits: the in-app browser fixture proves renderer behavior but simulates the operating-system browser handoff. Main-process validation and DNS/SSRF guards are covered by existing automated checks; this pass did not automate VoiceOver speech order or prove behavior for every installed default browser.

follow-up result: passed for truthful source opening, copy confirmation, failure recovery, clipboard cleanup, and explicit retrieval-versus-verification boundaries within the stated evidence limits.

## 2026-07-27 official-source action affordance follow-up

- Audited the post-retrieval task “open this official page or copy its address.” The actions were accessible buttons but visually rendered as 11px helper text with a thin vertical target, so the most important next step was easy to overlook beside dense publisher, quote, URL, and verification metadata.
- Every official-source open and copy control now uses an explicit surface with a persistent border, 32px minimum height, hover response, and three-pixel `:focus-visible` ring. The outcome-specific labels from the preceding follow-up retain the same treatment.
- Citation and retrieval-receipt action groups now wrap instead of compressing source content. At the 520px source-card breakpoint they align to the readable content edge; the longer `打开失败 · 重试` state remains distinct from `复制链接` and the visible failure notice.
- Accepted browser evidence: `.audit/source-action-affordance-2026-07-27/01-small-source-actions-before.png`, `03-larger-source-actions-final.png`, and `04-long-error-label-wrap.png`. The targeted source-action regression check binds class assignment, target height, group wrapping, and narrow alignment.
- Evidence limits: the in-app browser used its fixed 1280×720 viewport. Narrow behavior is covered by responsive source assertions rather than a new narrow screenshot, the preview simulates operating-system handoff failure, and a full VoiceOver pass was not automated.

follow-up result: passed for visible action hierarchy, practical target size, keyboard focus treatment, long-label failure recovery, and source-card wrapping within the stated evidence limits.

## 2026-07-27 processing-surface privacy follow-up

- Audited the main task “submit a sensitive English message, wait, and cancel if needed.” The processing card repeated the first 160 characters of the source after submission, including a fictional passport-request excerpt. That content was no longer needed for editing, contradicted the privacy-safe notification boundary, and increased shoulder-surfing exposure.
- Replaced the excerpt with a semantic note containing only the source type, exact retained character count, and an explicit explanation that processing does not repeat the content. The selected local or online destination and full-source transfer disclosure remain immediately below it.
- The summary is derived from `sourceType` and `inputText.length`; no source substring is copied into the processing component or DOM. Regression checks bind the metadata-only contract and reject the former preview construction.
- A fresh deterministic DeepSeek preview session loaded the 355-character safe sample, submitted it, and cancelled a slow first analysis. The complete source returned to the focused editor with the existing retained-source confirmation.
- Accepted evidence: `.audit/main-task-continuity-2026-07-27/01-capture-entry.png` through `04-cancel-restores-source.png`, plus `05-before-after-comparison.png` for the same-viewport comparison.
- Evidence limits: the browser fixture proves renderer content, semantic labels, focus, and task continuity at 1280×720; it does not prove third-party retention, prevent operating-system screenshots, automate VoiceOver speech, or replace a packaged macOS shoulder-surfing review.

follow-up result: passed for no raw source in the waiting DOM, persistent destination disclosure, explicit privacy rationale, exact retained count, confirmed cancellation recovery, and stable desktop layout within the stated evidence limits.

## 2026-07-27 guided-reply completion follow-up

- Audited the user task “prepare the required confirmation reply and copy it into email.” After choosing `我已完成原文要求`, the generated draft still ended with `[Your name]`, but `复制回复` was immediately enabled. The placeholder sat below the visible portion of the textarea, so a user could copy a polished-looking but unfinished real-world message without seeing the defect.
- Added a fail-closed placeholder check for generated bracket instructions such as `[Your name]` and `[Insert reference]`. The drawer now reports the exact remaining count, associates the warning with the textarea, and keeps copy disabled until every recognized placeholder is replaced.
- Replacing the placeholder with `Li Ming` removes the warning and enables copy. The existing clipboard confirmation and guarded clear authority remain intact.
- The same walkthrough exposed a version-truthfulness issue: after copying one reply and changing completion status, the new draft still showed `英文回复已复制到系统剪贴板`. The notice now says the clipboard contains the previous version, explains that it does not update automatically, and keeps the old copy safely clearable.
- Accepted final evidence: `.audit/action-execution-continuity-2026-07-27/01-result-actions-entry.png`, `02-reply-status-choice.png`, `06-copy-confirmed.png`, `09-placeholder-before-after.png`, `10-stale-copy-before-after.png`, and `11-outdated-copy-cleared.png`. The intervening current-run captures are retained as supporting states.
- Evidence limits: the deterministic browser fixture proves renderer copy gates, semantic alerts, draft editing, clipboard-version feedback, and guarded clearing at 1280×720. It does not prove real mail-client paste behavior, test every user-authored bracket expression, automate VoiceOver speech, or replace a packaged macOS keyboard pass.

follow-up result: passed for unresolved-placeholder blocking, visible and assistive recovery guidance, copy re-enablement after a real edit, truthful clipboard versioning, and guarded previous-copy clearing within the stated evidence limits.

## 2026-07-27 deadline urgency follow-up

- Audited the user task “decide whether a source-backed deadline needs action now.” The result header previously showed only `截止日期 · 2026 年 7 月 28 日`, leaving the user to calculate whether it was tomorrow or already overdue. The deterministic fixture also represented that date-only source as a fabricated UTC end-of-day instant.
- Added a separate `calendarDate` contract for an unambiguous full date. `normalizedAt` remains reserved for a real instant backed by source date, time, and timezone. Invalid or impossible calendar dates are cleared with a warning rather than parsed from display text.
- The result header now makes relative urgency primary while preserving the exact date. It exposes the same meaning in its accessible name, does not rely on color alone, and schedules a refresh just after the next local midnight so a long-open result does not keep yesterday's label.
- Accepted browser evidence: `.audit/deadline-urgency-2026-07-27/01-absolute-date-before.png`, `02-relative-urgency-after.png`, and `03-before-after-comparison.png`. The current DOM names the element `截止日期，明天截止，2026 年 7 月 28 日`.
- Deterministic checks cover today, tomorrow, 2–7 days, later dates, overdue dates, invalid values, trustworthy full-instant fallback, and midnight refresh timing. The browser fixture covers the tomorrow state at 1280×720; it does not automate VoiceOver speech, timezone changes during an open session, or packaged locale behavior.

follow-up result: passed for truthful date semantics, immediate urgency recognition, preserved absolute dates, non-color assistive meaning, invalid-date failure safety, and local-midnight refresh within the stated evidence limits.

## 2026-07-27 multi-deadline priority follow-up

- Audited the user task “find the first required deadline when one source contains several dates.” The deterministic source mentions material receipt by 28 July before a mandatory share-code action due 27 July. The previous header always selected the first grounded deadline, so it prominently said “明天截止” while the action list contained a requirement due today.
- Added source-grounded deadline selection that ranks dates linked to mandatory user action before other user, linked non-user, or unlinked dates. Within the strongest priority group, only validated calendar dates or genuine instants are sorted. An earlier institution-only date cannot displace a required user action, and any incomparable candidate makes the selector preserve source order instead of guessing.
- The headline and urgency pill now use the same selected deadline. The pill says “最早截止” or “优先截止” only when that claim is supported, names the total count, exposes a chevron and keyboard focus treatment, and opens the deadline disclosure with a truthful live announcement.
- Every expanded deadline card now shows its own today/tomorrow/remaining/overdue state, exact date, condition, provenance, and evidence. The selected card is labeled `顶部优先提醒`. Deadline metadata remains visible below 720px instead of being removed from the narrow result header.
- Accepted browser evidence: `.audit/multi-deadline-priority-2026-07-27/01-later-deadline-promoted-before.png`, `02-earliest-deadline-promoted-after.png`, `03-all-deadlines-expanded.png`, and `04-before-after-comparison.png`.
- Evidence limits: the current browser run used a fixed 1280×720 viewport. Narrow visibility is source-level regression coverage rather than a screenshot. Automated cases cover mandatory-user versus institution-only ranking and incomparable dates, but do not replace VoiceOver, real-provider ordering variation, locale, timezone, or zoom testing.

follow-up result: passed for correct required-action priority, honest chronology, visible multi-deadline count, direct all-date inspection, per-date urgency, source evidence continuity, keyboard focus, and narrow metadata retention within the stated evidence limits.

## 2026-07-27 action progress follow-up

- Audited the user task “work through the action path and remember what is already done.” Before the fix, all four source-backed actions were static: there was no completion control, progress summary, or way for a copied checklist to carry execution state.
- Added native checkbox controls with explicit `标记完成` / `已标记完成` labels, a live `你已标记 x / y 项完成` summary, reversible state, and icon-library completion treatment. Completed actions keep their evidence visible; the interface explicitly says this is the user's current-window record and not Slipstream verification.
- The same record now survives Settings navigation, ten-second clear undo, and sanitized 30-minute same-window interruption recovery. A genuinely new result resets it. Copied action checklists label each generated action `[待完成]` or `[已完成]`.
- Guided reply preparation receives the progress count only as context and still requires a separate real-world status choice, so unchecked actions do not block a truthful reply and checked actions cannot silently authorize a completion claim.
- Accepted browser evidence: `.audit/action-progress-2026-07-27/01-static-actions-before.png`, `02-progress-initial.png`, `03-one-complete.png`, `05-all-complete-top.png`, `07-reply-progress-context.png`, `08-unmark-restored.png`, `09-clear-undo-preserved.png`, and `10-before-vs-progress.png`.
- Evidence limits: the current in-app browser run used a fixed 1280×720 viewport and deterministic result fixture. Native checkbox semantics, focus styling, copy text, state sanitization, and recovery wiring have automated coverage; this pass does not prove external task completion, automate VoiceOver speech, or replace a packaged macOS keyboard/zoom review.

follow-up result: passed for honest self-reported progress, reversible completion, evidence continuity, copied checklist state, reply safety context, settings preservation, clear undo, and bounded interruption recovery within the stated evidence limits.

## 2026-07-27 completed-action focus follow-up

- Audited the user task “after finishing several actions, find and continue the next unfinished one.” With three of four actions marked complete, the previous interface still rendered every completed evidence card at full height. The only unfinished reply action was present in the DOM but reduced to a partially visible purple step marker below the first viewport.
- Completed actions now replace expanded evidence with an independent `查看原文依据 · n 条` disclosure. The progress summary, title, provenance, completion control, required detail, and ordered connector remain visible, while the unfinished action and its evidence move fully into the first viewport.
- Expanding one completed action reveals only its own exact evidence and changes the control to `收起原文依据`. Removing the completion mark removes the disclosure and restores the evidence immediately; marking it again returns to the compact state.
- Settings round trips keep the same completion record and compact presentation. Action-checklist copying remains available and retains pending/completed labels. The evidence region remains in the DOM with a safe generated ID and `hidden` state so `aria-controls` keeps a valid relationship.
- Accepted browser evidence: `.audit/completed-action-focus-2026-07-27/01-three-complete-next-hidden-before.png`, `03-completed-evidence-expanded.png`, `04-unmark-keeps-evidence.png`, `07-final-all-complete-compact.png`, `08-final-three-complete-next-visible.png`, and `09-before-after-comparison.png`.
- Evidence limits: the current in-app browser run used the deterministic 1280×720 result fixture. Native button semantics, named expansion state, focus styling, hidden-region wiring, copy continuity, and existing completion recovery have automated coverage; this pass does not automate VoiceOver speech, 200% zoom, or unusually long provider-generated action titles.

follow-up result: passed for next-action visibility, compact completion, independent evidence expansion, reversible disclosure state, source traceability, settings continuity, and copy preservation within the stated evidence limits.

## 2026-07-27 action-order integrity follow-up

- Audited the user task “follow the generated action path in the order shown.” The deterministic result first asked the user to prepare an `eVisa share code`, then asked them to generate that same code in the next step. This turned source-sentence grouping into a contradictory execution order even though the process context elsewhere described the correct sequence.
- Added optional direct prerequisite references to normalized next steps. The main process resolves only valid candidate indices, rejects missing, self, duplicate, and cyclic relationships, and applies a stable topological order. Cycles preserve the original order only after all dependency claims are removed and the brief is downgraded with a visible warning.
- The fixture now keeps the earlier share-code generation deadline first, separates passport preparation from share-code creation, makes submission depend on both, and makes the reply depend on submission. The result and copied checklist name those numbered prerequisites.
- Completion remains self-reported and reversible. As prerequisites are marked, the dependent step narrows from `建议顺序：先完成第 1、2 项` to the remaining prerequisite and then to `前置步骤已标记完成`. If a prerequisite is later unmarked while the dependent step stays complete, the UI announces `进度需核对` instead of silently changing the user's record.
- Accepted browser evidence: `.audit/action-order-integrity-2026-07-27/01-conflicting-order-before.png`, `03-prerequisites-marked.png`, `04-progress-order-conflict.png`, `05-before-after-comparison.png`, `06-copy-confirmed.png`, and `07-final-corrected-order.png`. `02-corrected-order-after.png` is retained as supporting evidence from the same run.
- Evidence limits: the in-app Browser run used a deterministic 1280×720 fixture. It proves visible ordering, dependency state changes, reversible progress, and copy confirmation. Automated normalization cases cover out-of-order input, missing/self/duplicate references, cycles, prompt requirements, and copied prerequisite text; this pass does not prove every provider will infer every dependency, automate VoiceOver speech, or replace packaged zoom and locale testing.

follow-up result: passed for executable ordering, explicit direct prerequisites, safe invalid/cycle handling, truthful reversible progress, copied dependency context, and evidence continuity within the stated limits.

## 2026-07-27 reply-progress consistency follow-up

- Audited the user task “move from an unfinished action path into the required confirmation reply.” With `0 / 4` actions marked, choosing `我已完成原文要求` immediately produced a credible completion claim. The overall progress stayed visible, but the interface did not turn the contradiction into a decision; replacing `[Your name]` would have enabled copy.
- Added a separate pre-reply progress model containing only mandatory user actions before the reply. The reply action itself, optional actions, and institution actions are excluded, so the deterministic path is truthfully complete at `3 / 3` even while the overall checklist is `3 / 4`.
- A completed claim with missing pre-reply actions now produces a semantic alert, names the remaining step numbers, blocks copy, and keeps the safer `我还没有完成` choice available without friction. Users whose checklist is stale may explicitly confirm reality; the acknowledgment is ephemeral, does not mark any action complete, and resets on status change or drawer reopen.
- The sticky drawer footer remains visible when the warning lengthens the flow and reports all current blockers. Acknowledging the mismatch removes only that blocker; unresolved generated placeholders still prevent copy. Replacing the placeholder enables the button and the existing guarded clipboard confirmation remains intact.
- A browser walkthrough verified the consistent `3 / 3` path has no mismatch alert or extra acknowledgment, while switching to the in-progress status removes the gate and reopening restores the unacknowledged state.
- Accepted evidence: `.audit/reply-progress-consistency-2026-07-27/01-zero-progress-reply-entry.png`, `03-completed-claim-no-mismatch-gate-before.png`, `06-mismatch-gate-with-copy-summary.png`, `07-mismatch-acknowledged.png`, `08-acknowledged-and-filled-ready.png`, `09-before-after-comparison.png`, `11-three-of-three-no-extra-gate.png`, and `12-final-mismatch-gate.png`. Other captures in the folder are retained as supporting states.
- Evidence limits: the deterministic in-app browser fixture proves renderer behavior, semantic alert/checkbox/button states, edit and copy gating, and reset behavior at 1280×720. It does not verify real-world completion, automate VoiceOver speech, prove paste behavior in a mail client, or replace a packaged macOS keyboard/zoom pass.

follow-up result: passed for contradiction detection, fail-closed copy, explicit stale-checklist recovery, placeholder composition, consistent-path non-interference, ephemeral reset, sticky blocker visibility, and truthful progress wording within the stated evidence limits.

## 2026-07-27 completed-task closure follow-up

- Audited the user task “finish the required reply, mark the last action done, and leave the completed task.” After all four actions were marked, the previous interface still showed `需要回复`, kept `准备英文回复` as the main action, labeled model timing only as `完成`, and offered the quiet destructive label `清空并返回`. The product showed four completed checkboxes without presenting a coherent task-complete state.
- Added a derived completion state that accepts only action IDs in the current result and separately identifies whether the source-required reply action is marked complete. Empty action paths and stale IDs cannot create a completed state.
- At full completion, the progress card now says all actions were marked, retains the explicit no-external-verification boundary, and points to a reversible exit. The header says `回复已标记完成`, repeat reply preparation becomes secondary, model timing says `处理完成`, and `完成并返回` becomes the primary footer action.
- Removing any completion mark immediately restores `需要回复`, the primary `准备英文回复`, `x / y` progress, and the ordinary `清空并返回` action. No completion state is persisted or inferred beyond the existing self-reported record.
- `完成并返回` reuses the existing memory-only clear snapshot and ten-second countdown. The capture screen receives focused undo; using it restores the full result and all four completion marks while keeping official-lookup approval revoked.
- Accepted evidence: `.audit/task-closure-2026-07-27/02-three-of-four-ready-to-reply.png`, `03-reply-copied.png`, `05-all-actions-complete-before.png`, `06-all-actions-complete-after.png`, `08-complete-and-return-undo.png`, `09-undo-restores-completed-task.png`, and `10-before-after-comparison.png`. `01`, `04`, and `07` are retained as supporting states from the same run.
- Evidence limits: the deterministic in-app browser fixture proves visible labels, semantic status/button changes, reversible progress, clear-and-undo behavior, and focusable native controls at 1280×720. It does not verify that the external reply was sent, automate VoiceOver announcements, prove 200% zoom reflow, or replace a packaged macOS keyboard pass.

follow-up result: passed for coherent self-reported completion, source-truth preservation, processing/task-state separation, reversible unmarking, primary completed-task exit, and full clear-undo restoration within the stated evidence limits.

## 2026-07-27 completed-task clipboard privacy follow-up

- Audited the user task “copy a sensitive reply, finish the task, and leave the result.” Before the fix, the copied reply remained in the macOS system clipboard, but the privacy notice and guarded `清除剪贴板` action were local to the unmounted result component. The capture screen showed only task undo, so the product had retained the sensitive content while removing its safe cleanup control.
- Clipboard notice state now belongs to the task surface rather than the result view. Leaving a task converts a copied or outdated notice into an explicit `任务已结束，系统剪贴板仍保留…` warning and keeps the sender-bound clear token without storing clipboard plaintext.
- Ten-second result undo remounts against the same clipboard state. Repeated exit/undo cycles preserve the authority instead of silently dismissing it. A new result does not overwrite the active cleanup decision.
- Clearing still uses the main-process fingerprint guard. A matching copy reports confirmed removal; externally replaced content reports `已有新内容，没有清除`; an IPC failure says confirmation was not received, keeps the token, and offers `重试清除` without claiming a result it cannot prove.
- Accepted evidence: `.audit/clipboard-exit-2026-07-27/03-clear-control-lost-after-exit-before.png`, `04-all-complete-before-exit-after.png`, `05-clipboard-control-preserved-after-exit.png`, `06-clipboard-cleared-safely-after-exit.png`, `07-external-clipboard-change-preserved.png`, and `08-clear-failure-remains-retryable.png`. The folder also retains copied-reply and completed-result supporting states plus `audit-notes.md`.
- Evidence limits: the deterministic in-app browser fixture proves renderer state, semantic status/alert roles, task undo continuity, guarded outcomes, and retry affordance at 1280×720. It simulates clipboard result codes and does not replace a packaged macOS pasteboard race test, automate VoiceOver announcement order, or prove clipboard behavior in every third-party application.

follow-up result: passed for post-exit cleanup visibility, plaintext-free state lifting, repeated undo continuity, matching-only removal, external-content preservation, and honest retryable failure feedback within the stated evidence limits.

## 2026-07-27 clipboard-aware application exit follow-up

- Audited the user task “finish a task, leave a sensitive result in the system clipboard, then quit Slipstream.” Before the fix, once the ten-second task undo expired, the quit handshake saw no remaining risk and auto-confirmed exit even while the UI still warned that the copied result remained in the system clipboard. The in-memory matching authority would then be revoked with no explicit decision.
- A live guarded copy is now part of the renderer-confirmed quit risk. The decision names the system clipboard consequence and presents three distinct actions: continue using Slipstream, clear the matching copy before exit, or knowingly keep the clipboard and exit.
- `清除剪贴板并退出` first invokes the existing sender-bound fingerprint guard. Only confirmed `cleared` or `changed` outcomes proceed to the quit decision; the latter means newer external content was preserved. Rejected, missing, or expired cleanup leaves the application open with an assertive explanation.
- The clipboard risk is pinned for the open decision so an expired authority cannot relabel the dialog as risk-free or trigger the existing automatic-settlement path. The unusable clear action is removed while the explicit keep-and-exit choice remains.
- The waiting state disables every exit choice, names matching-only removal, and says the application will not exit before system confirmation. Canceling returns to the intact clipboard notice and cleanup control.
- Accepted evidence: `.audit/clipboard-quit-2026-07-27/01-clipboard-risk-auto-quit-before.png`, `02-clipboard-aware-quit-dialog.png`, `03-cleared-before-preview-quit.png`, `04-new-clipboard-content-safe-quit.png`, `05-clear-failure-blocks-quit.png`, and `06-clear-and-quit-pending.png`, with `audit-notes.md` in the same folder.
- Evidence limits: the deterministic in-app browser fixture proves renderer decision state, focusable native controls, pending/error semantics, guarded outcome routing, and preview quit behavior at 1280×720. It simulates the operating-system quit and clipboard result codes; packaged macOS termination timing, VoiceOver announcement order, 200% zoom, and third-party pasteboard races remain outside this browser pass.

follow-up result: passed for clipboard-aware quit blocking, explicit retain-versus-remove consent, matching-only cleanup, external-content preservation, failure-safe settlement, stable risk wording, and accessible pending/error feedback within the stated evidence limits.

## 2026-07-27 saved credential visibility follow-up

- Audited the user task “return to Settings after saving a DeepSeek API key, understand whether it can be viewed, and rotate it safely.” The main process correctly returned only a `hasDeepseekApiKey` flag, but the empty redacted field still exposed an eye action named `显示 API Key`. Activating it changed the empty input to text mode and renamed the action to `隐藏 API Key`, falsely suggesting that the stored credential might be available to reveal.
- A saved secret now has a directly associated explanation: `已保存的密钥不会显示`, followed by the renderer boundary and replacement behavior. The empty redacted field has no reveal control. Its accessible description is wired through `aria-describedby` without adding secret material to the DOM.
- Typing a replacement creates a 32px keyboard-focusable reveal action labeled `显示新输入的 API Key`; its pressed state and hide label apply only to that draft. Clearing, saving, deleting, or changing providers resets visibility. A successful replacement still invalidates the previous capability result and requires the fixed fictional readiness check before full analysis can be re-enabled.
- Accepted evidence: `.audit/saved-credential-visibility-2026-07-27/01-saved-key-misleading-reveal-before.png`, `02-saved-key-protected-after.png`, `03-replacement-key-masked.png`, and `04-after-rotation-requires-revalidation.png`, with `audit-notes.md` in the same folder.
- Evidence limits: the deterministic in-app browser fixture proves the renderer's redacted, replacement, reveal/hide, save, revalidation, and accessible-description states for DeepSeek at 1280×720. The same shared component covers OpenAI, Anthropic, and a custom API key, with state coverage in the credential-visibility regression check. This pass does not retrieve a real Keychain value, automate VoiceOver speech order, test password-manager extensions, or replace a packaged macOS key-rotation smoke test.

follow-up result: passed for truthful redaction, stored-versus-new credential separation, masked replacement entry, explicit reveal semantics, safe visibility reset, and post-rotation revalidation within the stated evidence limits.

## 2026-07-27 full-data reset follow-up

- Audited the user task “finish a sensitive result, then use Settings to clear all local data and start over.” Before the fix, the confirmation promised to clear all local data and return to first use, but only the persisted settings transaction ran. The hidden result surface stayed mounted, `panelSessionStarted` stayed true, and the user returned directly to the old source-backed result; action progress, clear undo, pending captures, and the same-window recovery record could also survive.
- Reset now uses one tested order: resolve any live system-clipboard choice, purge renderer session data, then clear persistent data in the main process. The renderer purge invalidates active requests, removes current source/result/last-good state, progress, correction drafts, clear undo, pending clipboard and screenshot decisions, clipboard notices, and the session-storage recovery record. App-owned deferred capture state is cleared in the same stage.
- Success resets the remembered panel session and opens only the first-use choice. A simulated first persistent failure leaves Settings open and says the current session is already cleared while credentials, terms, or settings remain unconfirmed; retry completes the remaining stage. Clipboard cleanup failure stops before app data changes. A live guarded copy adds separate matching-only clear and explicit preserve actions because macOS clipboard data is outside the app store.
- Accepted evidence: `.audit/full-data-reset-2026-07-27/before-reset-old-result-remains.png`, `after-reset-confirmation.png`, `after-reset-first-use.png`, `after-reset-clipboard-choice.png`, `after-reset-partial-failure.png`, and `before-after-comparison.png`, with `audit-notes.md` in the same folder.
- Evidence limits: the deterministic in-app browser fixture proves the 1280×720 renderer states, focusable native actions, first-use destination, clipboard decision, and retryable partial-failure copy. The transaction and failure ordering have automated coverage. This pass simulates the main-process reset and clipboard outcome; it does not replace a packaged macOS Keychain/settings-directory deletion inspection, VoiceOver announcement-order pass, or third-party pasteboard race test.

follow-up result: passed for complete renderer-session purge, ordered persistent deletion, first-use-only success, explicit clipboard ownership, failure-safe settlement, and truthful retry within the stated evidence limits.

## 2026-07-27 app-quit modal ownership follow-up

- Audited the user task “open Settings over a completed result, then dismiss the application quit confirmation with Escape.” Before the fix, Settings listened on `window` capture while the visually higher quit dialog listened only on its own element. The first Escape unmounted Settings behind the dialog, left the quit decision open, and destroyed the intended focus-return target; a second Escape was required.
- The quit dialog now declares the app top layer and consumes Escape from `window` capture, including the frame before initial focus moves. Every background Settings capture handler first yields to that owner. Closing restores each sibling's previous `inert`/`aria-hidden` state and returns focus after the settled render to the exact surviving control or a named safe fallback.
- Strict last-in-first-out behavior was verified for the destructive full-data-reset confirmation: one Escape removes only app quit, keeps the reset confirmation mounted, and restores focus to `保留我的数据`; the next Escape closes reset and returns focus to its trigger.
- Option+C/F2 requests arriving during app quit remain memory-only and do not switch the background view or create a hidden draft/provider decision. The existing pending request is processed only after app quit is canceled. An in-progress full-data reset is also included in quit risk until persistent clearing finishes.
- The visual pass exposed an unrelated default English `Details` label above the ordinary support card. Regular Settings now renders that expanded content in a normal container; only the first-use support entry retains disclosure semantics.
- Accepted browser evidence: `.audit/quit-modal-ownership-2026-07-27/01-before-quit-over-settings.png`, `02-before-first-escape-closes-settings.png`, `04-after-focus-restored.png`, `05-after-nested-reset-preserved.png`, `06-before-after-comparison.png`, and `07-after-support-semantic-fix.png`, with `audit-notes.md` in the same folder. `03-after-one-escape-keeps-settings.png` is retained as intermediate evidence before the two-frame focus-return hardening.
- Evidence limits: the deterministic in-app browser fixture proves visible layer order, single-Escape transitions, exact CSS focus targets, nested confirmation preservation, post-decision shortcut release, and the ordinary Settings support label at 1280×720. Source-backed regression checks cover all four background window-capture handlers, initial-focus timing, reset quit risk, queued capture ownership, and inert restoration. This pass does not automate VoiceOver announcement order, 200% zoom, every credential-removal variant in the browser, or packaged macOS menu-key timing.

follow-up result: passed for strict top-layer ownership, one-transition-per-Escape behavior, exact focus return, nested decision preservation, shortcut queuing, reset quit safety, inert-state restoration, and removal of the stray default disclosure label within the stated evidence limits.

## 2026-07-27 saved-term deletion ownership and reset settlement follow-up

- Audited the user task “remove one saved term, notice the card beneath move into the same pointer position, then recover the deletion.” Before the fix, both fast reflow clicks and deliberate sequential removal could delete A and B while the library retained only B in its single undo slot. Undoing restored B and silently left A permanently removed from renderer state and persistent storage.
- Saved-term removal now uses a pure four-phase transaction (`idle → deleting → undo → restoring`) plus a synchronously updated ref. A second event cannot begin before React renders the disabled state, and no new deletion can replace the pending recovery right.
- While a deletion is unresolved, remaining remove controls and import/export are disabled and the visible sentence explains why. The ambiguous dismissal icon is replaced by the explicit secondary action `保留删除`; only Undo or Keep releases the transaction.
- Focus now follows ownership: successful deletion moves to `撤销删除`, successful undo returns to the exact restored term's remove action, and Keep returns to the next enabled remove action or a safe search/close fallback. Restore failure keeps the same term and recovery action.
- The same audit found a P0 state-machine issue around full-data reset plus application quit. Reset success previously erased only renderer quit state, leaving the main-process registry pending and able to suppress future quit events. Reset now preserves the request and lets the existing risk-settlement effect confirm it when reset risk clears.
- Historical gate (pre-prompt-v2): a real DeepSeek compatibility smoke test passed metadata discovery and a complete `deepseek-v4-flash` structured analysis through the then-current production prompt, parser, and representative ActionBrief gate. The supplied credential was runtime-only and was not added to the repository.
- Accepted evidence: `.audit/maturity-reset-terms-2026-07-27/03-first-term-delete-before.png`, `04-second-delete-loses-first-undo-before.png`, `05-after-first-delete-locks-next.png`, `06-after-undo-restores-exact-term.png`, `07-after-keep-enables-next.png`, and `08-before-after-comparison.png`, with `audit-notes.md` in the same folder. Reset evidence `01` and `02` remains accepted evidence for the next modal-containment slice.
- Evidence limits: the deterministic in-app browser fixture proves visible one-transaction behavior, disabled controls, item counts, and exact focus outcomes at 1280 × 720. Automated coverage proves transition ownership and reset source-state settlement; this pass does not inspect saved terms after a real relaunch, automate VoiceOver or 200% zoom, or reproduce slow reset plus packaged Cmd+Q against the main-process registry.

follow-up result: passed for one-gesture deletion safety, non-overwritable recovery ownership, explicit deletion finalization, exact undo, focus continuity, reset quit-request preservation, the historical pre-prompt-v2 live DeepSeek compatibility gate, and evidence continuity within the stated limits. Full reset modal containment remains the next priority.

## 2026-07-27 full-data reset modal containment follow-up

- Audited the user task “clear every application-owned record while Settings, keyboard focus, and a possible app-quit request all compete for control.” The previous reset `alertdialog` was an inline card inside the Settings scroll area: the background stayed clickable, focus could move to `刷新状态`, and no modal layer owned Tab or Escape.
- Reset confirmation is now a direct Settings modal with `aria-modal`, exact sibling `inert`/`aria-hidden` snapshot restoration, a complete Tab trap, safe initial focus, busy-root focus, and pointer-blocking backdrop. Escape is consumed while clearing; idle dismissal returns to the exact reset trigger after the settled render.
- A persistent-layer failure keeps the same modal open, focuses an accessible error, explains that the session is already gone, and aligns the visible and accessible retry name. If AppQuit is above it, failure focus waits until that top layer leaves and then returns to the recovery error.
- Reset-in-progress and reset-recovery are now separate quit risks. The former truthfully says clear completion will settle the pending exit; the latter prevents automatic exit and requires an explicit choice to continue recovery or abandon it. Development quit requests use fresh one-shot IDs and reject stale decisions.
- Verified normal confirmation, background containment, Tab/Shift+Tab wrap, exact Escape return, five-second busy state, first persistent failure and retry, successful reset/quit settlement, failed reset/quit preservation, and 520×680 stacked actions with no horizontal overflow.
- Accepted evidence: `.audit/reset-modal-containment-2026-07-27/01-inline-reset-before.png` through `14-before-vs-after.png`, with the full numbered audit in `audit-notes.md`.
- Evidence limits: deterministic browser IPC proves visible renderer state, DOM semantics, focus targets, pointer hit testing, and request overlap at 1280×720 and 520×680. Source checks prove exact state restoration and one-shot decision semantics. Packaged Cmd+Q timing, Keychain/filesystem inspection, VoiceOver speech order, and 200% zoom remain outside this pass.

follow-up result: passed for destructive-modal containment, strict top-layer ownership, exact focus recovery, truthful busy/failure semantics, retry continuity, quit-request settlement, and supported narrow-window reflow within the stated evidence limits.

## 2026-07-27 first-use capture-intent follow-up

- Audited the first interaction with the two advertised capture shortcuts. The main process delivered Option+C and F2 immediately, but an unconfigured renderer mounted only the processing-mode choice; the sole clipboard and screenshot subscribers lived inside an unmounted FloatingPanel, so the new user's first request disappeared without feedback.
- FloatingPanel now remains mounted but visually hidden behind the first-use and Settings surfaces. It owns shortcut events for the full window lifetime, forces hidden text into the existing latest-only memory queue, and turns hidden screenshot requests into a pending intent instead of opening frame selection.
- The first-use screen shows a privacy-safe status without rendering captured text. It states that nothing is sent before a mode is selected; screenshot requests state that frame selection has not opened. An empty clipboard is a visible alert and creates no waiting content.
- Choosing basic translation or finishing full setup reveals the existing task-level start-or-ignore card. Neither text processing nor screenshot capture begins automatically. A failed first-use settings write keeps both the notice and hidden queue intact; retry hands the same request off once.
- Historical gate (pre-prompt-v2): a real runtime-only DeepSeek test independently passed metadata discovery and a complete `deepseek-v4-flash` structured analysis through the then-current production prompt, parser, and representative ActionBrief gate. The credential was cleared from the runtime and a repository live-key-shaped scan returned zero files.
- Verified Option+C before/after, explicit processing, F2 before and after setup, empty clipboard, basic-selection save failure and retry, complete-analysis configuration, and 390×680 with no horizontal overflow.
- Accepted evidence: `.audit/first-use-capture-intent-2026-07-27/01-before-shortcut-silently-lost.png` through `13-final-preview.png`, including `11-before-vs-after.png`, with the full numbered audit in `audit-notes.md`.
- Evidence limits: deterministic browser fixtures prove renderer ownership, visible copy, no automatic processing/frame selection, retry continuity, explicit handoff, and responsive reflow. The historical pre-prompt-v2 live DeepSeek script proved the real network/provider/parser path but not macOS Keychain entry through the packaged UI. A packaged global-shortcut smoke test, full VoiceOver pass, and 200% zoom remain outside this follow-up.

follow-up result: passed for first-use shortcut ownership, memory-only retention, no premature transfer or screenshot, visible empty-state recovery, save-failure continuity, explicit start, and narrow-window layout within the stated evidence limits.

## 2026-07-27 guided-reply capture ownership follow-up

- Audited the user task “edit a guided reply and its real-world completion status, then press Option+C or F2 before copying it.” Before the fix, the open state lived inside `ResultDisplay` while `FloatingPanel` owned capture events, so the foreground guard could miss the reply. F2 could replace the result behind the drawer, and reopening regenerated the reply instead of returning to the edited draft.
- `FloatingPanel` now owns the reply modal's open state and a synchronously updated ref. Every shortcut event reads a live foreground context with a dedicated `reply-draft` reason; `app-decision` is still evaluated first, so AppQuit remains the strict top layer.
- The reply drawer is portaled directly under the application shell. While it is open, a complete Tab trap and Escape owner remain active; a child-list observer isolates newly mounted shell surfaces, and cleanup restores every affected node's exact previous `inert` and `aria-hidden` values instead of assuming a default state.
- Screenshot and clipboard requests remain memory-only while the reply is open. Protected metadata survives later foreground resolution and the existing latest-only clipboard queue; accepting uses the explicit destructive labels `放弃回复草稿并截图` or `放弃回复草稿并处理`, while the safe action is `继续编辑回复`.
- Closing to inspect the waiting request and choosing the safe action reopens the same edited text, selected completion status, and exact textarea selection direction/range. Capture and processing do not start behind the modal, and the in-modal status names any waiting screenshot or copied text without exposing its content.
- Deterministic `reply-screenshot` and `reply-clipboard` fixtures now expose `demoScreenshotCaptureRequests` and `demoProcessRequests` counters. The dedicated `check:guided-reply-capture-ownership` gate covers parent ownership, live priority, queue metadata and copy, portal containment and restoration, focus/selection continuity, fixtures, and counters; it is also included in the default test chain.
- Accepted evidence: `.audit/guided-reply-capture-ownership-2026-07-27/01-before-edited-reply.png`, `02-before-f2-destroys-reply.png`, `03-before-reopened-draft-lost.png`, `04-after-f2-protected.png`, and `05-after-option-c-protected.png`.
- Evidence limits: the deterministic browser fixtures demonstrate the visible pre-fix loss and protected post-fix states plus zero premature capture/process requests. They do not replace packaged macOS global-shortcut timing, automate VoiceOver announcement order or 200% zoom, or exercise every possible stacked foreground decision through native UI automation.

follow-up result: passed for live parent ownership, AppQuit-first priority, modal containment, dynamic exact background restoration, memory-only capture queuing, explicit destructive consent, and draft/status/caret continuity within the stated evidence limits. The next smallest user-visible maturity slice is to keep destructive credential-removal decisions isolated from unrelated Settings controls with exact focus return; signed/notarized distribution remains externally blocked by the missing Developer ID identity and notarization environment.

## 2026-07-27 credential-removal dialog isolation follow-up

- Audited the user tasks “delete this saved provider credential” and “delete it while switching to basic translation.” Both confirmations previously rendered inline inside Settings, so an unrelated provider switch or other background control could still receive activation while the destructive choice was visible.
- Both paths now use the shared `CredentialRemovalDialog` with `role="alertdialog"` and `aria-modal="true"`. The rest of Settings becomes `inert` and `aria-hidden` for the dialog lifetime, preventing pointer, keyboard, and assistive-technology activation behind the decision.
- The safe action receives initial focus; Tab and Shift+Tab wrap inside the dialog, Escape cancels only that layer, and dismissal returns focus to the initiating control. Successful deletion provides explicit feedback and focuses a stable credential input rather than a removed trigger.
- While deletion is pending, dismissal and submit actions are locked and repeated activation cannot start a second transaction. A failed persistence attempt leaves the credential saved, keeps the same confirmation available for retry, and moves focus to its in-dialog error instead of exposing the background.
- AppQuit remains the strict top layer. With both dialogs mounted, dismissing AppQuit closes only that top decision and leaves the credential-removal dialog and its background isolation intact.
- Deterministic demo coverage now accepts `credentialDelete=once`, `slow`, and `fail`. The dedicated `check:credential-removal-dialog` verifies the shared semantics, focus loop and return, pending lock, failure preservation, and success destination; exit, modal-ownership, and capture guards were expanded for cross-layer priority.
- Accepted browser evidence: `.audit/credential-delete-isolation-2026-07-27/01-before-inline-confirmation.png`, `02-before-background-provider-switch.png`, `03-after-isolated-dialog.png`, `04-after-delete-success.png`, `05-after-delete-failure-retry.png`, and `06-after-translation-delete-dialog.png`.
- Evidence limits: the direct browser pass covers the two removal paths, success, failure/retry, semantics, and focus behavior at 1280×720. This follow-up did not include direct 390px narrow-window browser acceptance and does not replace native Electron verification.

follow-up result: passed for shared destructive-dialog ownership, background isolation, contained keyboard navigation, exact cancellation return, pending deduplication, failure-safe credential preservation, stable success focus, and AppQuit-first stacking within the stated evidence limits. The next maturity item is to bring the remaining Settings draft and connection confirmations under the same precise modal ownership/restoration contract, then add narrow-window and native Electron verification. Signed/notarized distribution remains externally blocked by the missing Apple Developer ID identity and notarization credentials.

## 2026-07-27 native Electron UI fixture isolation gate

- The existing renderer demos were useful in a browser but were not a safe native acceptance surface: opening a demo URL through the ordinary Electron startup path could still expose the production preload, real application profile, IPC handlers, tray, global shortcuts, clipboard monitoring, and production cleanup behavior.
- Native fixture startup now requires one complete development-only handshake: `--dev`, `--ui-fixture`, an exact canonical `http://127.0.0.1:<port>/` renderer URL with one allowlisted demo query, and a newly created empty mode-0700 direct child of the real temporary directory. Packaged, partial, legacy, non-loopback, duplicate-query, symlinked, wrong-owner, non-empty, and secret-bearing attempts fail closed.
- The fixture sets isolated `userData` and `sessionData` paths before loading the store, sanitizes inherited credentials and process-injection variables, uses a dedicated demo preload, and skips production IPC, tray, shortcuts, clipboard monitoring, DevTools, and shared-profile cleanup. Renderer navigation, popups, permissions, and network access outside the exact loopback fixture origin are denied.
- A hidden real Electron 43 smoke test verifies the setup fixture title and native marker, context isolation, sandboxing, absent Node globals, rejected Settings IPC, stubbed clipboard writes, allowed same-origin fixture access, no inherited fake secrets, no native tray or shortcut registration, isolated paths, temporary-directory cleanup, and unchanged metadata for the normal macOS Slipstream profile. Network isolation is proved through Electron's isolated session network stack, which triggers `webRequest` without being pre-empted by the page's CSP, against a second live loopback server on another port: the session request rejects and the server's request count remains exactly zero, so DNS, CORS, or CSP failure cannot create a false pass.
- Automated evidence: `check:electron-ui-fixture`, `check:electron-ui-fixture-runtime`, the complete `npm test` chain, lint, production dependency audit, and the unsigned dual-architecture release gate all pass. The packaged release config also proves the development launcher and preload are not production application entry points, while packaged fixture intent is rejected by the main-process guard.
- Evidence limits: this is a safety and testability gate, not a visual Product Design acceptance pass. It intentionally launches the runtime check hidden, captures no before/after screenshots, does not claim 390px reflow or VoiceOver coverage, and cannot replace signed/notarized packaged interaction testing. Browser permission is still required before visually accepting the remaining Settings draft and connection modal work.

follow-up result: passed for isolated native fixture startup, fail-closed configuration, real-process renderer/main boundary proof, normal-profile non-interference, external-network denial, secret sanitization, temporary cleanup, and unsigned release compatibility within the stated evidence limits. The next user-visible slice remains the remaining Settings confirmation ownership and exact restoration contract, followed by narrow-window and visible native Electron acceptance.

## 2026-07-27 Settings transition ownership and hidden native interaction gate

- Unsaved connection input and an active provider test now use the shared `SettingsTransitionDialog` instead of maintaining separate modal implementations. A requested return or provider/path change follows one explicit ownership chain, `draft → connection → perform`, so resolving a draft cannot skip a still-running validation and no transition executes before both risks settle.
- The shared layer snapshots and restores every Settings sibling's exact prior `inert` and `aria-hidden` values. A window capture-phase listener owns Escape and both Tab directions, `focusin` contains programmatic focus, the backdrop blocks pointer activation, and all three mechanisms yield while the real AppQuit layer is on top. AppQuit therefore dismisses first without closing or stealing restoration from the Settings decision beneath it.
- Initial, busy, error, and completed states move focus to a useful safe action, dialog root, alert, or retained-result notice. Confirmation sets a synchronous lock before React can render, so a second click or Escape in the same frame cannot duplicate or dismiss committed work. Idle cancellation waits two animation frames for the Settings DOM to settle, then prefers the exact initiating control, its live replacement, or a named Settings fallback.
- Connection changes are transaction-owned as well as visually contained. API Key, endpoint, model, and full-data-reset controls stay locked while validation or cancellation remains active. Each renderer validation receives a run token and connection revision, so an old completion cannot overwrite a newer test or edited configuration.
- Explicit discard now resets the parent draft owner and all local API Key, endpoint, and model editors to their saved values before continuing. A separate native run injects the first provider-change save failure, actually confirms discard, and proves that the input is cleared, dirty state agrees, validation uses the still-selected saved DeepSeek configuration, and the same pending transition remains retryable.
- A stop error is no longer treated as proof that the provider task ended. If that same owned run later completes, the dialog replaces the stale stop warning with `completed`, focuses the completed notice, unlocks fields only after settlement, and `查看验证结果` suppresses obsolete trigger restoration before focusing the actual retained connection result.
- The dialog uses bounded internal scrolling in short Settings windows. At 620px or below, its layout and every action slot become a full-width single column without horizontal clipping.
- `check:settings-transition-dialog` enforces the ownership order, exact isolation/restoration, capture-phase keyboard handling, `focusin` containment, AppQuit priority, synchronous submit lock, focus timing, run-token and control-lock contracts, late completion, responsive CSS, and shared component adoption. It is now part of the default `npm test` chain.
- Hidden native evidence comes from real Electron 43 at 520×680. The fixtures completed the draft dialog, real AppQuit LIFO dismissal, forward and reverse Tab wrap, pointer/backdrop and inert containment, real discard plus injected persistence failure, busy Escape consumption, stop failure followed by late completion, API Key/endpoint/model and reset locking, and final result focus. The same processes continued to prove per-run temporary isolated `userData`/`sessionData`, the demo-only preload, rejected Settings IPC, stubbed clipboard, blocked second-origin network access, sanitized inherited secrets, sandbox/context isolation, and owned-directory cleanup without reading or snapshotting the live normal profile.
- Verification completed with `npm test`, `npm run lint`, production audit at 0 vulnerabilities, and the final unsigned x64/arm64 DMG/ZIP release gate including checksums and artifact inspection passing.
- Evidence limits: the Electron scenario intentionally ran hidden. This follow-up captured no visible desktop or narrow-window screenshots, used no visible browser pass, and does not claim manual visual, VoiceOver, or screenshot acceptance. Visible desktop and narrow-window review remains pending user authorization. Signed/notarized distribution also remains blocked by the missing Developer ID Application identity and notarization credentials.

follow-up result: passed for shared Settings transition ownership, ordered draft/validation settlement, exact modal restoration, AppQuit-first stacking, duplicate-submit prevention, truthful late completion, responsive interaction contracts, and hidden native Electron interaction within the stated limits. Visible screenshot acceptance remains open.

## 2026-07-27 saved knowledge, reply continuity, and native first-action trust follow-up

- Audited the user task “save a pending explanation, reuse it after restart, then export/import it.” The prior save path discarded term classification and provenance, so a visibly pending explanation became an unqualified fact in the library, clipboard, and backup. SavedTermV2 now preserves only strict type/provenance enums alongside bounded text; legacy and invalid values fail closed to `other / unknown`. Cards keep the trust label visible, evidence is named “the source excerpt at save time,” and every copy scope carries type/trust plus a pending/unknown warning without copying the excerpt. V2 backups keep the enums but exclude evidence, IDs, timestamps, settings, and credentials; v1 remains importable as explicitly unknown.
- Audited “edit a guided reply, clear the result, Undo, or recover after renderer interruption.” Reply text, real-world status, exception confirmation, and selection previously lived only in the result child and disappeared on unmount. A parent-owned, model-identity-bound state now restores the exact work through ten-second Undo. The 30-minute session record may restore bounded text, status, and selection but clears the stale-checklist exception and never restores copy animation, clipboard authority, or full reply-model content. A closed unsent reply is included in AppQuit risk.
- Audited the advertised first screenshot action on ordinary Apple keyboard settings. A fresh bare F2 registration could report success even though the top-row key changed brightness unless Fn/Globe was held. Fresh installs now use Option+Shift+S from one shared default. Existing function-key choices are not rewritten; their registered state remains truthful while the UI explains the physical-key limitation and offers the recommended combination through the same rollback-safe settings transaction.
- Replaced Electron's generated development-oriented application menu with explicit app/edit/window roles. Reload, Force Reload, and Toggle Developer Tools are absent, preventing a native menu command from bypassing task or Settings guards while retaining ordinary macOS editing and window commands.
- Automated evidence: saved-term storage migration, copy, trust, v1/v2 transfer, reply clear/session recovery, quit safety, shortcut readiness and registration, support diagnostics, source/runtime native-menu proof, renderer evidence, hidden Electron isolation, and hidden 520×680 Settings interaction gates.
- Evidence limits: this pass intentionally used source checks and hidden native Electron automation. It captured no new visible screenshots, did not open a user browser or desktop UI, and does not claim visual, VoiceOver, or packaged signed acceptance. The existing visible Settings screenshot review still requires explicit authorization; signing/notarization still requires Apple credentials.

follow-up result: passed for the automated trust and recovery contracts above within the stated evidence limits. Copied-reply version semantics across task exit/Undo and delayed clipboard settlement remain the next source-level continuity gap; visible screenshot acceptance and signed distribution remain externally gated.

## 2026-07-28 copied-reply settlement and guarded-clear follow-up

- Audited the user task “copy a prepared reply, edit or close the task before a delayed system write settles, then clear or Undo.” The earlier copy feedback had no request/task/version owner, so a late success could imply that the edited draft was copied, disappear with the closed result, or return after clear without a truthful relationship to Undo.
- Reply copy now becomes pending before the write starts. The renderer binds settlement to one request, task generation, model identity, and non-plaintext content identity. An unchanged current draft settles as copied; an edit or status switch settles as outdated; and completion after task exit settles as retained. A wrong or late request cannot claim a newer draft or different task.
- Clear marks the in-flight request as exited without cancelling the system operation. Undo restores task state but never writes again: an exact restored version may reconcile to copied after success, while an edited restoration remains outdated. Late failure remains visible without destroying any earlier live matching-only clear authority.
- Pending reply settlement is part of destructive-exit risk and blocks application quit and full-data reset. It also disables competing result, action-list, and official-source-link clipboard writes, preventing ambiguous out-of-order ownership. Dismissing the notice hides only presentation; settlement and guarded-clear authority stay live until resolved, explicitly cleared, or removed at TTL expiry.
- Copy request IDs, receipts, task generations, and content/version identities remain renderer-memory-only. Matching cleanup uses a short-lived token mirrored in the renderer notice and a sender-bound token/fingerprint record in main-process memory; neither layer keeps copied plaintext for cleanup. The bounded same-window recovery record excludes all of this authority and restores a guided reply as not copied, so an interrupted window cannot revive a copied claim or cleanup authority.
- The production preload now includes only the allowlisted `clipboard:clear-if-matches` bridge for cleanup. A VM contract executes the real preload with a fake Electron bridge, proves matching-only clear reaches `ipcRenderer`, and proves an unknown unconditional clear request is rejected.
- Hidden real Electron evidence uses the delayed-write reply fixture. It chooses an in-progress reply, fills the required name, observes immediate pending and disabled state, edits, then closes the drawer and confirms result, action-list, and real GOV.UK source-link copy controls all remain disabled; native click attempts leave the write count at exactly one. The flow then clears the task while pending, receives late retained settlement, performs clear Undo, reconciles the edited reply as outdated, and completes guarded cleanup. Clipboard behavior is an in-memory stub, and the same fixture retains its isolation from external network, normal-profile `userData`, secrets, production IPC, shortcuts, tray, and clipboard monitoring.
- Evidence limits: this follow-up uses source contracts and hidden Electron automation. It touches no real system clipboard, captures no visible desktop or narrow-window screenshots, and does not claim visual, VoiceOver, signed, notarized, or packaged interaction acceptance. Visible review still requires explicit authorization, and public signing remains blocked by the missing Developer ID Application identity and notarization credentials.

follow-up result: passed for request/task/version-bound settlement, truthful edit/exit/Undo outcomes, pending-operation risk containment, dismissal and TTL authority semantics, recovery exclusion, production preload reachability, and isolated delayed native automation within the stated evidence limits. Visible screenshot acceptance and signed distribution remain externally gated.

## 2026-07-28 app-level clipboard ownership follow-up

- Audited every user action that explicitly copies from Slipstream. Result, action-list, guided-reply, and official-source copies already used guarded cleanup, but saved-term scopes, support diagnostics, and recovery commands wrote outside that lifecycle. Two quick actions from different surfaces could therefore race, replace the wrong cleanup authority, or leave quit/reset protection describing only part of the application.
- One App-owned coordinator now acquires a synchronous write-or-clear lock before native IPC and settles the matching request before releasing it. Every app-provided copy route uses that boundary. A successful replacement atomically becomes current; a failed replacement restores only a still-live prior authority; an expired authority or stale settlement cannot return.
- The authoritative notice and matching-clear action follow the user between task, Settings, first use, and the saved-term library. Leaving a task relabels only task-scoped copies, while independent saved-term or Settings authority remains valid. Pending operations block competing copy controls, application quit, and full reset.
- A pending replacement keeps any prior cleanup authority inside its non-plaintext transaction snapshot and counts it in quit risk. A quit request snapshots live authority before the first presentation frame and rechecks synchronous operation/notice refs before automatic confirmation. The risk remains pinned when the token later expires or a clear returns expired, so a handoff race cannot become permission to leave clipboard content behind. A clear already submitted to the main process also pauses renderer TTL expiry until its confirmed `cleared`, `changed`, or failure response settles.
- Clipboard errors use assertive announcements. The reply modal now carries whichever global authority is current—even when a failed reply replacement restores a result or saved-term notice—while the inert background copy is suppressed. Saved-term cards keep their detailed visible boundary text without publishing the same copy outcome through a third live region or reviving an old announcement after reopen.
- Clipboard version language now follows changes to the copied payload. Toggling a completion marker, completing official verification, or replacing a result through reanalysis marks a matching result/checklist notice as an older version without discarding its matching-clear token. Repeating the same copy also cancels the prior button-reset timer, so an earlier success cannot make a newer in-flight copy appear idle.
- The renderer notice contains no copied plaintext. It retains only bounded status and token metadata, while the main process retains the sender-bound expiring token and SHA-256 fingerprint needed for matching-only cleanup. Newer clipboard content remains protected.
- The hidden Electron delayed-write fixture now attempts result, action-list, source-link, all three saved-term scopes, support-diagnostic, and two recovery-command copies while a reply write is pending. Every competing control is disabled, native attempts leave the write count at exactly one, retained authority is actionable in both Settings and the saved-term library, and final cleanup remains exactly one matching clear.
- Historical gate (pre-prompt-v2): automated evidence included the clipboard privacy, task-exit, quit-safety, reply lifecycle, official-source, static native-fixture, and hidden native Electron runtime checks, plus lint and renderer build. That earlier full release gate passed package configuration, production runtime audit at zero vulnerabilities, the complete test chain, lint, x64/arm64 DMG and ZIP builds, artifact inspection, and canonical checksums. A credential-in-memory live DeepSeek run separately passed the then-current provider metadata and representative structured full-analysis gate with `deepseek-v4-flash`; no credential was added to source, fixtures, project test logs, or generated artifacts. These results are historical and do not claim that the full release gate has been rerun after the latest startup and prompt-v2 changes.
- Evidence limits: this follow-up uses source/unit/VM contracts and hidden native automation only. It touches no real system clipboard, opens no visible browser or desktop UI, captures no screenshots, and does not claim VoiceOver, packaged interaction, signing, or notarization acceptance.

follow-up result: passed for one global clipboard transaction, cross-surface notice continuity, replacement failure recovery, quit/reset containment, and isolated native cross-entry proof within the stated evidence limits. Visible screenshot acceptance and signed distribution remain externally gated.

## 2026-07-28 Option+C source-integrity follow-up

- Audited the first empty-surface Option+C handoff from the user's perspective. The captured source was rendered immediately but submitted 400 ms later from a closed-over value, so an edit—or an explicit Generate for the edited draft—could still be followed by a stale second request for the original clipboard text.
- The delayed handoff now owns a one-shot token plus a monotonic source revision. Dispatch revalidates strict token/revision equality, visible task ownership, foreground decisions, and active-processing state. Editing, edit-then-undo, manual submission, replacement, clear, paste, screenshot, restore, Settings, saved terms, hiding, and competing capture paths revoke the owner synchronously.
- Editing preserves the latest text as manual provenance and announces that it was not automatically sent, with Generate and Command+Enter as the explicit next actions. Ordinary manual typing does not receive that warning. The timer consumes its owner before invoking processing, preventing re-entrancy or a later duplicate request.
- A hidden real Electron fixture delivers one Option+C capture, edits within the 400 ms transition, waits beyond the original deadline with the process counter still at zero, confirms the visible polite pause notice and preserved draft, then submits once. After the result, `修正原文` restores the edited text rather than the captured original and the request counter remains exactly one. The fixture uses isolated temporary user data, an in-memory clipboard stub, sanitized inherited secrets, and a blocked external-network trap.
- Automated evidence includes malformed-token fail-closed matrices, edit-then-undo revision coverage, source-order contracts, capture-integrity and foreground/Settings ownership gates, both hidden Electron suites, full `npm test`, lint, and renderer production build. The first workspace lint attempt also exposed an unrelated top-level `return` in the concurrent single-instance guard; removing it restored the intended prior quit behavior and full lint compliance.
- Evidence limits: the native scenario intentionally ran hidden. It did not touch the real clipboard or provider network and produced no visible desktop/narrow-window screenshots, so no visual, VoiceOver, signed, notarized, or packaged-interaction acceptance is claimed.

follow-up result: passed for edit-time revocation, latest-draft preservation, explicit exactly-once submission, stale-source rejection, strict foreground ownership, and isolated native runtime proof within the stated evidence limits. Visible screenshot acceptance and signed distribution remain externally gated.

## 2026-07-28 startup storage and prompt-v2 readiness follow-up

- Persistent settings startup now validates and migrates a private mode-`0600` staged copy before the active store is replaced or normal persistent runtime starts. Malformed JSON, schema rejection, migration failure, and unavailable storage preserve the original, block first-use/default writes, expose only sanitized reasons, and offer safe retry.
- The alternative recovery path requires explicit confirmation. It archives the complete old settings file locally with mode `0600`, starts with new settings, performs no automatic archive re-import, and restores the original if fresh-store creation fails. Full data reset removes recognized recovery archives.
- Deterministic storage evidence uses the lockfile-pinned real `electron-store@8.2.0` with temporary filesystem profiles. It covers malformed JSON, schema and migration failure, original-byte preservation, private permissions, retry, recovery rollback, fresh-store startup, and reset cleanup. Electron and `safeStorage` are stubbed, so this is storage/startup-logic evidence rather than native Keychain, packaged-app, or visible-UI proof.
- Full-analysis readiness now sends the fixed fictional compatibility message with built-in `action-brief.prompt.v2` through the production `processText` call, parser, and `ActionBriefV1` validator, then requires extra probe-specific evidence for grounded translation, deadline, material, actions, terms, and process context. Merely valid JSON does not pass. The candidate contract and schema remain v1, and prior prompt v1 remains accepted.
- The current strengthened live DeepSeek gate passed with model `deepseek-v4-flash`, provider metadata `ok`, `structuredBrief: true`, 5,738 output characters, and 42,276 ms elapsed. The credential remained process-only, and the diagnostic prints neither the key nor raw model response. Earlier live PASS entries above are explicitly historical pre-prompt-v2 gates.
- Deterministic provider-connection and action-brief checks pass. The exact final worktree, including the root documentation, was copied into a local `/tmp` directory and its lockfile-pinned dependencies were freshly installed. That isolated copy passes the complete `npm test`, lint, renderer build, runtime dependency audit at 0 vulnerabilities, and `release:unsigned`, including x64/arm64 DMG and ZIP packaging, checksums, the complete `check:release`, history non-retention, Info.plist inspection, and artifact validation. Generated artifacts did not overwrite the workspace's existing `release` files; the temporary verification copy was removed after the checks.
- Evidence limits: `check:deepseek-live` is outside `npm test` and covers one fixed fictional probe through the production main-process provider/parser/validator chain plus probe assertions. It does not cover renderer UI, IPC, `safeStorage`, packaged-app interaction, arbitrary-input quality, provider uptime, billing, or retention policy. This follow-up also adds no visible desktop/narrow-window screenshots, VoiceOver acceptance, signing, or notarization evidence.

follow-up result: passed for the storage-startup logic, prompt-v2 readiness contract, deterministic provider/action gates, the current fixed-probe live DeepSeek gate, and the final automated unsigned-release gate within the stated limits.

## 2026-07-28 request-time clipboard-clear quit follow-up

- Audited the user task “start safe clipboard removal, then quit before macOS confirms the outcome.” The existing quit handshake correctly failed closed, but it compressed both write and clear operations into “confirming clipboard copy” and permanently retained a boolean clipboard pin after the in-flight clear returned `cleared` or `changed`. The clear action then disappeared while the dialog still warned that the old content might remain.
- Quit ownership is now request-scoped and metadata-only. It records the current request, clear operation ID, and clear token; only an exact `cleared` or `changed` settlement releases the old clipboard pin. Expired, failed, unknown, malformed, or mismatched settlement remains fail closed. A successful settlement never sends a quit decision directly: all live draft, result, task, settings, operation, authority, and current-request refs are rechecked first.
- The pending decision now says `正在确认剪贴板清除`, with wait/cancel language distinct from clipboard writes. After safe settlement, only the clipboard warning and unusable clear-and-exit action disappear. If another result or draft risk remains, that decision stays visible. `changed` preserves newer external clipboard content; unconfirmed outcomes keep the application open and describe retry only when a live authority still exists.
- A newer macOS quit request can arrive while the previous renderer decision Promise is returning. The new request now supersedes the old decision identity before publication; old responses, catch paths, finalizers, and double-animation-frame presentation work all fail their identity checks and cannot freeze or overwrite the new flow.
- Deterministic gates cover strict pin creation and settlement, unsafe IDs, oversized or mismatched tokens, stale outcomes, separate pending-clear copy, live-ref rechecks, direct clear-and-quit revalidation, and quit-request supersession. A hidden real Electron fixture performs result copy → slow guarded clear → quit while settlement count is zero. It confirms the clear-specific pending title, then proves the old clipboard risk disappears while the result-loss decision and decision count remain intact; explicit confirmation sends and confirms exactly one quit decision. The fixture uses fresh temporary user data, in-memory clipboard stubs, sanitized secrets, and a zero-request external-network trap.
- The exact final-worktree `/tmp` gate passes the complete `npm test`, full lint, production renderer build, runtime dependency audit at 0 vulnerabilities, and `release:unsigned`, including dual-architecture DMG/ZIP packaging, checksums, the complete `check:release`, history non-retention, Info.plist inspection, and release-artifact validation. The workspace's existing `release` artifacts were not replaced, and the temporary validation copy was removed after the checks. The only renderer build notice is the existing non-fatal large-chunk advisory.
- The first inspection of the existing artifacts encountered `hdiutil` detach status 16 even though the mounted volume had already disappeared. `scripts/check-release-artifacts.js` now gives detach at most four attempts separated by 250 ms and accepts an absent mount point as complete; it still fails if the mount survives the bounded retry. The repaired existing-artifact inspection and the final `release:unsigned` gate both pass.
- Evidence limits: this follow-up uses source contracts and hidden Electron automation. It does not touch the real system clipboard, open a visible desktop/browser surface, capture screenshots, automate VoiceOver, or claim signed, notarized, or packaged interaction acceptance.

follow-up result: passed for truthful pending-clear copy, exact safe settlement, fail-closed stale/error handling, remaining-risk preservation, newer-request supersession, isolated native interaction, and the final automated unsigned-release gate within the stated evidence limits.

## 2026-07-28 trust-boundary and committed-choice follow-up

- Reviewed three remaining user-state failures from source and deterministic state contracts because visible desktop/browser capture was not authorized for this pass. First, changing a custom endpoint origin previously persisted the new URL before clearing the old key; a failure between writes, followed by retry, could leave the old credential available to the new host. The store now decides the origin boundary and uses one `electron-store.set({ ... })` commit for the new URL plus empty key. Same-origin path edits keep the key, a failed atomic write keeps the complete old boundary, and the renderer clears its redacted saved-key flag only from the matching successful response.
- A partial failure while enabling full analysis previously left a generic retry transaction containing `setupMode=full`. If the user then changed a key, endpoint, model, provider, prompt, or verification policy, that old retry could replay the completed mode without validating the new configuration. Failed activation state now carries its processing-configuration generation. A generation change synchronously removes the stale full/setup intent and its bound entries from both retry and error ownership; ordinary unrelated failed settings remain retryable, and retry performs the same generation check again before writing.
- After the user chose `停止后截图`, the stop request was already irreversible in main, but the renderer still offered `继续当前任务`; ignoring the screenshot could not withdraw the cancellation. The continue action now exists only before stop dispatch. Stopping explicitly says the request cannot be withdrawn, exposes no ignore action, and distinguishes in-flight stop, unconfirmed stop with retry, and task-ended capture preparation.
- Deterministic evidence covers one object write, same-origin key retention, injected atomic failure with unchanged boundary, response correlation and malformed-response rejection, absence of a second renderer clear request, unchanged/stale/mixed failed-activation generations, and all waiting/stopping screenshot copy and action states. The first full-tree pass caught one obsolete static assertion for the prior retry representation; after the contract was updated, a freshly installed isolated copy passed complete `npm test`, full lint, production build, runtime audit at 0 vulnerabilities, and `release:unsigned`, including x64/arm64 DMG and ZIP creation, checksums, history non-retention, Info.plist checks, and artifact inspection. The verified renderer and canonical release artifacts were synchronized to the workspace, where all four checksums pass.
- Evidence limits: this follow-up adds no visible desktop or narrow-window screenshots, does not operate the real macOS Keychain or system clipboard, and does not claim VoiceOver, Developer ID signing, notarization, or visible packaged-app interaction acceptance. The renderer build retains the existing non-fatal large-chunk advisory; cold-start measurement remains the prerequisite for deciding whether code splitting is warranted.

follow-up result: passed for atomic custom-provider credential boundaries, stale activation revocation, and truthful irreversible screenshot-stop ownership within the stated evidence limits.

## 2026-07-28 abandoned-input and evidence-quality follow-up

- Traced four user transitions that could otherwise make visible input or trust outlive its real owner. Explicitly discarding a failed API-key, endpoint, or model draft now removes the matching failed settings write and any paired full-analysis pause, so a later generic retry cannot resurrect an abandoned credential or destination.
- A custom endpoint origin change still revokes the old origin's stored key atomically. If a replacement key is already visible, that authoritative saved-state change no longer clears the local dirty owner: return, provider switching, and shortcut capture remain behind the draft guard until the user saves or explicitly discards the replacement.
- Evidence-free term backups retain their exported label for preview transparency but import `original`, `inference`, and `official` as `unknown`. The preview counts accepted downgrades, merge sanitizes parser bypasses, and a lower-trust backup cannot overwrite a stronger local record.
- When source B fails over visible result A, B remains in a separate renderer-memory slot. Reviewing B, changing it to B′, returning to A, and retrying now submits B′ rather than A or the original B. Corrected text is labeled manual, stale OCR/truncation metadata is removed, and an empty correction reopens review instead of silently falling back. Interruption recovery continues to persist only clean A.
- Hidden real Electron 43 evidence at the native fixture boundary covers A → failed B → edited B′ → return A → retry B′. It proves exactly two processing requests, manual B′ metadata, no review-time request, result promotion only after success, an isolated temporary profile, an in-memory clipboard stub, sanitized inherited secrets, and zero external-network requests.
- A security audit rejected the attempted launch timer because its distributed activation surface still shared production lifecycle capabilities and did not prove compositor-visible readiness. The formal application now contains no activation flag, environment handshake, runtime/launcher file, npm measurement command, or packaged resource for that path. Old timing claims are withdrawn, and a negative default-test gate prevents the integration from returning before a separately identified capability-minimized harness earns real subprocess evidence.
- The exact final tree was copied to a fresh temporary directory and installed from the lockfile. It passes complete `npm test`, full lint, renderer build, package configuration, production dependency audit at 0 vulnerabilities, and `release:unsigned`, including dual-architecture ZIP/DMG builds, checksums, repeated full tests, history non-retention, Info.plist, and release-artifact inspection. Extracted x64 and arm64 `app.asar` payloads are identical and contain no API-key-shaped value or retired startup activation token.
- Evidence limits: all native interaction remained hidden. This pass did not operate the real clipboard, open a visible browser or desktop surface, capture screenshots, automate VoiceOver, or prove Developer ID signing, notarization, Gatekeeper installation, or user-visible startup performance. The previously shared credential was not reused for a broad live corpus and was not written to source or artifacts.

follow-up result: passed for abandoned-write revocation, replacement-key draft ownership, evidence-free backup downgrading, corrected B′ retry continuity, retired startup activation, isolated native interaction, and the complete automated unsigned-release gate within the stated limits.

## 2026-07-28 visible Settings transition and minimum-window follow-up

- Audited the user task “leave Settings with an unsaved connection draft or a provider validation still running, recover from an unconfirmed stop, then inspect a late result.” A real visible Electron development fixture ran at the production capture geometry of 520×680, with fresh isolated user data, a loopback-only renderer, blocked external networking, and a fake non-secret input. The same native window was resized to the supported 400×400 minimum.
- Pre-fix screenshots showed complete controls but compressed 11px body text, 10px notices/actions, 36px targets, a 1.86:1 white-on-warning action in dark mode, and translucent state focus rings. The completed copy also failed to say the original return action had been discarded.
- Transition title/body/notice/action copy is now 15/13/12/12px, every action is at least 44px high, and the warning action reaches 4.82:1 in light and 6.38:1 in dark. Opaque semantic warning/error/success focus rings replace translucent halos. The 400×400 completed state remains fully visible without horizontal clipping or scrolling.
- Programmatically focused descriptions, notices, and errors now route Tab to the first action and Shift+Tab to the last. Direct native keyboard evidence confirms reverse focus on `重试停止并返回主面板` with the error ring. Errors are included in `aria-describedby`, the inapplicable `aria-errormessage` is removed, and spinner animation stops under Reduced Motion.
- Copy now matches behavior: a failed save offers `返回草稿` without promising an automatic retry; a late completion says the original action must be repeated; guided setup names `返回首次使用选择`; and cancellation says it waits for an actual stop confirmation.
- Accepted evidence: `docs/ux-evidence/2026-07-28-settings-transition-compact/01-draft-dialog-before.jpeg` through `11-error-shift-tab-after.jpeg`, with the evidence README in the same folder. Complete `npm test`, full lint, and the production renderer build pass in the isolated current tree.
- Evidence limits: this is real visible native Electron development-fixture evidence, not a signed/notarized installed build. It does not automate VoiceOver speech, 200% text scaling, Increase Contrast, or operating-system Reduced Motion presentation, and it uses deterministic fake provider states rather than a live network request.

follow-up result: passed for visible 520×680 transition acceptance, supported 400×400 containment, readable compact hierarchy, warning/focus contrast, directional status focus, truthful state copy, and the complete regression/build gates within the stated limits.

## 2026-07-28 committed user-intent and bounded-recovery follow-up

- Audited three ordinary user journeys plus one copied-state transition from source and deterministic contracts. After clearing a task, “读取剪贴板” previously destroyed the visible 10-second undo before native read success; an empty clipboard or read error therefore returned no new source and permanently removed the old one. Clipboard reads now own a monotonic attempt token. Empty, failed, and stale responses keep the undo; only the current response with usable replacement text commits its loss.
- The first-use basic-translation save and retry previously disabled only part of the choice surface. A same-frame click on “配置完整分析” could open Settings while the older asynchronous transaction later persisted `free_translate` and `translation-only`. All three actions now share a synchronous compare-and-set owner, every CTA is disabled while one owns the transition, and choosing full setup after a failed basic save removes every matching old retry key before Settings becomes interactive.
- The temporary recovery heartbeat previously rewrote `savedAt` every five minutes and on pagehide, so an idle open window could retain sensitive source/result data indefinitely despite promising at most 30 minutes. Heartbeat and pagehide now flush the existing record without refreshing its timestamp; only a real state snapshot gets a new lifetime.
- A copied “completed” reply was versioned only by its text. Unchecking a required action could therefore make the real-world completion claim false while the clipboard notice still said it was current. Required-action changes now revoke the old checklist exception, and reply settlement/reconciliation includes the latest completion consistency. A regressed claim becomes outdated even when its English text is unchanged; restoring consistent progress can truthfully reconcile the exact same version again.
- Deterministic gates cover empty/failed/stale clipboard reads, clear-undo commit ordering, setup choice locking and old-retry revocation, non-renewing recovery flush, reply settlement before/after progress regression, and restored consistency. A fresh isolated application-source snapshot with newly installed lockfile dependencies passes complete `npm test`, full lint, renderer build, production dependency audit at 0 vulnerabilities, and `release:unsigned`, including the repeated full test/lint chain, hidden real-Electron fixtures, dual-architecture ZIP/DMG packaging, checksums, history non-retention, Info.plist, and artifact inspection. Extracted x64/arm64 `app.asar` hashes match, and extracted application text contains no API-key-shaped value or retired startup activation token.
- Evidence limits: this follow-up used source/unit contracts and the existing hidden isolated Electron suite. It did not touch the real system clipboard, operate a visible desktop/browser surface, capture new screenshots, automate VoiceOver or text scaling, run the broad network-backed DeepSeek corpus, or prove Developer ID signing, notarization, Gatekeeper installation, or visible packaged-app interaction.

follow-up result: passed for clipboard-read recovery ownership, mutually exclusive first-use choice commitment, truthful temporary-data expiry, copied completion-claim consistency, and the complete automated unsigned-release gate within the stated evidence limits. Failed-radio focus rollback, additional assistive-technology acceptance, and signed distribution remain open.

## 2026-07-28 Settings transition 200% text-scaling follow-up

- Audited the bounded Settings journey “leave with an unsaved connection draft or running validation, recover from an unconfirmed stop, then inspect a late result” in a real visible Electron development fixture at an exact 400×400 native window and Chromium zoom factor 2. The renderer had a 200×200 CSS viewport, fresh isolated user data, a loopback-only origin, blocked external networking, and fictional non-secret input.
- Before repair, the dialog correctly owned focus but called every internal focus move with `preventScroll`. At this scale the initial safe draft/running action was fully offscreen; stop-error recovery was only partially reachable; and completion inherited an obsolete scroll position that hid the start of its retained-result notice.
- All dialog-internal moves now use one focus-and-reveal path: focus without an automatic jump, confirm the target actually owns focus, then scroll it to the nearest visible position. Dialog-root and external-trigger restoration remain scroll-neutral so ordinary dismissal does not disturb the surrounding Settings page.
- Programmatic modal focus now receives the same opaque semantic 2px ring, 2px offset, and raised-surface separation as keyboard-visible focus. Six-pixel scroll padding keeps the complete ring inside the dialog instead of touching or crossing the scrollport edge.
- A dedicated native gate fixes content size and window bounds at 400×400, sets `zoomFactor=2`, and records eight draft/error/completed paths. It requires real DOM focus, the target plus the complete four-pixel ring inside the scrollport, and no horizontal dialog or page overflow.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-28-settings-transition-text-scale/00-min-window-100-reference.png` through `11-completed-200-after.png`, with the numbered scope and limits in the folder README. Same-viewport before/after comparison confirms the repaired safe action, running action, stop-error notice, retry path, and completed notice.
- The current isolated snapshot passes the complete `npm test`, full lint, and renderer production build. One obsolete capture-integrity extractor was also hardened to recognize a legitimate non-empty React dependency list instead of swallowing later handlers and falsely reporting that the safe example auto-submitted.
- Evidence limits: this accepts only the Settings transition dialog. The 200% entry capture still shows the application header, Settings control, and capture content clipping horizontally at the supported minimum window. First use, capture/result, drawers, and the full Settings shell still need 200% reflow acceptance. This is not a signed/notarized installed build and does not automate VoiceOver, Increase Contrast, or operating-system Reduced Motion presentation.

follow-up result: passed for bounded 200% Settings-transition focus visibility, complete semantic-ring geometry, forward/reverse recovery reachability, honest native automation, and current-tree regression/build gates. Whole-app 200% reflow is the next visible maturity slice.

## 2026-07-28 renderer-interruption clipboard consequence follow-up

- Audited the user journey “copy with Slipstream, lose or replace the renderer, then recover, reset, or quit.” A healthy copy previously held clear authority only in renderer memory. A clean renderer exit or full main-frame replacement could discard that authority while leaving the main-process consequence dormant, so the recovered interface could omit the warning and destructive transitions could proceed without explicit preservation.
- A successful write now creates two separate in-memory records in main: a metadata-only consequence and an expiring matching-clear authority. The consequence contains only sender ownership, an opaque ID, and internal interruption state; copied text, fingerprints, receipts, task identity, and clear tokens never cross recovery. A newer write replaces the old consequence, while exact `cleared` or `changed` settlement resolves only its linked record.
- Crash, clean exit, and every post-startup full main-frame navigation promote the current consequence before revoking the old clear token. The first application navigation is excluded. A real navigation already in progress owns recovery, and delayed crash reload, native confirmation return, and later navigation share one reload state so two sequential reloads cannot stack.
- Recovery performs no clipboard read or clear. The global assertive warning states that prior content may remain and that the old clear entry is invalid; it exposes only `我已检查或手动覆盖`. Exact current-ID acknowledgement is required. Main independently gates full reset before persistent deletion and rejects confirmed quit without the exact interrupted ID; crashed/loading native quit fails closed for any current consequence.
- The hidden isolated Electron 43 fixture seeds only opaque main-owned consequence metadata, performs a real `webContents.reload()`, and verifies one focused `role=alert` warning with no clear control. An invalid acknowledgement leaves the warning live and the exact UI acknowledgement removes it. The run records zero clipboard writes/clears, zero external-network requests, no normal-profile change, and no inherited secret exposure.
- Full validation also exposed two 200% gate defects. The setup-to-capture probe could sample a mounted-but-not-yet-laid-out header as an all-zero baseline; it now waits for non-zero live header and brand boxes. At a 200×200 CSS viewport, loaded text could expand the source field beyond the capture scrollport and make its focus outline impossible to reveal; the narrow layout now caps that field at 92px and keeps internal vertical scrolling. The hidden fixture explicitly requests Chromium focus-visible rendering and measures the production outline rather than assuming focus implies a visible ring.
- A fresh isolated application-source snapshot with newly installed lockfile dependencies passes complete `npm test`, full lint, renderer build, package configuration, production dependency audit at 0 vulnerabilities, and `release:unsigned`. The release gate repeats the full tests/lint plus history, Info.plist, checksum, DMG/ZIP, codesign, and entitlement checks. x64 and arm64 `app.asar` payloads are identical at `e61bdb920e0c9474b834e7655aeaef5740caddef7189b992861543c77ab1bc1d`; extracted application text contains no API-key-shaped value or retired startup activation token. The only renderer build notice is the existing 683.05 kB large-chunk advisory.
- Evidence limits: macOS pasteboard comparison and clearing remain separate operations, so automatic compare-then-clear is not claimed atomic; preserve/manual overwrite stays available and platform-atomic clearing or removal is backlog work. This follow-up used source contracts and hidden native automation only. It did not touch the real system clipboard, create new visible screenshots, automate VoiceOver/Increase Contrast/OS Reduced Motion, run the full live DeepSeek corpus, or prove Developer ID signing, notarization, Gatekeeper installation, or visible packaged-app interaction. The previously shared credential was not reused or persisted and should be rotated before any future broad live run.

follow-up result: passed for metadata-only consequence survival, clean-exit/main-frame lifecycle promotion, exact acknowledgement, main-authoritative quit/reset containment, single-owner recovery reload, honest 200% geometry automation, and the complete unsigned-release gate within the stated limits. Atomic clipboard clearing, broader visible accessibility acceptance, rotated-key model evaluation, and signed distribution remain open.

## 2026-07-28 visible first-use and capture-shell 200% follow-up

- Audited the new-user journey from processing-mode choice through the first privacy handoff, empty capture, fictional safe sample, and keyboard access in a real visible Electron development fixture at an exact 400×400 native content area with Chromium zoom factor 2. The resulting 200×200 CSS viewport previously exposed a wider-than-window shell: Settings and Hide were lost, the capture heading clipped, and loading the sample shifted the entire surface sideways.
- Removed the intrinsic-width veto at the application session and panel hosts, made capture content vertical-scroll-only and shrink safe, and introduced an ultra-narrow two-row header that retains Slipstream, Saved Terms, Settings, and Hide. First-use cards use more available width, capture actions become single-column where necessary, and long source text scrolls inside a 92px-high field instead of widening the outer surface.
- Restored a production 3px opaque keyboard outline with a 2px offset on the source textarea. A native keyboard-modality probe and computed-style assertions require the real outline, its complete five-pixel extent, actual focus ownership, and containment rather than treating DOM focus as sufficient evidence.
- Added the fixed `first-use-capture-text-scale-native` gate for only `demo=setup&fixture=check`. At exact 400×400 bounds and zoom factor 2 it checks stacked choices, complete processing/privacy copy, vertical-only reachability, stable non-overlapping header controls, privacy acknowledgement, empty and loaded source states, exact safe-sample text/count, Clear, explicit enabled Generate, no automatic processing, and zero horizontal overflow. The isolated run blocks external networking, production IPC, normal-profile access, inherited secrets, and real clipboard use.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-28-app-shell-text-scale/01-capture-entry-200-before.png` through `09-settings-focus-200-after.png`, plus the three same-viewport comparison sheets. A fresh isolated application-source snapshot with an exclusive temporary runtime passes complete `npm test`, full lint, and the production renderer build; only the existing non-fatal large-chunk notice remains.
- Evidence limits: this accepts first use and the capture shell, not the result workspace, Saved Terms drawer, full Settings shell, or simultaneous shortcut/monitoring/capture banners. It is not a signed/notarized installed build and does not automate VoiceOver speech, Increase Contrast, operating-system text-size settings, or Reduced Motion presentation. It makes no live provider-quality, billing, real Keychain, real clipboard, or arbitrary-input result claim; the previously shared credential was neither used nor persisted.

follow-up result: passed for visible/native first-use and capture-shell 200% reflow, complete global-action reachability, privacy and source-entry containment, explicit safe-sample submission, real keyboard focus indication, and the complete regression/build gates within the stated limits. Result, drawer, full-Settings, stacked-banner, assistive-technology, and signed-distribution acceptance remain open.

## 2026-07-28 completed-result, Saved Terms, and preserve-only clipboard follow-up

- Audited the user journey “understand a completed result, move between action and source evidence, finish or leave the task, reuse a saved term, then reset or quit without those transitions mutating the system clipboard beyond copies the user explicitly requested.” A real visible Electron development fixture ran at an exact 400×400 native content area and Chromium zoom factor 2, producing a 200×200 CSS viewport with fresh isolated user data, fictional sample content, a loopback-only renderer, blocked external networking, and in-memory clipboard stubs.
- Before repair, the completed-result header consumed excessive narrow-screen height, summary metadata and detail cards retained intrinsic width, footer actions compressed into partial rows, and the Saved Terms title/privacy/search stack pushed the focused search target below the first viewport. Multiple nested scroll owners made the bottom actions difficult to predict.
- The completed-result shell now keeps Slipstream, result order, Saved Terms, Settings, and Hide in one compact two-row hierarchy. Summary metadata wraps; action and source-evidence panes share one vertical scroll path; numbered action/evidence cards shrink against the real viewport; the processing disclosure and every footer control become vertically reachable full-width targets; and activating the deadline summary routes to its relevant detail.
- The Saved Terms drawer now owns its bounded scroll surface. Title, local-only privacy boundary, count, and the complete opaque search focus ring fit in the first viewport; copy scopes, removal, export, and import remain reachable by native keyboard traversal; newly mounted background siblings stay inert; Escape closes only the drawer and restores focus safely.
- Added the fixed `completed-result-text-scale-native` gate for only the deterministic completed-result check fixture. Its ordered native path uses 17 trusted clicks for pane/evidence round trips, four completion check→uncheck→recheck cycles, and Saved Terms opening, followed by two trusted Tabs and Escape; every event must be trusted and hit the expected target, and an out-of-sequence probe is rejected. Result-order, processing, footer, and remaining drawer controls are separately focused and measured rather than claimed as native activations. Together the gate verifies exact 400×400 bounds, zoom factor 2, complete source text, focus/ring containment, vertical-only reachability, Saved Terms isolation/dismissal, eleven zeroed side-effect counters, and zero requests to the external-network trap.
- Removed automatic clipboard clearing and every production conditional-clear IPC path. Copied results, action lists, source links, saved-term scopes, support diagnostics, recovery commands, and guided replies now retain only opaque consequence state. Quit offers preserve-and-exit; renderer interruption offers exact acknowledgement/manual overwrite guidance; Undo never writes again; and notices contain no copied plaintext.
- Full data reset now uses a cryptographically random sender-bound one-time ticket with a 30-second lifetime. Preparation validates the exact clipboard consequence and preserve choice before renderer state is purged, blocks conflicting writes and acknowledgement while live, and commit revalidates the consequence before persistent deletion. Mismatch, tampering, expiry, cross-sender use, replay, renderer loss, and state changes fail closed. A persistent-stage failure remembers that session data is already gone and retries only the remaining stage without repeating the purge or promising preservation of cleared session data.
- The native runtime acceptance harness now creates one mode-`0700`, inode-tracked temporary root per gate invocation, passes it to every child temp variable, scopes cleanup inspection to that root, and waits for a timed-out child to terminate before settlement. It no longer snapshots the live normal profile. Two complete runtime gates pass concurrently, each removing only its own fixture state.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-28-result-workspace-text-scale/01-result-entry-200-before.jpeg` through `10-saved-terms-footer-200-after.jpeg`, plus three same-viewport comparison sheets and the numbered evidence README. A fresh isolated application-source snapshot with an exclusive temporary runtime passes complete `npm test`, full lint, the production renderer build, `git diff --check`, reset/fixture syntax checks, and an exact workspace scan showing that the previously shared credential was not persisted. Only the existing non-fatal large-chunk advisory remains.
- This preserve-only conclusion supersedes the former guarded-clear conclusions recorded in earlier historical follow-ups. Preserve-only is not erasure: sensitive text can remain readable to other applications or clipboard-history tools until the user deliberately overwrites it, and Slipstream intentionally does not read back the clipboard to verify that overwrite.
- Evidence limits: this accepts the completed-result workspace and Saved Terms drawer, not the full Settings shell or simultaneous shortcut/monitoring/capture banners at 200%. It uses real Electron events emitted through the fixture but does not operate a physical mouse/keyboard, touch the real system clipboard, automate VoiceOver speech order, macOS Increase Contrast, operating-system text sizing, or Reduced Motion presentation, run arbitrary content or a live DeepSeek/provider-quality corpus, or prove Developer ID signing, notarization, Gatekeeper installation, or visible packaged-app interaction.

follow-up result: passed for visible/native completed-result and Saved-Terms 200% reflow, source-to-action trust preservation, complete footer/drawer reachability, opaque focus containment, preserve-only clipboard behavior, sender-bound fail-closed reset, and the complete regression/build gates within the stated limits. Full-Settings, stacked-banner, assistive-technology, rotated-key model evaluation, and signed-distribution acceptance remain open.

## 2026-07-30 full Settings-shell 200% and authoritative-radio-focus follow-up

- Audited the complete regular Settings journey in a real visible Electron development fixture at an exact 400×400 native content area with Chromium zoom factor 2. The 200×200 CSS viewport used isolated deterministic fixture state, a loopback-only renderer, fictional non-secret provider data, and blocked external networking.
- Before repair, the current-mode summary retained a side-by-side layout that compressed Chinese into character-wide columns; the local “隐私优先” badge overlapped its title; saved-state and credential actions collapsed into narrow vertical strips; the narrow breakpoint forced diagnostics into two clipped columns; and the advanced disclosure offered only about 15 CSS pixels of height. The page could still report zero horizontal overflow, so an overflow-only gate would have accepted a visibly broken layout.
- The narrow Settings shell now uses explicit structural classes and a concentrated ≤280px reflow. Mode metadata stacks into a readable hierarchy, the privacy badge participates in the card grid, connection status/actions stack while keeping usable widths, diagnostics use one wrapping column, advanced/secondary summaries meet a 32px product target, reset remains reachable, and the Settings scroll surface contains overscroll.
- Audited the keyboard path “use ArrowRight to request another provider, then cancel or encounter a failed save.” The destination radio previously received focus before activation, but persisted state stayed on the old choice; cancellation or rejection could therefore leave focus on an unchecked `tabIndex=-1` option. A shared authoritative-radio resolver now normalizes dialog return focus and rejected direct/backend saves only when the obsolete attempted radio still owns focus, avoiding a late rollback that would steal focus after the user moved elsewhere.
- The expanded native gate fixes the CSS viewport at exactly 200×200 and measures every major Settings section. It requires vertical reachability, zero horizontal page/scrollport movement, complete computed focus rings for Return, checked location/provider/verification radios, credential/model editors, Save, connection test, disclosures, monitoring, shortcut recorders, support actions, and reset, plus one checked/matching tab stop per radiogroup.
- Defect-specific geometry rejects false passes: the mode label/detail must be vertically stacked; privacy badge/title intersection area must be zero; editor rows must stack with readable status/button widths; support diagnostics must expose one computed column with wrapping, unclipped strong/small text; and the advanced summary must be at least 32 CSS pixels high.
- Native ArrowRight coverage first cancels a provider-change draft dialog and proves exact DeepSeek focus/check/tab-stop restoration, then repeats the change, confirms discard, injects one `save=once` failure, and proves the same authoritative state plus a complete focus-visible ring without the test forcing the final focus.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-30-settings-shell-text-scale/01-before-settings-top.png` through `09-after-radio-failure-focus.png`, three original-size same-viewport comparison sheets, and the numbered evidence README. The current tree passes complete `npm test`, full lint, the production renderer build, the static fixture gate, the full native Electron runtime gate, and `git diff --check`; only the existing non-fatal renderer large-chunk advisory remains.
- Strengths retained: privacy-first local/online framing, redacted saved credentials, preview-before-copy diagnostics, guided-setup removal of destructive reset, and exact focus-visible keyboard semantics. The supplied credential was not used, entered, persisted, or placed in fixture state, so this pass makes no live provider-quality claim.
- Evidence limits: simultaneous shortcut/monitoring/capture/recovery banners at 200% remain open. This pass does not automate VoiceOver speech order, macOS Increase Contrast, operating-system text sizing, or Reduced Motion presentation; touch the real Keychain or system clipboard; run arbitrary provider output or a live network quality corpus; or prove a signed/notarized installed build.

follow-up result: passed for complete regular Settings 200% reflow, defect-specific geometry, vertical-only reachability, computed focus containment, authoritative radio cancellation/failure recovery, and the complete current-tree regression/build gates within the stated limits. Stacked-banner, assistive-technology, rotated-key model evaluation, and signed-distribution acceptance remain open.

## 2026-07-30 simultaneous foreground-status 200% follow-up

- Audited the user journey where renderer-interruption clipboard recovery, a queued screenshot, queued copied text, two unavailable global shortcuts, and active online clipboard monitoring all compete with the core task in a real visible Electron development fixture. The window used an exact 400×400 native content area and zoom factor 2, producing a 200×200 CSS viewport with isolated fixture state, fictional content, a loopback renderer, blocked external networking, and in-memory clipboard behavior.
- Before repair, the shell clipped overflow while the shortcut, monitoring, and capture notices were non-shrinking siblings above the only scrollable task view. The shortcut warning could occupy the first viewport while higher-priority capture decisions and the core task remained unreachable. Queue focus used an undefined focus color, monitoring had no programmatic focus treatment, and `preventScroll` could move focus to clipped content.
- Added one semantic foreground status center ordered by user decision priority: pending screenshot, pending copied text, runtime problems, shortcut readiness, then monitoring. Clipboard-residue recovery sits before the header and owns the first decision; only the visible panel can own it, preventing a hidden Settings/setup panel from mounting a duplicate alert or consuming the one-time focus handoff.
- At narrow or short viewports the panel shell becomes the only page-level vertical scroll owner. Header, recovery, statuses, capture/result content, and task controls keep natural height; nested status scrolling and z-index competition are removed. The height query also covers an 800×400 native window at 2× zoom, where the CSS viewport is 400×200 rather than ≤280px wide.
- Programmatic focus now verifies ownership, scrolls the compact target into view, and verifies focus again. Recovery, queued capture titles, monitoring status, and all actions use opaque semantic rings; every status action has a 40px minimum height. Clipboard stays blocked behind the screenshot decision, and degraded runtime or monitoring-stop errors keep warning—not success—summary semantics.
- Visible acceptance confirmed the initial recovery-title ring, explicit acknowledgement, screenshot-title handoff, screenshot/clipboard actions, shortcut repair, complete monitoring explanation/action, and the transition into the core task. A native Tab issued from the lower task scrolled back to the first enabled screenshot action with its complete five-pixel ring visible.
- The fixed native run requires the exact canonical scenario and proves pre-ack recovery plus all four statuses, post-ack screenshot focus, explicit fictional-sample processing with all statuses retained, fixed DOM/keyboard priority, no card/copy/action overlap, one scroll owner, positive maximum scroll, complete passive/action/focus reachability, and zero horizontal movement. It then resizes the same window to 800×400, repeats the lightweight wide-short ownership/reachability/focus proof, and restores exact 400×400 bounds, 200×200 CSS, and zoom 2.
- The run records zero screenshot captures, clipboard writes or mutations, production application IPC, renderer reloads, inherited-secret exposure, and external-network requests. The supplied credential was not used, entered, persisted, or included in fixture state, so no live DeepSeek quality result is claimed.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-30-stacked-status-text-scale/before-stacked-status-200pct.jpeg` through `after-keyboard-priority-focus-200pct.jpeg`, the 810×400 same-viewport comparison sheet, and the numbered evidence README. The current tree passes complete `npm test`, full lint, the production renderer build, the static fixture gate, the full isolated native runtime gate, and `git diff --check`; only the existing non-fatal large-chunk advisory remains.
- Evidence limits: VoiceOver speech order and live-region verbosity, macOS Increase Contrast, operating-system text sizing, and Reduced Motion presentation remain open. Runtime-degraded and result-specific error notices have separate deterministic coverage but were not added to this five-surface visible composition. This is not a signed/notarized installed build and does not prove real shortcut registration, Keychain entry, system clipboard mutation, live provider output, billing, or arbitrary-input quality.

follow-up result: passed for simultaneous recovery/capture/shortcut/monitoring hierarchy, single-scroll reachability at 200×200 and 400×200 CSS, recovery-to-screenshot focus handoff, complete keyboard rings, core-task preservation, isolated zero-side-effect native proof, current-run visible evidence, and the complete regression/build gates within the stated limits. Assistive-technology, rotated-key model evaluation, performance-harness, and signed-distribution acceptance remain open.

## 2026-07-30 programmatic Reduced Motion navigation follow-up

- Audited the user journey “move from a completed result to its evidence, deadline details, and official-verification plan, or from connection recovery to the exact Settings field” against the operating-system Reduced Motion preference. The CSS foundation already compressed animation and transition timing, but all four production paths passed `behavior: 'smooth'` directly to `scrollIntoView`, which the CSS media rule could not override.
- Added one shared renderer motion policy. It queries `prefers-reduced-motion: reduce` at navigation time, returns `auto` for Reduced Motion and `smooth` for ordinary motion, and fails safely to `auto` when media-query support is unavailable or throws. The evidence, deadline, verification, and recovery paths retain their existing disclosures, focus targets, and live announcements.
- Added a deterministic static gate for both preference states, missing/error fallbacks, the exact media query, the global CSS suppression, all four call sites, and a renderer-wide rejection of new literal smooth-scroll behavior outside the shared policy.
- Added an isolated real Electron gate using Chromium media emulation. The hidden production result fixture runs once with `no-preference` and once with `reduce`; a production deadline click is intercepted at `Element.prototype.scrollIntoView` and must produce `smooth` then `auto`. The gate also verifies the active universal CSS rule, `0.01ms` animation/transition durations, one animation iteration, a fresh temporary profile, loopback-only renderer access, context isolation/sandboxing, stubbed clipboard writes, stripped secret-bearing environment variables, and zero network-trap requests.
- Accepted current-run visible evidence: `docs/ux-evidence/2026-07-30-reduced-motion-navigation/01-before-result-overview.jpeg` through `05-after-official-verification-target.jpeg`. The exact-size deadline comparison places before and after side by side and measures SSIM 0.999902, confirming that the accessibility change does not disturb the destination layout.
- A clean current tree passes complete `npm test`, full lint, the production renderer build, and `git diff --check`; only the existing non-fatal renderer large-chunk advisory remains. The first complete run overlapped a deliberately visible development fixture and tripped an existing native focus-geometry assertion; after closing that competing fixture, the native gate and then the complete suite passed.
- Strengths retained: source/action traceability, explicit deadline and verification announcements, useful focus handoff, ordinary-mode continuity, and a single reusable motion boundary. The supplied credential was not used, entered, persisted, or added to fixture state, logs, or evidence.
- Evidence limits: screenshots establish destination, focus, and visual continuity but cannot show scroll duration; the emulated Electron gate carries the temporal claim. The native probe activates the deadline path, while evidence and Settings recovery are covered by the shared-policy/static contract. This does not claim real VoiceOver speech order, Increase Contrast, operating-system text-size settings, every Reduced Motion surface, signed/notarized installed-app behavior, real clipboard behavior, or live/arbitrary provider quality.

follow-up result: passed for ordinary-versus-reduced programmatic navigation policy, production deadline runtime behavior, global motion suppression, result destination/focus continuity, isolated zero-side-effect evidence, and the complete current-tree regression/lint/build gates within the stated limits. Broad assistive-technology, Increase Contrast, text-size, remaining Reduced Motion, rotated-key quality, performance-harness, and signed-distribution acceptance remain open.

## 2026-07-30 Increase Contrast and forced-colors follow-up

- Audited the completed-result and regular-Settings journeys from the user's viewpoint. Ordinary cards depended heavily on pale boundaries, the completed-action banner referenced an undefined `--success-bg`, the global focus ring was translucent, overlays depended on blur, and several ordinary dark-theme filled buttons paired white text with bright semantic colors at only about 1.86–2.42:1.
- Split semantic text from filled-control colors across ordinary light/dark themes. Accent, error, and warning fills now keep white text at 5.14:1 or better; the completed-action banner has an explicit success surface; focus uses an opaque token; and all modal backdrops share one scrim boundary.
- Added light and dark `prefers-contrast: more` token sets with stronger text and control boundaries, opaque surfaces, explicit two-pixel dialog containment, three-pixel focus, non-opacity disabled/muted states, and no blur, gradients, or shadow-only hierarchy.
- Added `forced-colors: active` behavior using Canvas, CanvasText, ButtonText, Highlight, HighlightText, LinkText, and GrayText. Selected controls, checkboxes, evidence markers, steps, disabled controls, dialogs, and focus remain distinguishable without preserving brand hex colors. Screenshot review found Chromium text backplates hiding content inside Highlight containers; a narrowly scoped system-color opt-out repaired those exact states without disabling forced-color adjustment globally.
- Added `check:contrast-preferences` to `npm test`. Its static half parses all renderer CSS and inline styles, checks ordinary/high-contrast token ratios, rejects semantic backgrounds with hard-coded white text, verifies media/backdrop/focus/system-color contracts, and prevents broad forced-color opt-out. Its isolated Electron half explicitly emulates normal, more, and forced states across Result, Settings, and reset using native Tab modality and computed contrast, boundary, blur, focus, media, and overflow assertions.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-30-increase-contrast/` contains four before captures, two visible after captures, two exact same-viewport before/after sheets, three normal/more/forced comparison sheets, and all nine original-resolution native matrix images. The visible run retains the exact completed Result and unsaved-Settings-dialog states; the emulated native run uses fixed fictional content, an isolated mode-`0700` profile, sandbox/context isolation, an in-memory clipboard, stripped secret-bearing environment variables, and zero blocked-external or network-trap requests.
- The current tree passes complete `npm test`, full lint, the production renderer build, the contrast-preference evidence-mode runtime, and `git diff --check`; only the existing non-fatal renderer large-chunk advisory remains.
- Strengths retained: source/action traceability, honest self-reported completion, local-versus-online Settings hierarchy, destructive-reset consequence copy, focus continuity, and the ordinary Slipstream visual language. The supplied credential was not used, entered, persisted, logged, or placed in fixture state or evidence.
- Evidence limits: Increase Contrast and forced colors are Chromium media emulations, not a manual macOS system-setting or Windows High Contrast pass. Dark enhanced-contrast tokens are statically gated but not separately screenshot-matrixed. First use, capture, Saved Terms, every transition dialog, real VoiceOver speech order, operating-system text sizing, signed/notarized installed-app behavior, real clipboard behavior, live provider output, and arbitrary content remain outside this slice.

follow-up result: passed for ordinary dark filled-control contrast, missing success-surface repair, light/dark Increase Contrast tokens, forced system colors, overlay/focus/boundary behavior, Result/Settings/reset native media emulation, current-run visible comparisons, isolated zero-network evidence, and the complete current-tree regression/lint/build gates within the stated limits. Manual assistive-technology/system-setting acceptance, rotated-key quality, performance-harness, and signed distribution remain open.

## 2026-07-30 bilingual screen-reader semantics and first-use focus follow-up

- Audited the core journey “choose basic translation, enter source text, understand a bilingual result, inspect source-backed actions, prepare an English reply, and reuse a Saved Term” from a screen-reader user's viewpoint. The root document correctly declared `zh-CN`, but ActionBrief `mixed` and `unknown` values could be emitted as invalid HTML language tags; English source/quotes/drafts often inherited the Chinese shell; and several controls flattened Chinese actions plus English evidence into one `aria-label`.
- The first-use choice also removed its focused button without assigning the capture destination, leaving the resulting textarea journey dependent on browser fallback. The processing card wrapped its dedicated atomic status in a second live region, and source evidence described every item with one shared mutable navigation announcement.
- Added one fail-closed language boundary: `en` → `en`, `zh` → `zh-CN`, `mixed` → `mul`, `unknown`/invalid → `und`, plus conservative Chinese/Latin/mixed inference for user and externally sourced fragments. Capture input, source/evidence, deadlines, official excerpts, guided-reply grounding/draft, and Saved Terms now expose content-level boundaries.
- Evidence/action cards and Saved Terms use visible segmented content, labelled relationships, and natural names instead of concatenated bilingual labels. Deadline summary segments the source date while retaining an invisible “查看全部截止日期” action cue. The first-use hidden→visible transition now hands focus to the source textarea after two render frames only when capture is idle and no foreground owner exists; Settings return focus stays higher priority, and Result/Processing do not receive this fallback.
- Removed the processing-card outer live region while retaining its dedicated polite atomic status, and removed the stale shared description from source evidence. These changes reduce duplicate or outdated inputs to assistive technology without claiming actual speech behavior.
- Added `check:screen-reader-semantics` to `npm test`. The static half verifies mappings, invalid-tag rejection, all core call sites, segmented names, focus ownership, and single live owners. The hidden isolated real-Electron half checks seven source-evidence boundaries, seventeen result quotes, five reply-grounding quotes, one saved term, Chromium Accessibility Tree roles/names across Result/reply/Saved Terms/setup/capture, and the actual first-use textarea focus handoff without a test-side focus call. Its sandboxed mode-`0700` profile/session, stripped secret-like environment, denied permissions, loopback renderer, in-memory clipboard, and external-network trap all pass with zero trap requests.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-30-screen-reader-semantics/01-before-result.png` through `11-saved-terms-before-after.png`, with numbered scope and limits in the folder README. The exact-size Result, reply, and Saved Terms comparisons measure SSIM 0.996783, 0.999979, and 0.999996; no visible layout regression was found. The post-choice capture screenshot visibly retains the complete source-textarea focus ring.
- The current tree passes complete `npm test`, full lint, the production renderer build, and `git diff --check`; only the existing non-fatal renderer large-chunk advisory remains. Strengths retained: source/action traceability, honest completion semantics, privacy boundaries, bilingual visual hierarchy, dialog isolation, and the ordinary Slipstream visual language. The supplied credential was not used, entered, persisted, logged, or included in fixture state/evidence.
- Evidence limits: screenshots establish the audited surfaces, focus, and visual continuity; DOM and Chromium AX automation carry the language/name/role claims. This does not exercise real VoiceOver pronunciation, speech order, interruption, rotor navigation, macOS system settings, a signed/notarized installed build, real clipboard/Keychain behavior, live provider output, or arbitrary-input quality.

follow-up result: passed for valid bilingual DOM language boundaries, segmented accessible names, deterministic first-use focus, reduced duplicate/stale live inputs, Result/reply/Saved-Terms Chromium AX coverage, visible no-regression evidence, isolated zero-network runtime proof, and the complete current-tree regression/lint/build gates within the stated limits. Real VoiceOver and remaining manual system-setting acceptance remain open.

## 2026-07-30 processing handoff and Result landmark follow-up

- Continued the core journey from a screen-reader user's viewpoint at the two transitions most likely to feel like a dead end: submitting captured text into processing, and navigating from the completed conclusion through evidence to the final actions. Capture→processing previously allowed focus to fall back to the document body, while the first status could be present before assistive technology observed a change. The Result conclusion and footer also sat outside its named `main`, and the evidence workspace introduced a nested `main` plus a duplicate summary region.
- Capture submission now focuses the processing heading when the panel is visible and no modal, pending-capture decision, or other foreground owner has priority. The one polite atomic status region mounts empty and publishes the initial state on the next animation frame; only meaningful state transitions update it, while elapsed seconds remain visual-only and cannot create a recurring announcement stream. Existing cancellation focus back to capture and successful completion focus to Result remain intact.
- The completed Result now contains its conclusion, evidence workspace, and footer/core actions inside exactly one `main` labelled by the visible result heading. The evidence workspace is a normal container rather than a nested landmark, and the redundant summary-region naming is removed without changing the visible layout or action behavior.
- The targeted `check:screen-reader-semantics` gate passes. Its static contracts cover the processing focus/live owner and Result landmark shape. The isolated hidden real-Electron fixture adds a capture→processing journey without a test-side focus call, proves the heading becomes active, observes an empty live-region mount followed by exactly the bounded status updates, and exposes one processing heading/status in Chromium's Accessibility Tree. The run reports hidden windows, profile exclusion, no inherited secret-like environment variables, and zero external-network-trap requests.
- Both the runtime's default no-write mode and its evidence-output mode pass. Accepted current-run evidence in `docs/ux-evidence/2026-07-30-result-processing-accessibility/` consists of the manually inspected `01-result-named-main.png` and `02-processing-status.png`; the companion README records the isolated mode-`0700` profile, sanitized environment, denied permissions, loopback renderer, network trap, stubbed clipboard, and fictional built-in sample boundary.
- The final tree was copied to a clean temporary directory while preserving the workspace-root structure, then installed from the lockfile with fresh `npm ci`. Complete `npm test`, full lint, and `npm run build:renderer` pass there; the build retains only the existing non-fatal >500 kB chunk advisory.
- Evidence limits: this is screenshot, DOM, and Chromium Accessibility Tree evidence, not real VoiceOver acceptance. VoiceOver pronunciation, speech order, interruption, Rotor navigation, and announcement-queue behavior remain untested while explicit user consent to change the global macOS accessibility setting is pending. The supplied API credential was not used, entered, persisted, logged, or included in fixture state/evidence; live-provider quality still requires a newly rotated process-only credential.

follow-up result: passed for deterministic capture→processing focus, empty-first bounded status semantics, silent elapsed-time updates, one complete named Result landmark, isolated processing DOM/Chromium AX coverage, manually reviewed current-run evidence, and the complete final-tree test/lint/build gates within the stated limits. Real VoiceOver, rotated-key provider quality, and signed-distribution acceptance remain open.

## 2026-07-30 credential-save retry reconciliation follow-up

- Audited the Settings journey “replace a saved connection value, encounter a failed save, use the global retry, then validate the provider.” Before repair, the retry could persist successfully while the originating editor still owned a local failure and dirty draft. The same screen therefore announced success, still said the input must be saved, disabled provider validation, and allowed focus to fall away from a useful action.
- A successful global retry now returns only the saved setting keys. The Settings shell wraps them in a monotonic renderer-only receipt and maps those keys to the exact credential, endpoint, or model editor without transporting stored values back into the renderer.
- Each editor records its draft revision. A matching retry receipt clears only the failed revision; if the user produced a newer same-field draft, that draft stays visible and guarded. Editors remain disabled while the global retry owns the write, preventing an ambiguous edit/save race.
- Recovery removes the obsolete local failure and duplicate live ownership, releases the parent connection-draft guard, enables the provider test, and hands focus there after settlement. If another draft still needs attention, focus instead returns to that editor.
- The isolated hidden Electron DOM/Chromium AX journey injects one credential-save failure, performs the visible global retry, and requires two save attempts, one success, an empty local draft, no failed-save text, one global success live owner, an enabled and focused provider test, and zero external requests. A separate native fixture uses the production Electron boundary with fictional data and rejects any non-canonical scenario URL.
- Accepted current-run evidence: `docs/ux-evidence/2026-07-30-credential-save-retry/03-before-fix-save-retry-contradiction.png` and `04-after-fix-save-retry-recovered.png`. Both were inspected at their original 1040×1360 backing resolution for the same 520×680 logical viewport.
- A clean local source copy with fresh lockfile dependencies passes complete `npm test`, full lint, `npm run build`, the targeted screen-reader semantics gate, the static Settings-transition contract, and the native Electron fixture. The macOS build completes ad-hoc x64 and arm64 artifacts; the existing non-fatal >500 kB renderer chunk advisory remains.
- Strengths retained: saved credentials stay redacted, retry receipts contain keys rather than values, connection drafts remain loss-protected, provider validation remains an explicit user action, and fixture networking stays blocked. The supplied credential was not used, entered, persisted, logged, or included in evidence.
- Evidence limits: this is isolated fixture, DOM/Chromium Accessibility Tree, and Electron automation evidence. It does not prove real VoiceOver speech/announcement order, real Keychain interaction, live provider output or billing, arbitrary-input model quality, Developer ID signing, notarization, or installed-app behavior.

follow-up result: passed for exact-editor retry reconciliation, newer-draft preservation, single-owner success feedback, useful provider-test focus, zero-network fictional evidence, and the complete clean-copy test/lint/build gates within the stated limits. Saved-Term import trust-warning focus, Result disclosure Rotor semantics, real VoiceOver, rotated-key quality, and signed distribution remain open.

## 2026-07-30 Saved Terms import trust-review follow-up

- Audited the journey “open Saved Terms, choose a backup, understand privacy/trust consequences, cancel or confirm, then close and reopen” at a 520×680 viewport. The preview previously focused **Confirm import** before the user encountered its trust downgrade, the warning was neither focusable nor associated with Confirm, its dynamic tail rendered at 9 px, and the exact transfer result could be exposed by both the visible status and the hidden Saved Terms live region.
- Preview entry and re-entry now focus a named `role="note"` trust review after two render frames, guarded by the expected transfer mode and preview ID. The review precedes the action row, carries a complete three-pixel focus ring, states the evidence boundary and safe downgrade policy, and raises dynamic warning text to 10 px. Confirm is described by the stable trust summary plus only the downgrade/capacity warnings that apply.
- The visible transfer status is the sole owner of import/export outcome announcements; it is polite for ordinary outcomes and assertive for failures. The general Saved Terms live region remains available for removal/restore events, while the search count is live only when an active query changes it. Cancellation and completion return focus to the originating import control.
- No-op imports now say there is nothing to change and offer **Return to library**. Close/reopen resets preview identity, pending/mode bookkeeping, prior focus state, and transfer feedback so a delayed callback cannot focus a stale control.
- Current-run evidence in `docs/ux-evidence/2026-07-30-saved-terms-import-trust/` includes the premature-confirm baseline, baseline success surface, repaired trust-focus surface, and an exact-size side-by-side comparison. All images were inspected at original resolution; the repaired layout retains the established drawer hierarchy and keeps both actions reachable.
- The static transfer gate covers semantic relationships, warning readability, one announcement owner, no-op handling, and reset behavior. The isolated hidden Electron DOM/Chromium AX journey covers preview, commit, close, and reopen without test-side focus calls. The native fixture uses trusted clicks and keys to prove natural trust-note focus, Confirm description, warning order, complete 200% focus-ring geometry, one exact cancellation owner, unchanged local terms, and trigger focus restoration.
- The exact changed tree passes complete `npm test`, full lint, `npm run build`, targeted syntax checks, and `git diff --check`. A filename-only credential-pattern scan found only the existing intentional native-fixture URL canary; the same scan excluding that known canary file found no matches. The build completes ad-hoc x64 and arm64 macOS artifacts with only the existing non-fatal >500 kB renderer chunk advisory.
- Strengths retained: preview-before-merge, evidence stripping, fail-closed trust downgrade, stronger-local-record retention, no capacity eviction, privacy copy, keyboard containment, and the existing Slipstream visual language. The supplied credential was not used, entered, persisted, logged, or included in fixture state/evidence.
- Evidence limits: this is screenshot, DOM, Chromium Accessibility Tree, and isolated Electron automation evidence. It does not exercise real VoiceOver pronunciation/order/interruption/Rotor/announcement queues, real file-picker or arbitrary-backup behavior, Keychain/system clipboard behavior, a signed/notarized installed app, or live provider output. Result disclosure heading/ID/`aria-controls` relationships remain the next identified Rotor-navigation gap.

follow-up result: passed for trust-first import preview focus, Confirm consequence descriptions, readable dynamic warnings, no-op and reopen recovery, one transfer announcement owner, native 200% focus geometry, fictional zero-network evidence, and the complete clean-copy test/lint/build gates within the stated limits. Result disclosure Rotor semantics, real VoiceOver/system-setting acceptance, rotated-key quality, performance-harness evidence, and signed distribution remain open.

## 2026-07-30 Result disclosure heading and Rotor-semantics follow-up

- Audited the completed-result journey “scan the result hierarchy, move through disclosure headings, open and close a deadline, then inspect processing details” at a 520×680 browser viewport. The fixture exposes eight disclosures; the shared component supports a conditional ninth warnings disclosure.
- Before repair, the disclosure buttons had no accordion heading wrapper, stable trigger ID, or `aria-controls` relationship, and collapsed content was removed from the DOM. Processing details were also conditionally mounted without a labelled region. The visible controls worked, but a screen-reader user could not rely on a complete heading/IDREF contract.
- Every common disclosure now uses an H2 whose only substantive child is its button. Stable trigger IDs cover `result-translation`, `result-explanation`, `result-materials`, `result-deadlines`, `result-terms`, `result-context`, `result-sources`, `result-verification`, and `result-warnings`; each title and metadata description has a stable relationship, and each controlled panel remains mounted with `hidden` when collapsed.
- Common panels intentionally do not use `role="region"`, preventing eight or nine repeated landmarks from overwhelming the landmark list. The footer processing-completion panel is the single labelled region, controlled by a stable H2/button. Decorative icons and carets are hidden from assistive technology.
- Current-run Chromium semantics show H1 → H2 → H3 order, concise disclosure names with metadata as descriptions, exact trigger/panel IDREFs, deadline open/close with focus retained on its trigger, processing-detail open/close with focus retained, and no horizontal overflow. The completed-evidence toggle also retains a real outline when Increase Contrast removes shadow-only styling.
- The isolated native Electron journey passes twice at exact 400×400 bounds and zoom factor 2. Its 24 accepted steps comprise 21 trusted mouse actions and three trusted keys after one deliberate rejection. Saved Terms proves keyboard focus-ring geometry before the drawer closes; the final two pointer steps prove deadline collapsed → open → collapsed on the same trigger/panel, two fictional deadline cards, natural pointer focus, vertical reachability, zero horizontal loss, and zero application side-effect counters. The two input modalities are intentionally assessed separately instead of requiring Chromium's keyboard-only `:focus-visible` heuristic after a mouse click.
- The narrow 200% rule reduces the representative deadline trigger from 150×89 to 150×42 by compacting horizontal padding and gaps. It stays fully visible in the 69 px Result scrollport without hiding metadata from its `aria-describedby` relationship.
- Accepted current-run evidence is in `docs/ux-evidence/2026-07-30-result-disclosure-rotor/`: six original 520×680 captures plus two exact 1040×680 before/after comparison sheets. All eight files were inspected at original resolution; the established Result layout and footer position remain visually intact.
- The isolated source copy passes complete `npm test`, full lint, targeted syntax/static checks, the screen-reader runtime three consecutive times, the native Result runtime twice, `npm run build`, and `git diff --check`. The build completes ad-hoc x64 and arm64 artifacts with only the existing non-fatal >500 kB renderer chunk advisory. A scan for the exact exposed 32-hex credential shape returned zero repository matches; no credential was entered, persisted, logged, or used for a network request.
- Strengths retained: visible disclosure order, concise title-plus-metadata hierarchy, source/action traceability, deadline evidence, footer actions, reduced landmark count, and the existing Slipstream visual language.
- Evidence limits: this run uses only the fixed fictional fixture. It did not enable or operate real VoiceOver Rotor, request real network/API access, use a provider credential, or assess arbitrary content. Screenshots do not establish assistive-technology behavior; the semantic claims come from current-run DOM/Chromium Accessibility Tree inspection. Hidden native automation does not replace a visible signed/notarized installed-app audit.

follow-up result: passed for current-run browser appearance, H1/H2/H3 Chromium hierarchy, stable accordion IDREFs, common-panel landmark restraint, deadline and processing-detail focus retention, correct pointer-versus-keyboard focus evidence, 200% native reflow, zero horizontal overflow, and the complete test/lint/build gates within the stated fixture limits. Real VoiceOver and signed/notarized installed-app acceptance remain open.

## 2026-07-30 Guided Reply compact and 200% reflow follow-up

- Audited the user journey “open Guided Reply from a completed Result, claim completion while required actions remain unchecked, acknowledge a stale checklist, finish a placeholder, copy, edit after copy, close, reopen, and dismiss with Escape.” Current-run visible evidence covers the established 520×680 drawer and the Codex in-app browser's minimum 240×200 viewport. The browser could not resize to 200×200, so exact 200% evidence comes only from the separate hidden Electron run at 400×400 native bounds and zoom factor 2.
- Before repair, the compact drawer retained its ordinary sticky footer. At 240×200 that footer covered most of the viewport, collapsed the blocker summary into a narrow vertical strip, clipped the title, and continued obscuring the real-status controls after scrolling. The user could not keep the warning, choice, and next action in a dependable reading path.
- The ultra-narrow layout now fills the viewport with one vertically scrolling drawer, removes horizontal scroll, bounds the English editor, makes the footer static and one-column, allows long blocker and clipboard messages to wrap, and keeps every measured button, choice, disclosure summary, and editor at least 32×32 CSS pixels. The 520×680 baseline/after comparison retains the established hierarchy and action placement.
- Safety behavior is preserved rather than simplified away. A completed claim with unchecked mandatory actions exposes the mismatch alert and a blocker summary naming both the progress acknowledgement and unresolved placeholder. Acknowledging stale progress removes only that blocker; placeholder replacement remains mandatory. Copy produces one explicit confirmation, and any later edit immediately replaces it with the outdated-version warning.
- The exact hidden `guided-reply-text-scale-native` journey measures a 200×200 CSS viewport and accepts 18 ordered trusted steps: six pointer actions, two fixture-owned fixed text insertions, nine Tabs, and one Escape, after one deliberate out-of-sequence edit is rejected. It proves visible initial radio focus, modal background isolation, complete nine-stop focus containment and rings, static non-overlapping footer geometry, zero page/drawer/footer horizontal overflow, state retention after close/reopen, and exact Result-trigger focus return after both Close and Escape.
- The native proof permits exactly one application clipboard request and one in-memory native write stub. Processing, screenshot, provider validation, credential deletion/write, and quit counters stay at zero; the blocked renderer-request count and external network-trap count remain zero. All fixture content and edits are fixed fiction, and no real credential was used, entered, stored, logged, or included in evidence.
- Accepted current-run evidence is in `docs/ux-evidence/2026-07-30-guided-reply-text-scale/`: twelve numbered JPEGs, one exact 240×200 before/after comparison, one exact 520×680 comparison, and a numbered audit README. All 14 accepted images were inspected at original resolution.
- Strengths: the repair keeps the truthfulness contract intact at the hardest viewport; treats progress and placeholder completion as independent blockers; explains why Copy is disabled; preserves version-aware clipboard feedback; restores the exact trigger; and improves compact reflow without flattening the ordinary desktop layout.
- UX risks: the exact minimum still requires substantial vertical paging between status, warning, draft, copy feedback, and footer. The stale-checklist acknowledgement is intentionally dense and high-friction, and the copied/outdated notices lengthen the path further. The 32×32 target floor is the compact desktop contract, not evidence of a 44×44 touch interface.
- Accessibility risks and evidence limits: visible captures prove clipping and visual focus only; hidden Chromium/Electron checks prove DOM focus containment and geometry, not real VoiceOver labels, pronunciation, speech order, Rotor behavior, interruption, or live-region queuing. Manual macOS Increase Contrast on this exact drawer, operating-system text sizing, remaining Reduced Motion presentation, arbitrary reply content, real Keychain/system clipboard behavior, live provider output, and a signed/notarized installed build remain outside this slice.
- Current validation passes the browser walkthrough, `check:reply-progress-consistency`, targeted ESLint for `ResultDisplay.jsx`, PostCSS parsing of the touched CSS, and `git diff --check`. An isolated physical copy of the exact current source also passes complete `npm test`, full-repository lint, the renderer build, unsigned x64/arm64 ZIP and DMG packaging, SHA-256 checksum generation, and release-artifact inspection. The renderer retains one non-fatal large-chunk advisory (697.98 kB minified / 196.28 kB gzip).

follow-up result: passed for visible compact reflow, exact hidden 200×200 geometry, independent safety blockers, copied-to-outdated version feedback, native modal/focus containment, exact Close/Escape trigger restoration, fictional zero-network evidence, complete final-tree regression/lint/build, checksummed unsigned dual-architecture artifact inspection, and the stated targeted checks. Real VoiceOver/system-setting acceptance, rotated-key live quality, performance-harness evidence, and Developer ID signed/notarized distribution remain open.

## 2026-07-31 startup readiness and private renderer-baseline follow-up

- Audited the first useful interaction for fresh setup and returning capture, plus the previously withdrawn startup-measurement boundary.
- Formal BrowserWindows now remain hidden until `did-finish-load`. A production `loadFile` failure opens a native Retry/Quit decision whose copy does not expose an installation path, settings content, or API Key.
- After validated settings settle, fresh setup focuses the recommended CTA and returning capture focuses the source textarea. Recovery, modals, existing user focus, and other foreground owners retain priority.
- The replacement renderer harness is separately identified and non-packaged. It uses a private current-renderer build, mode-`0700` `HOME`/`TMPDIR`/`userData`/`sessionData`, an explicit environment allowlist, denied renderer networking and permissions, fixed adapters, hidden windows, fresh processes/profiles, and pre/post ad-hoc bundle-identity verification.
- The full-contract run recorded 40 samples after five warmups per scenario in counterbalanced order. First-use semantic DOM receipt was p50 240.150 ms, p95 241.641 ms, max 241.963 ms; returning capture was p50 240.546 ms, p95 241.956 ms, max 244.616 ms. Spawn-to-`did-finish-load` receipt p95 was 201.751 ms and 200.704 ms respectively. Unexpected adapter calls were zero.
- FCP was unavailable in every recorded sample. The run therefore makes no paint or compositor-visible claim. It is hidden, same-machine, warm-filesystem, direct-exec, ad-hoc evidence—not a physical cold boot, Finder/Gatekeeper/quarantine/translocation/first-install pass, signed/notarized installed-app acceptance, or a basis for an absolute release threshold.
- Machine-readable evidence: `docs/ux-evidence/2026-07-31-startup-readiness-performance/04-renderer-hidden-startup-baseline.json`.
- Current renderer-state evidence in the same directory includes exact 520×680 loading, first-use, and returning-capture PNGs plus `05-startup-evidence-proof.json`. All three were inspected at original output size. The source captures were exactly 1040×1360 at DPR 2 and were normalized only after the raw/content/DPR relationship passed; setup and capture matched their intended DOM focus targets, windows stayed hidden, and unexpected adapter calls remained zero.
- Visual findings: the loading state has a clear identity and one calm message instead of an empty shell; the recommended first-use path and primary action stay above the fold; and returning users land on the source-entry task with the safe example, distinct capture routes, and processing-location disclosure visible. The alternative first-use choice continues below the fold by design and remains in the same scroll surface.
- The broader native fixture timeout occurred before window creation: its validated development-only path synchronously loaded the production persistence graph through the store, LLM service, and provider readiness. The fixture now uses a strict memory-only settings adapter and omits only unreachable LLM/readiness imports after fixture validation; packaged and ordinary production paths still require the formal modules. The base native fixture passes twice at about five seconds and the complete native runtime suite passes.
- Targeted startup, recovery, modal, first-run, retired-mode, harness-isolation, screen-reader semantics, and native-fixture checks pass. The exact final tree also passes complete `npm test`, full-repository lint, and `npm run build:renderer`; the only build notice is the existing 692.64 kB minified / 196.39 kB gzip chunk advisory. No signed/notarized release gate is claimed.

follow-up result: passed for the isolated hidden renderer measurement contract, deterministic priority-aware startup interaction, reviewed startup-state evidence, and restored native-fixture reliability within the stated limits. Visible signed/notarized installed-app startup, FCP/compositor readiness, representative older/Intel hardware, and any absolute release threshold remain open.

## 2026-07-31 advanced analysis instructions draft-safety follow-up

- Audited the user journey “open optional advanced instructions, write a multiline prompt, leave with Escape or Return, finish first-use setup, recover a failed save, and deliberately discard only a newer draft” at an exact 520×680 viewport.
- Before repair, the textarea owned only local state and persisted on blur. Settings handled Escape in capture phase, unmounted the editor, and reopened with an empty value and no warning. The same draft could also be lost by completing first-use setup or retrying a translation fallback.
- The editor now uses explicit save, accepts an explicit empty value as the default, keeps Enter multiline, and distinguishes dirty, saving, failed, saved, and default states. Dirty instructions participate in the same Settings draft owner as connection fields across close, capture, provider/path changes, fallback completion, and full-mode activation.
- Failed prompt writes retain the draft and a correlated global retry. Returning to the persisted value or explicitly discarding the draft revokes exactly `customPrompt`, so abandoned text cannot be written later. Independent failed settings merge by key instead of replacing the earlier retry transaction.
- Retry success opens both enclosing disclosures before focusing the visible prompt textarea and falls back to a connected control if focus still cannot land. Escape cancellation returns to the textarea; a real Return-button action returns to that button; first-use enable cancellation returns to its enable trigger.
- Current-run evidence in `docs/ux-evidence/2026-07-31-custom-prompt-draft-safety/` contains eight accepted 520×680 states and two same-run before/after sheets. All ten images were inspected at original resolution; the repaired editor retains the established textarea geometry and Settings hierarchy.
- The isolated native fixture proves the first-use guard, distinct focus origins, failed A → persisted S retry revocation, folded-details retry recovery, and saved A authority after explicit B discard. Its final proof records three prompt-write requests, one success, and zero network-trap requests while exposing only booleans and safe counts.
- Targeted final-source gates pass for Settings transitions, Settings accessibility, quit safety, first-run readiness, renderer leaf-icon ownership, native fixture source, and native Electron runtime. Targeted whitespace checks also pass.
- The production renderer build passes after constraining 18 component imports to 47 Phosphor leaf exports: 186 modules in 38.26 seconds versus a deliberately stopped lower bound of more than 22 minutes and 3,163 modules through the root barrel. The final 696.13 kB minified / 197.66 kB gzip chunk and `vite:css-post` timing remain open performance debt.
- Two repeated full-contract hidden startup runs each satisfy the 40-sample/five-warmup contract with zero unexpected adapter calls. First-use semantic DOM p95 is 282.762 ms and 296.393 ms; returning capture is 285.365 ms and 295.622 ms. FCP is unavailable, and cross-run machine load is not controlled tightly enough to attribute the difference from the earlier same-day baseline. Full repository lint and complete `npm test` were not re-established on this final tree because local macOS dependency reads repeatedly stalled; the focused gates and production build above are the claimed boundary.
- Strengths retained: saved credentials remain redacted, retry receipts carry keys rather than values, the fixture uses fixed fictional content, and no provider request or real credential enters the app or evidence.
- Risks and limits: the optional editor remains below two disclosures; the effective prompt-length boundary is not explained beyond the count; real VoiceOver behavior, arbitrary prompts, live model quality/billing, Keychain behavior, system text sizing, and signed/notarized installed-app acceptance remain outside this slice.

follow-up result: passed for visible explicit-save feedback, shared exit ownership, exact stale-retry revocation, merged failed-setting recovery, folded-control focus restoration, trustworthy native focus origins, fictional zero-network evidence, leaf-icon build recovery, and repeated hidden-startup measurement within the stated limits. Full lint/complete-test verification and external release acceptance remain separately tracked.

## 2026-07-31 renderer startup code-splitting follow-up

- Audited fresh setup, returning capture, explicit Settings entry, and completed Result at the established 520×680 viewport before changing the startup graph.
- The result boundary initially could not split: App, FloatingPanel, Settings, and Saved Terms all borrowed `ClipboardActionNotice` from `ResultDisplay.jsx`. That small safety surface now has its own eager module, while Settings and Result are preloadable dynamic workspaces.
- The hidden FloatingPanel deliberately remains mounted during first use and Settings so Option+C/F2 ownership, capture subscriptions, and in-memory task state do not disappear. Settings and Result use named, nonblank, focus-owning fallbacks; Result begins preloading during processing or session recovery.
- First-use before/after screenshots are pixel-identical (SSIM 1.000000). Returning capture is 0.999633, with only the textarea caret visibly different. Settings resolves with one visible main and Return focus; Result resolves with one named main and conclusion-heading focus.
- The production entry changes from 696,136-byte JavaScript plus 182,851-byte CSS to 492,036-byte JavaScript plus 130,734-byte CSS, a combined raw reduction of 256,217 bytes (29.15%). Settings owns an async 131,028-byte JS / 47,618-byte CSS pair and Result owns 77,115-byte JS / 4,501-byte CSS. The previous >500 kB Vite warning is absent.
- The current hidden full-contract run records 40 samples after five warmups per scenario and zero unexpected adapter calls. Semantic-DOM p95 is 284.764 ms for first use and 291.223 ms for returning capture. FCP is unavailable, so this remains semantic, hidden, warm-filesystem evidence rather than a visible launch or release claim.
- Current-run evidence and the numbered audit are in `docs/ux-evidence/2026-07-31-renderer-startup-code-splitting/`. Static lazy-boundary, clipboard privacy, first-run, modal ownership, Settings accessibility, renderer evidence, capture ownership, native fixture source, and Chromium Accessibility Tree Electron runtime gates pass. The screen-reader semantics harness now gives cold Vite startup and async Settings transformation enough infrastructure-only time without changing product thresholds.
- The complete native Electron UI fixture runtime suite also passes on the final source, covering the established Settings, Result, Saved Terms, draft, clipboard, recovery, compact-layout, and focus-restoration journeys with isolated fixture data.
- Remaining risks: contextual chunk failure has no in-place retry; Saved Terms remains eager; Result/Saved Terms/reply rules still occupy entry-owned `App.css`; and complete `npm test` plus full-repository lint were not re-established because local ESLint/dependency reads stalled. Real VoiceOver, signed/notarized visible startup, physical cold start, older/Intel hardware, and live provider output remain outside this slice.

follow-up result: passed for a materially smaller startup dependency graph, unchanged established first-use/capture presentation, preserved capture ownership, correct lazy-workspace focus handoff, isolated hidden startup contract, and the stated focused regression gates. Saved Terms/CSS deferral, in-place chunk retry, full clean-copy verification, and external release acceptance remain open.

## 2026-07-31 lazy workspace recovery follow-up

- Audited the user journey “a completed Result or Settings workspace chunk rejects after being preloaded, the user sees a recoverable state, retries, safely returns or edits the source, and continues with the same task” at the established 520×680 viewport.
- Before repair, a rejected workspace import escaped its Suspense fallback and left the app root blank. Current-run evidence records a black Settings root and white Result root. The Settings comparison image is an exact 520×680 centre crop of the uniformly blank 1404×795 source; all repaired states are original 520×680 captures.
- The final failure proof uses a real Vite module request that receives HTTP 503, not a rejected Promise substituted inside the renderer. Settings and Result have narrow error boundaries outside their Suspense boundaries, and rejected imports are tagged as `LazyWorkspaceLoadError` before they reach those boundaries. An unexpected render error still exposes the state-preserving escape and restart guidance, but deliberately does not expose the module-retry action.
- Production emits stable `SettingsPanel.js` and `ResultDisplay.js` filenames. A tagged load failure permits exactly one bounded retry to the same physical file with `?workspace-attempt=1`; this creates a new module URL without random or repeated cache busting. The retry clears only the matching cached import and creates a fresh `React.lazy` identity, while App and FloatingPanel stay mounted.
- A hidden `file://` Electron harness proves the browser behavior behind that contract: after the primary module URL fails, adding the physical file does not make the same URL importable, while the query-qualified URL succeeds in the same renderer without a navigation or reload.
- Each production workspace JavaScript file imports only the already-loaded hashed entry. There are no async CSS assets: Settings and Result styles are eager in the entry stylesheet, so the JavaScript retry cannot inherit a failed CSS preload or another shared async dependency URL.
- The eager recovery surfaces use the established Setup/processing visual language. Each exposes one heading-labelled `main`, one nested alert, a focused primary retry, and a safe secondary escape. Focus waits two animation frames and yields whenever a modal or application top layer owns interaction; successful recovery restores focus to Settings Return or the Result conclusion, and safe returns restore the preserved source or Result target.
- Result copy states that analysis is already complete and retry will not call the model again. Its source escape returns to the intact editor; reopening Result shows the existing completion without reprocessing. Settings copy states that retry will not send source, call a model, or save settings. Its return path restores the same completed Result, and reopening Settings succeeds.
- The exact development-only native fixture proves Result failure → retry, Settings failure → retry → return, tagged-load versus unexpected-render handling, named-main/alert ownership, retry and recovered-workspace focus, unchanged result content, zero settings writes, zero provider processing, zero renderer reload/navigation, and zero external requests. Malformed, reordered, or extended scenario URLs are rejected.
- The production renderer contains five files totalling 892,738 bytes: entry JavaScript 499,097 bytes / 147.28 kB gzip, eager entry CSS 184,429 bytes / 26.29 kB gzip, Result JavaScript 77,328 bytes / 23.70 kB gzip, Settings JavaScript 131,241 bytes / 37.06 kB gzip, and `index.html`. The combined entry is 683,526 bytes, 195,461 bytes (22.24%) below the 878,987-byte pre-split entry. A production scan finds none of the fixture trigger or injected-failure sentinels.
- The exact final hidden run records 40 samples after five warmups per scenario and zero unexpected adapter calls. First-use `did-finish-load` p95 is 243.670 ms; semantic DOM is p50 265.872 ms, p95 284.157 ms, max 290.544 ms. Returning capture `did-finish-load` p95 is 248.603 ms; semantic DOM is p50 275.447 ms, p95 291.058 ms, max 308.691 ms. FCP is unavailable in all 40/40 samples in both groups, and `releaseClaimEligible` is false.
- Accepted current-run evidence is in `docs/ux-evidence/2026-07-31-lazy-workspace-recovery/`: seven JPEGs, a numbered audit README, and a machine-readable summary. Every accepted image was inspected at original resolution.
- Strengths: recovery is explicit and local; the retry contract is finite and tied only to tagged load errors; user work remains visible as preserved; potentially billable provider work is never described as a retry target; safe exits remain available; and test triggers are removed from production output.
- UX and accessibility risks: two recovery buttons add a decision at an already disruptive moment, long localized copy may require more vertical reading on smaller windows, and programmatic focus plus the alert may cause some screen readers to announce overlapping context. One alert is retained so the failure is not silent, but real VoiceOver speech order and interruption remain unverified.
- Evidence limits: screenshots prove visible layout and focus styling only. The fixed fixture uses fictional content and no network/API credential. Current DOM/Chromium Accessibility Tree and Electron evidence do not replace real VoiceOver, arbitrary-content, signed/notarized installed-app, physical cold-start, or representative older/Intel hardware acceptance.

follow-up result: passed for real-request 503 recovery, one bounded same-file/new-URL production retry, load-only retry classification, eager workspace styling, state-preserving safe exits, deterministic focus/landmark ownership, zero repeated processing or settings writes, production fixture isolation, production artifact inspection, and hidden startup no-regression within the stated limits. Saved Terms deferral, full clean-copy lint/test, real VoiceOver, and signed/notarized distribution remain open.

## 2026-07-31 Saved Terms on-demand loading follow-up

- Numbered journey and health:
  1. **Completed Result before opening Saved Terms — healthy baseline.** The 520×680 capture shows the established Saved Terms trigger, one saved item count, completed-result hierarchy, and no blank or competing workspace.
  2. **Express intent and open — healthy production runtime.** Pointer/focus intent can preload, while click synchronously claims foreground/modal ownership. Startup still requests zero Saved Terms resources; first open adds exactly `SavedTermsLibrary.js` and no CSS resource.
  3. **Use the loaded library — healthy baseline and runtime semantics.** The baseline capture shows one drawer, search-first hierarchy, one term, and import/export actions. The runtime gate proves one heading-labelled dialog, focused search, inert/hidden background, and no external or persistence side effects.
  4. **Close and reopen — healthy.** The workspace stays mounted, the renderer keeps the same `performance.timeOrigin`, reopening requests no new resource, search receives focus again, and close returns focus to the Result trigger. The established search-query reset-on-reopen behavior is intentionally unchanged.
  5. **Primary load fails, then retry — healthy recovery contract.** One dialog and one alert remain visible, the retry is focused, the background stays isolated, and exactly one query-distinct `?workspace-attempt=1` request recovers the same physical asset. Successful recovery focuses search and later returns focus to the trigger without navigation, provider work, or settings/term writes.
- The implementation uses a one-way mount latch for ordinary use and advances a session generation only during full data reset. The loading/recovery surface owns modal semantics while the lazy component is unavailable, then yields ownership cleanly to the loaded library. Both layers defer Escape and Tab to a higher `[data-app-top-layer]` owner and reacquire focus after that owner closes.
- Strengths: Saved Terms workspace JavaScript loading/evaluation now follows explicit user intent; loading is never a blank panel; recovery is finite and accurately limited to tagged load failures; ordinary close/reopen preserves the task session; full reset remains authoritative; same-frame capture and clipboard decisions respect the drawer immediately; and fixed adapters prove zero network/provider/settings/term side effects.
- UX and accessibility risks: the first open can still expose a short loading state on a cold or cloud-backed filesystem; a retry adds a decision during failure; long localized recovery text may require more vertical reading; and a focused control plus one alert may produce overlapping spoken context in some screen readers. Saved Terms CSS and the `terms:get` data read remain eager, so this is JavaScript deferral rather than complete feature deferral.
- Evidence limits: `01-result-before-open.png` and `02-library-open-before.png` are current-run 520×680 **pre-change baselines** and cannot prove the final lazy request, loading, or recovery behavior. Their `.png` filenames contain JPEG image data. The browser connection failed before final visual recapture, so final-state evidence is production `file:` request/DOM/focus proof rather than an after screenshot. Chromium DOM/Accessibility Tree assertions do not replace real VoiceOver pronunciation, reading order, Rotor, interruption, or announcement-queue acceptance. Final-tree full performance, complete `npm test`, full lint, and the screen-reader/reduced-motion/contrast Electron reruns were blocked before product assertions by a nearly full Desktop volume and cloud-offloaded `node_modules`; signed/notarized installed-app, physical cold-start, representative older/Intel hardware, arbitrary content, and live-provider quality remain outside this slice. The user-supplied credential was not used, entered, persisted, logged, or included in evidence and should be rotated before any future process-only live run.

follow-up result: passed for production Saved Terms JavaScript deferral, one finite recovery URL, modal-safe loading and recovery, state-preserving close/reopen, full-reset remount authority, same-frame foreground ownership, zero external/provider/persistence side effects, and the stated production resource graph. Final visual recapture, complete final-tree test/lint/performance reruns, retryable CSS deferral, real VoiceOver, and signed/notarized distribution remain open.

## 2026-08-03 production renderer fixture-boundary follow-up

- Audited the returning capture and fixed fictional safe-sample journey at an exact 520×680 viewport before and after the production boundary change.
- All renderer IPC consumers now import one build-selected alias. Vite serve retains the deterministic preview/native-fixture adapter; Vite build selects a minimal adapter that delegates only to the preload `window.api.invoke/on` bridge.
- Production entry JavaScript changes from 506,899 B / 148,685 B gzip to 487,656 B / 142,922 B gzip. The >500 kB warning is absent, and the artifact gate now fails if the entry reaches 500,000 bytes.
- The production scan rejects representative demo OCR, preview failure, process counter, and native fixture sentinels. Stable Result, Settings, and Saved Terms assets plus eager workspace CSS remain unchanged, so no user-facing async failure boundary was added.
- Current-run comparison images are in `docs/ux-evidence/2026-08-03-production-renderer-boundary/`. Empty capture is pixel-identical (`SSIM 1.000000`); the safe sample is visually equivalent (`SSIM 0.999780`) with only transient editor rendering/caret state differing.
- The development/native fixture matrix, renderer startup harness isolation, startup first interaction, production build, full lint, and two-sample smoke plumbing pass with zero unexpected adapter calls. Smoke mode is not performance evidence and remains release-ineligible.
- Strengths: smaller production behavior surface, explicit build-time ownership, no new request or recovery state, preserved processing-location disclosure, and an enforceable budget. Risks: only 12,344 raw bytes of entry headroom remain, CSS deferral still lacks packaged recovery evidence, and the screenshots do not prove a visible startup improvement.
- Evidence limits: fixed fictional content and a development adapter only; no live provider request, credential, billing, arbitrary-content quality, signed/notarized installed app, representative Intel/older Mac, or real VoiceOver acceptance.

follow-up result: passed for fixture-free production renderer IPC, preserved deterministic development fixtures, sub-500,000-byte entry enforcement, production sentinel removal, unchanged capture presentation, and the stated non-release evidence boundary.

## 2026-08-03 packaged main-process fixture-boundary follow-up

- The native UI proof runner, in-memory settings store, fixture IPC handlers, interception, and timing moved mechanically from formal `src/main/main.js` into the development-only `scripts/ui-fixture-main.js`. Formal startup keeps strict request detection, packaged rejection before dynamic loading, and a minimal attachment seam.
- Formal `main.js` falls from 444,076 B to 87,255 B. Excluding the former 17,972-byte fixture resolver removes 374,793 B (81.12%) from the packaged main-process source set.
- Package configuration now admits only `dist/renderer`, excludes fixture modules and source maps, rejects sibling generated trees, and allows exactly the two required OCR source resources. The release gate inspects the actual ZIP/ASAR contents rather than trusting configuration alone.
- A minimal clean copy completed fresh `npm ci`, unsigned x64 and arm64 DMG/ZIP builds, and `check-release-info` against both archives. The exact ASAR checks find the renderer entry, no fixture modules or proof markers, no unexpected `dist` tree, and only the compiled OCR helper plus shell runner in final runtime resources.
- Independent reviews passed after closing three adversarial artifact holes: wildcard/renamed extra resources, fixture-bearing source maps, and broad `dist` sibling inclusion. Packaged hostile fixture flags were also simulated and cannot touch the excluded module.
- Strengths: smaller shipped behavior and audit surface, exact allowlists instead of broad packaging globs, real-archive verification, and unchanged visible capture behavior. Risks: development routing seams remain in formal main for fixture attachment, and the unsigned archive result does not substitute for installed signed/notarized acceptance.
- Accessibility and evidence limits are unchanged: no visible control or semantic changed; Browser DOM and visual parity do not replace real VoiceOver; archive inspection does not prove live-provider quality, arbitrary content, Gatekeeper/translocation behavior, or older/Intel Mac performance.

follow-up result: passed for the development-only main-process proof boundary, exact renderer/resource packaging allowlists, dual-architecture real-ASAR inspection, preserved fixture coverage, and the stated unsigned/non-performance evidence boundary.

## 2026-08-03 OCR review-before-transfer follow-up

- Numbered journey and health:
  1. **Low-confidence screenshot completes OCR — previously critical, now healthy.** The pre-change 520×680 capture entered DeepSeek processing immediately. The repaired state returns to the editable capture surface, focuses “先核对截图文字”, names the 34% average confidence and nine low-confidence blocks, and records exactly one screenshot request with zero processing requests.
  2. **Read and decide — healthy.** The same region states both the current consequence (“完整原文尚未发送给 DeepSeek”) and the next consequence (“核对后，完整原文将发送给 DeepSeek”). The editor references those descriptions and keeps the complete recognized text available for correction.
  3. **Edit instead of confirming — healthy.** The first change revokes delayed dispatch ownership, converts the source to manual, clears stale OCR metadata and review state, and states that no automatic send occurred. The process counter remains zero until a separate Generate or Command+Enter action.
  4. **Confirm unchanged OCR — healthy.** The renderer computes SHA-256 confirmation from the exact current editor value and the canonical current provider/location/exact configurable endpoint. Retained consent also owns the monotonic processing-config revision, so D → E → D and same-provider credential replacement cannot revive an earlier receipt. The main process reads authoritative settings first, normalizes and bounds OCR metadata, recomputes both hashes, and rejects missing, malformed, extra-field, uppercase, stale-text, changed-provider, or changed-endpoint confirmation before background handoff, task creation, or provider work. Explicit confirmation advances the deterministic process counter exactly once.
  5. **Recover a main-process disagreement — healthy.** Renderer and main now share the 2,000-character meaningful-block boundary. If authoritative validation still rejects a stale or mismatched receipt, the response reinstalls the exact OCR source, capture metadata, authoritative reasons, and focused editable review at idle; it does not route through generic provider retry.
  6. **Background and reflow boundaries — healthy within scope.** Review-required OCR ends its local background task successfully rather than holding a four-second analysis handoff. At 280×680 the heading, editor, 44 px primary action, and recapture action stay in one scrollable, non-overflowing column.
- Strengths: the highest-cost error is stopped before transfer; destination and consequence are adjacent to the decision; editing is useful rather than punitive; screenshot pixels remain ephemeral; block-level weakness cannot hide behind a high average; and backend/endpoint changes cannot reuse a retained receipt. Renderer plus main defenses cover ordinary click, Command+Enter, legacy OCR payloads, stale retries, malformed receipts, and accidental source relabelling.
- UX/accessibility risks: the review adds reading and one explicit action to low-quality captures; long recognized text requires scrolling; focus plus the labelled region may be announced with some repetition by real screen readers; and the first version does not highlight individual low-confidence tokens because OCR block/text normalization cannot yet guarantee safe offsets. The copy therefore names categories to inspect without claiming token precision.
- Current-run evidence is in `docs/ux-evidence/2026-08-03-ocr-review-before-transfer/`, including same-size before/after comparison, edit-with-zero-send, confirmed processing, and 280 px reflow. Final-tree `npm test`, full ESLint, focused OCR helper/main gate and recovery checks, and the production renderer build pass; entry JavaScript remains under budget at 499,109 bytes.
- Evidence limits: the deterministic Browser adapter uses fixed fictional content and no real provider request. Pure main-process gates prove ordering, source/destination binding, pending-record lifecycle, and strict confirmation semantics but do not replace live billing/network observation. Hash receipts and sender-scoped pending state protect ordinary, stale, malformed, and accidentally relabelled sequences; they do not prove a user gesture against a compromised renderer able to alter text and mint public hashes. Browser/Chromium focus and responsive evidence do not replace real VoiceOver, system text sizing, signed/notarized installed-app, or older/Intel hardware acceptance. The exposed credential was not used, sent, logged, or persisted and must be revoked before future live testing.

follow-up result: passed for explicit low-confidence OCR review before transfer, exact-source-and-destination confirmation, destination-change invalidation, recoverable bounded main-process enforcement, honest background completion, edit-without-send behavior, deterministic keyboard/focus ownership, and narrow-window reflow within the stated zero-network evidence boundary.

## 2026-08-03 startup recovery focus and entry-reserve follow-up

- Numbered journey and health:
  1. **Reach corrupt-settings protection — healthy.** The normal runtime and first-use defaults remain blocked, the original settings are described as untouched, and Retry owns initial focus.
  2. **Open fresh recovery — previously keyboard-broken, now healthy.** The destructive confirmation previously rendered without owning a focus target. It now focuses its labelled heading with the established warning focus ring.
  3. **Cancel the decision — healthy.** Idle Escape closes only the confirmation and returns focus to the newly rendered fresh-recovery trigger. While application quit is the visible `[data-app-top-layer]`, the background recovery step yields Escape and preserves its context; recovery in progress remains non-dismissible.
  4. **Complete recovery — healthy within fixture scope.** A single confirmed archive request removes the blocked screen, enters first-use setup, exposes only the safe archive basename, and focuses the local recovery notice.
  5. **Recheck the ordinary capture-to-result path — healthy.** Empty capture, safe sample, processing, and the action-first fixed result remain visually and semantically unchanged. Same-state Result screenshots have SSIM 1.000000.
- The focus repair initially left only 313 raw bytes below the 500,000-byte entry ceiling. Full reply/evidence composition now remains in the lazy Result workspace while a compact shared helper preserves identical reply-progress semantics in capture. The final entry is 486,198 B / 143.31 kB gzip, with 3,802 B under the enforced 490,000-byte working budget and 13,802 B below the release ceiling.
- Strengths: the destructive path now has explicit keyboard ownership, exact focus return, honest layer priority, and success handoff; the core happy path remains stable; and local development has measurable bundle reserve instead of depending on a near-zero release margin.
- UX/accessibility risks: a focused heading may be announced with nearby alert content in a different order by real screen readers, and the confirmation still requires careful reading before the checkbox is enabled. The smaller entry is a maintainability/startup-risk improvement, not a measured physical startup-speed claim.
- Current-run screenshots and audit notes are in `docs/ux-evidence/2026-08-03-startup-recovery-focus/` and `docs/ux-evidence/2026-08-03-core-journey-maturity-audit/`. Complete `npm test`, focused storage recovery, modal ownership, reply-progress parity, lazy boundaries, native fixture source/runtime, screen-reader semantics, full lint, production build, and whitespace gates pass. Independent P1/P2 review passes after the stacked-Escape repair.
- Evidence limits: Browser and Electron fixtures use fictional/in-memory state, do not touch the normal settings store, and make zero external provider requests. They do not replace a real corrupt-file archive/restore smoke test, VoiceOver speech/Rotor/interruption acceptance, signed/notarized installed-app behavior, physical cold-start measurement, arbitrary content, or representative older/Intel hardware.

follow-up result: passed for corrupt-settings keyboard ownership, single-layer Escape behavior, exact focus restoration, focused recovery success, unchanged core-journey presentation, compact reply-progress parity, and enforceable production-entry reserve within the stated zero-network evidence boundary.

## 2026-08-03 manual clipboard replacement guard follow-up

- Numbered journey and health:
  1. **Start with unsent work — healthy baseline.** A current-run 54-character draft owns the source editor and focus. Before the repair, one manual read immediately replaced it with the 440-character clipboard fixture without a consequence preview or confirmation; this was a critical work-loss path.
  2. **Read while work is protected — healthy after repair.** The same first click leaves the draft byte-for-byte unchanged and moves focus to a labelled decision that says the current content is unchanged, names Replace and Keep, and states that neither processing nor model transfer has started.
  3. **Keep current work — healthy.** Keep discards only the newly read in-memory text, preserves the draft and any existing recovery/error context, and returns focus to the exact read control. Empty, failed, and stale reads do not mutate source content.
  4. **Replace deliberately — healthy.** Replace synchronizes the source and any active correction draft, focuses character zero, and shows “尚未开始处理”; it does not call processing, write the clipboard, or create an external request.
  5. **Protect correction and Undo — healthy.** An unsaved source correction survives the first read and is replaced only after confirmation. A live clear-undo opportunity likewise survives Read and Keep, after which Undo restores the exact source.
  6. **Handle competing captures — healthy.** A waiting manual-read decision has priority over later automatic or shortcut captures. Those later values are not retained, and the polite status announcement now says so instead of falsely claiming that they are waiting.
- Strengths: the destructive boundary is adjacent to the user action; labels describe concrete consequences; exact focus restoration supports keyboard continuity; source-edit synchronization avoids split state; and both static contracts and isolated native Electron evidence assert zero implicit provider, clipboard-write, or external-network work.
- UX and accessibility risks: the extra decision adds one step when the user intentionally wants replacement, and long localized explanation text still requires reading. Programmatic heading focus plus a polite status may be spoken differently by real VoiceOver. The entry remains only 14 raw bytes below the working budget, so the next renderer change must reclaim reserve rather than relax the gate.
- Current-run accepted evidence is in `docs/ux-evidence/2026-08-03-manual-clipboard-replacement-guard/`: the pre-fix overwrite, post-fix first decision, Keep, explicit Replace, source-edit synchronization, and a same-size before/after comparison. Every accepted image was inspected at original resolution.
- Evidence limits: the Browser and Electron fixtures use deterministic fictional text, an in-memory clipboard adapter, sanitized inherited secrets, and blocked external networking. They do not exercise the real system clipboard, a live provider, arbitrary content, real VoiceOver speech/Rotor/interruption, signed/notarized installed-app behavior, or representative older/Intel hardware. The exposed credential was not used, sent, logged, or persisted and must be rotated before any future process-only live run.

follow-up result: passed for explicit manual replacement ownership, exact draft/correction/Undo preservation, Keep and Replace focus handoff, no automatic processing, truthful collision announcements, and isolated zero-network evidence within the stated limits. Final complete regression and lint results are recorded in `PROGRESS.md`.

## 2026-08-03 Clear Undo timer ownership during manual read

- Numbered journey and health:
  1. **Clear current work — healthy baseline.** Clear keeps the exact source/result/reply snapshot only in memory and exposes an actionable ten-second Undo.
  2. **Read the clipboard before Undo expires — previously critical, now healthy.** The prior replacement guard disabled Undo but left its independent expiry running, so reading the decision for ten seconds could silently delete the only snapshot. The read now pauses the exact remaining milliseconds synchronously before its IPC request and binds that pause to the exact read attempt and cleared-session object.
  3. **Review the replacement consequence — healthy.** The focused decision and the persistent Undo status both say that the countdown is paused, show the retained seconds, state that Keep resumes the remainder, and state that Replace ends Undo. The disabled Undo no longer looks like a silent failure.
  4. **Keep current work — healthy.** Keep resumes only the retained remainder, prioritizes focus on the now-enabled Undo action, and leaves the new clipboard candidate discarded and unprocessed. A deterministic sub-second fake-clock case proves that the code does not grant a fresh ten seconds.
  5. **Wait past the old expiry, then Undo — healthy.** The isolated Electron fixture holds the decision for 10,500 ms, confirms that the same snapshot, decision, paused copy, and disabled Undo remain, chooses Keep, focuses the same enabled Undo control, and restores the exact source. Processing, clipboard-write, native-clipboard-stub, and loopback-trap counts remain zero.
  6. **Choose Replace or supersede the attempt — healthy.** Replace alone consumes the paused snapshot. Empty/error reads resume the exact remainder; stale reads, Undo, reset, source replacement, a newer Clear, or an old queued timer cannot revive or delete a different session. Returning from Settings gives the still-live manual decision priority over the stale Settings focus destination.
- Strengths: destructive ownership stays beside the decision; timing copy matches the actual timer; exact object/attempt checks close delayed-callback races; Keep returns users to the action they explicitly preserved; and the end-to-end native proof exercises elapsed wall time rather than relying only on source inspection.
- UX/accessibility risks: the two visible status surfaces add explanation density, and the fixed lower Undo surface covers part of non-decision content at 520×680. The primary decision actions remain visible and horizontally contained, but system text sizing and real VoiceOver speech/order/interruption can differ from the hidden Chromium focus proof. A near-expiry remainder is intentionally not reset, so users may have little time after Keep; focus therefore goes directly to Undo.
- Current-run screenshot, renderer-manifest/hash proof, and audit notes are in `docs/ux-evidence/2026-08-03-clear-undo-manual-read-pause/`. The screenshot was inspected at original 520×680 resolution. The production entry is 487,074 B / 143.81 kB gzip, restoring 2,926 B beneath the 490,000-byte working gate and 12,926 B beneath the 500,000-byte release ceiling.
- Evidence limits: the capture uses a hidden, private-profile, network-denied Electron harness with fictional in-memory source and clipboard text. It does not exercise the real system clipboard, provider billing/network behavior, signed/notarized installation, Finder/Gatekeeper/translocation, physical cold start, arbitrary content, real VoiceOver, or representative older/Intel hardware. The exposed credential was not used, sent, logged, or persisted and must be revoked before any future process-only live run.

follow-up result: passed for exact pause/resume ownership, truthful elapsed-time behavior, Keep/Replace consequences, stale-owner and queued-timer protection, 10.5-second native restoration, focused actionable Undo, zero implicit side effects, and meaningful renderer-entry reserve within the stated evidence boundary.

## 2026-08-03 Result stylesheet finite-recovery follow-up

- Numbered journey and health:
  1. **Stay in capture — healthy.** `ResultDisplay.css` is absent from `index.html` and the entry JavaScript, so first use and returning capture do not fetch the Result-private stylesheet. Shared live-region, recovery, and Saved Terms dependencies remain eager.
  2. **Complete analysis and prepare Result — healthy.** The Result module starts its stable non-inlined stylesheet request during evaluation, but `React.lazy` resolves only after that exact style promise. A user therefore sees the eager named loading surface until both presentation resources are ready; there is no deliberate flash of an unstyled result.
  3. **Primary CSS request fails — previously uncovered, now healthy.** A separate fixed native run leaves the JavaScript module available and gives only the exact DEV fixture-marked primary Result CSS request a real HTTP 503. The completed result/state stay mounted behind one named recovery `main`; one alert and enabled retry own focus. The failure is classified as a workspace load, not an analysis failure.
  4. **Retry once — healthy.** The retry advances both stable resources to `?workspace-attempt=1`, removes the failed link, parses the private `.reply-status-picker` rule, and retains exactly one loaded Result stylesheet link. It does not create random URLs or cycle after exhaustion.
  5. **Continue the task — healthy.** The deterministic sample result is unchanged, Result focus is restored, opening Settings then returns to the same result, and process/settings-write counters remain zero. `performance.timeOrigin`, URL, and the single navigation entry prove no reload; renderer and loopback traps remain untouched.
  6. **Exercise real production files — healthy within stated scope.** An isolated Electron window loads the actual production `index.html`, intercepts the ordinary `assets/ResultDisplay.css`, then imports the same physical Result JS/CSS under the bounded query. The rule computes to `display: grid`, one link remains, and no external request occurs.
- Strengths: JavaScript and CSS now form one user-visible Result gate; error copy and focus reuse the established eager recovery surface; the retry set is finite; actual parsed styling is tested instead of inferred from file presence; and the capture graph loses Result-private CSS without weakening the entry budget.
- UX/accessibility risks: the first Result presentation now waits for a second local resource, and a slow stylesheet can hold the loading surface for up to the bounded ten-second timeout before recovery. The timeout is fail-closed but may feel long. Programmatic focus and alert speech still require real VoiceOver acceptance. Remaining Settings/shared CSS cannot reuse this claim without its own ownership and runtime evidence.
- Build evidence: production emits `ResultDisplay.css` at 4,372 bytes and `ResultDisplay.js` at 95,417 bytes. Entry JavaScript is 487,185 bytes / 143.86 kB gzip, 2,815 bytes below the 490,000-byte working gate and 12,815 bytes below the release ceiling. `index.html` references only the 185,402-byte eager stylesheet. Fresh isolated unsigned arm64 and x64 release inspection confirms both stable Result assets in each architecture's ASAR.
- Current-run machine-readable proof and audit limits are in `docs/ux-evidence/2026-08-03-result-stylesheet-recovery/`. The failure surface and recovered ordinary Result use existing previously accepted visual components; this slice's new evidence is request, focus, DOM, parsed-rule, computed-style, navigation, and side-effect evidence rather than a claim of a visual redesign.
- Evidence limits: the fixed native journey uses fictional in-memory data and blocked networking. The actual-build gate executes `file:` assets outside an installed ASAR; dual-architecture archive inspection proves presence only. Neither substitutes for signed/notarized installation, Finder/Gatekeeper/translocation, real VoiceOver, arbitrary-content/provider quality, physical startup measurement, or representative older/Intel hardware. The exposed credential was not used, sent, logged, or persisted and must be revoked.

follow-up result: passed for Result-private CSS deferral, JavaScript-plus-style presentation gating, one bounded same-file/new-URL recovery, exact focus/state preservation, parsed production styling, zero repeated processing/settings writes/reload/external requests, stable packaged-asset presence, and the stated non-ASAR-runtime evidence boundary.

## 2026-08-03 Settings stylesheet finite-recovery follow-up

- Numbered journey and health:
  1. **Keep Settings out of first presentation — healthy.** The eager Settings CSS import is removed. The 48,467-byte private stylesheet now begins only on pointer, keyboard, or click intent, while Setup/loading/recovery, shared clipboard notices, live regions, contrast, and forced-colors rules remain eager.
  2. **Present one styled workspace — healthy.** The lazy Settings module owns a stable `SettingsPanel.css?url&no-inline` URL and exports its in-flight stylesheet promise. Every DEV and production primary/retry loader waits for that promise before resolving `React.lazy`, so Settings cannot deliberately mount between JavaScript and CSS readiness.
  3. **Fail the real CSS request — healthy.** The existing native stylesheet journey now gives the exact Settings primary CSS link one HTTP 503 while leaving its JavaScript module available. The eager recovery surface remains the only visible named `main`; its exact Retry owns a visible focus ring, and the completed Result remains mounted and unchanged behind it.
  4. **Retry once without poisoning — healthy.** The alternate stable JS URL reevaluates the module at `workspace-attempt=1`, and the CSS loader applies the same bounded attempt to the same physical stylesheet. Failed links are removed, stale callbacks cannot settle current ownership, and exactly one loaded/parsed Settings link remains.
  5. **Protect document and file boundaries — healthy after review.** Independent review found that the first loader draft shared pending ownership across documents and accepted another local directory under `file:`. Result and Settings loaders now use `WeakMap<Document, record>`, require exact normalized href reuse, and require the local CSS path to remain under the renderer base directory. Concurrent-document, distinct-href, timeout, supersede, stale-callback, and local-directory escape tests pass.
  6. **Recover at exact 200% — healthy.** The native journey uses 400×400 content bounds and zoom 2 for a 200×200 CSS viewport. Result Retry, recovered Result heading, Settings Retry, recovered Settings Return, and returned Result heading all own the exact expected focus; recovery rings are browser `:focus-visible`, rendered, and visible.
  7. **Do no hidden work — healthy.** Settings/credential/custom-prompt writes, credential deletion, provider tests, processing, screenshots, clipboard writes, and native clipboard stubs remain zero. URL, `performance.timeOrigin`, and the single navigation entry remain stable; blocked renderer and loopback-trap request counts are zero.
  8. **Honor user display preferences — healthy within scope.** The Reduced Motion runtime now opens the actual deferred Settings workspace, verifies one parsed Settings link, and observes the private spinner change from `settings-reset-spin` to `none` while transition duration collapses to the bounded global value. Existing normal/Increase Contrast/forced-colors Settings focus gates remain green.
- Product effect: eager CSS falls from 185,402 to 136,456 bytes, a 48,946-byte (26.4%) reduction. Production emits stable `SettingsPanel.css` at 48,467 bytes and `SettingsPanel.js` at 138,496 bytes. Entry JavaScript is 488,279 bytes, retaining 1,721 bytes beneath the working budget and 11,721 bytes beneath the release ceiling. This is a resource-ownership result, not a claim of measured physical startup time.
- Complete isolated `npm test`, full ESLint, the final native matrix after boundary repair, production `file:` runtime, Reduced Motion runtime, renderer build, ad-hoc x64/arm64 ZIP and DMG builds, release Info/ASAR asset checks, release-artifact checks, and the high-severity production dependency audit pass. The 18 synchronized implementation/test files are byte-identical between the isolated tree and working tree; focused working-tree gates pass.
- Current-run machine-readable proof and detailed limits are in `docs/ux-evidence/2026-08-03-settings-stylesheet-recovery/`. The failure surface reuses the established eager recovery presentation, so this slice records request, parsed-rule, computed-style, focus geometry, reflow, navigation, and side-effect evidence rather than claiming a visual redesign.
- Evidence limits: this does not replace real VoiceOver speech/Rotor/interruption, signed/notarized installed-app behavior, ASAR/translocation `file:` execution, physical cold-start measurement, arbitrary content/provider quality, or older/Intel hardware acceptance. A capture shortcut or App Quit layer spanning the exact Settings loading-to-failure transition remains a separate combined collision journey; existing component gates pass, but this evidence does not claim those timelines. The exposed credential was not used, sent, logged, or persisted and must be revoked before any live provider run.

follow-up result: passed for Settings-private CSS deferral, JavaScript-plus-style presentation gating, one finite same-file retry, document/path ownership hardening, no unstyled frame, exact 200% focus/state preservation, real Reduced Motion behavior, zero product writes/reload/external requests, packaged stable assets, and complete regression/release checks within the stated limits.

## 2026-08-03 Settings failure × App Quit × capture overlap follow-up

- Numbered journey and health:
  1. **Hold Settings presentation while App Quit arrives — healthy.** The exact primary Settings CSS response is held at a 400×400 native window with zoom 2. The eager loading `main` owns focus until App Quit becomes the only accessible top layer and moves focus to its safe action.
  2. **Fail Settings beneath the top layer — healthy.** The intentional CSS 503 swaps loading for the eager recovery surface without displacing App Quit. The recovery Retry stays connected and enabled but inert and unfocused behind the dialog; the preserved Result remains unchanged.
  3. **Cancel Quit and recover focus — previously clipped, now healthy.** A trusted native Escape dismisses App Quit. Product recovery code focuses the exact Retry and centers it inside the scrollport, leaving its 3 px outline plus 2 px offset fully visible at the exact 200×200 CSS viewport with no horizontal overflow.
  4. **Queue capture under App Quit — healthy.** The renderer observes the screenshot shortcut, but no screen capture or processing begins while Quit owns the foreground. Cancelling the second Quit transfers the single Settings-origin intent exactly once, producing one screenshot request and one process request.
  5. **Retry Settings without replay — healthy.** The same physical private stylesheet recovers through its bounded attempt URL, exactly one parsed Settings link remains, and Return owns focus. No queued capture, process, or write repeats.
  6. **Confirm Quit with a second capture waiting — healthy.** The second shortcut is observed but cannot begin capture. Confirmed Quit clears shadow and approved intent before releasing quit ownership, so final counts remain two shortcut events, one capture, and one process.
- Product repairs: Settings publishes mounted workspace ownership; Settings-origin deferred captures require both synchronous quit-request and quit-decision refs to be clear; process/ignore/approved-consumed paths settle the shadow request; explicit and automatic confirmed Quit clear pending capture state; and recovery focus reveals the real control rather than relying on fixture-side scrolling.
- Native proof records three quit requests, three decisions, one confirmation, one stylesheet arm/hold/manual release/failure, zero watchdog release, zero settings/credential/prompt/model/provider/clipboard writes, no reload, unchanged URL/time origin, one navigation entry, and zero external renderer requests.
- Current-run accepted and diagnostic screenshots, runtime facts, and detailed limits are documented in `docs/ux-evidence/2026-08-03-settings-overlap-recovery-audit/`. The rejected intermediate images are retained because they exposed first an offscreen focused Retry and then a still-clipped focus ring; `07-settings-retry-ring-fully-visible-after-fix.jpeg` is the accepted final recovery state.
- Current built-artifact inspection reports entry JavaScript at 489,217 bytes, leaving 783 bytes below the 490,000-byte working gate and 10,783 bytes below the release ceiling.
- Full final-tree `npm test`, complete ESLint, the focused collision/ownership/lazy-boundary gates, the static and full native Electron fixture matrices, a fresh production renderer build, and lazy-asset inspection pass.
- Evidence limits: the isolated fixture uses fictional in-memory state and blocked networking. Only Escape is asserted as native trusted input; quit requests, screenshot events, capture, and processing are deterministic fixture operations rather than a physical global shortcut, real screen selection, or real application termination. It does not exercise provider billing, user clipboard, normal settings persistence, signed/notarized installation, Finder/Gatekeeper/translocation, physical cold start, arbitrary content, older/Intel hardware, or real VoiceOver. Clipboard settlement and automatic confirmed-quit capture cleanup remain source-level guarded rather than separate timed native IPC journeys. The in-app browser could not attach, so final screenshots were captured from the actual Electron app through macOS Computer Use.
- Security boundary: inherited provider secrets were removed from every fixture process. The exposed credential was not used, transmitted, logged, or persisted and must be rotated before any future live-provider run.

follow-up result: passed for combined Settings failure/top-layer ownership, deferred capture intent, exact-once takeover after cancellation, confirmed-quit capture disposal, bounded stylesheet retry, natural fully visible recovery focus at 200% reflow, and zero hidden writes/reload/external requests within the stated limits.

## 2026-08-03 Saved Terms private-presentation follow-up

- Numbered journey and health:
  1. **Keep the current task light — healthy.** Setup/capture/Result startup requests neither Saved Terms JavaScript nor its private stylesheet. The existing trigger and fresh local count remain eager without mounting the library.
  2. **Open one complete library — healthy.** First open requests one stable JavaScript/stylesheet pair and waits for both before resolving `React.lazy`. Search receives focus; the local-only privacy explanation, classified/trust-labelled fictional record, scoped copy actions, removal, export, and import remain clear. Close/reopen makes no new resource request and preserves the import-preview session.
  3. **Fail the exact primary stylesheet — healthy.** A production Electron protocol handler returns 503 for the ordinary `SavedTermsLibrary.css`. Private controls never appear unstyled. The eager modal/recovery shell retains one alert, focused Retry, inert/assistively hidden background, and no private search/card/transfer controls.
  4. **Retry once without reloading — healthy.** The retry uses the same physical `SavedTermsLibrary.js` and `SavedTermsLibrary.css` under `?workspace-attempt=1`. Failed links are removed; one loaded, parsed attempt-1 link remains; a private search rule computes correctly; Search receives focus; URL and `performance.timeOrigin` stay unchanged.
  5. **Recover at exact 200% — healthy.** At a 400×400 content window and zoom 2, the drawer is the sole vertical scroll owner, has no horizontal overflow, and keeps alert edges, hint, Close, Retry, Return, privacy notice, and Search reachable with contained focus rings.
  6. **Protect user state — healthy.** Term mutations, import/export, clipboard reads/writes, model/provider calls, reloads, downloads, navigation, window opens, and external requests remain zero. Existing JavaScript-failure, empty-library, and delayed/rejected/malformed data-read journeys remain green.
- Resource ownership: the 12,770-byte private stylesheet now belongs to `SavedTermsLibrary.js`; eager CSS falls from 136,456 to 123,972 bytes. Trigger, backdrop, drawer frame/header/body, loading/failure state, recovery actions, shared operation error, and shell contrast/forced-color rules remain eager.
- Entry reserve: production build selection now keeps only the fictional safe sample source in the entry and leaves structured Result/OCR fixture payloads development-only. The final entry is 481,196 bytes / 142.21 kB gzip, 8,804 bytes below the 490,000-byte working gate and 18,804 bytes below the release ceiling. This is a graph/ownership result, not a physical startup-speed claim.
- Current-run evidence is in `docs/ux-evidence/2026-08-03-saved-terms-presentation-boundary/`: two ordinary actual-Electron captures plus exact-200% failure and recovered captures. All four were inspected at original resolution. The compact recovery captures intentionally show the active scrollport rather than compressing the entire drawer.
- Complete final-tree `npm test`, full ESLint, renderer/lazy-artifact builds, six-scenario Saved Terms production Electron runtime, static/native UI fixture matrices, generic same-file retry, screen-reader semantics, contrast, Reduced Motion, modal ownership, term transfer/trust/removal, and Result integrity checks pass.
- A clean temporary source snapshot with fresh lockfile dependencies completed unsigned x64/arm64 ZIP and DMG builds. New checksums, `check:release-info`, and `check:release-artifacts` pass; both ASARs contain the stable Saved Terms JavaScript/stylesheet pair and are byte-identical at `d71ec396310f8e82ea539a6b2451ba032bc32335061604e7614ac22c81a74eec`.
- Evidence limits: fixed fictional isolated state and programmatic fixture actions do not replace the user's persistent data, arbitrary imports, physical mouse/keyboard coverage, real VoiceOver, signed/notarized installed-ASAR behavior, Finder/Gatekeeper/translocation, physical cold start, representative older/Intel Macs, provider billing, or network-backed model quality. The exposed credential was not used, sent, logged, or persisted and must be rotated before any future live-provider run.

follow-up result: passed for Saved Terms private CSS deferral, eager recovery-shell preservation, one bounded same-file JavaScript-plus-stylesheet retry, exact 200% focus/reflow, stable resource identity, and zero hidden data/network effects within the stated limits.

## 2026-08-03 Safe-sample date-freshness follow-up

- Numbered journey and health:
  1. **Open the first grounded example — failed before, healthy now.** On 3 August 2026 the existing sample immediately labelled its 27 July deadline `已逾期 7 天`, making the product's own first example look abandoned. The source now anchors generation to one day after receipt of the fictional email and submission to two days after receipt, so the example remains meaningful without scheduled maintenance.
  2. **Preserve what the source actually says — healthy.** Both deadline records keep the exact relative `whenText`; `calendarDate`, `normalizedAt`, and timezone remain null. The preview does not use generation metadata to fabricate an absolute date or countdown.
  3. **Tell the user what happens first — healthy.** Source order, deadline order, headline, and action dependencies agree: generate the eVisa share code, prepare the passport scan, submit both materials, then reply. The completed Result retains four actions and two independently traceable deadline cards.
  4. **Do nothing without consent — healthy.** Loading the example still only populates the editor. The native completed-Result journey records zero processing, screenshot, provider-connection, credential-deletion, clipboard-write, quit-request, or confirmed-quit effects.
  5. **Keep the entry resilient — healthy.** Prompt templates remain in the CommonJS main-process constants used by provider code but leave the renderer ESM surface. Production entry falls from 481,196 to 478,148 bytes, restoring 11,852 bytes under the working gate without altering lazy workspace ownership.
- Strengths: the example can no longer age into a false emergency; relative semantics stay grounded instead of being guessed into calendar dates; action ordering is clearer; and the byte recovery creates room for the next user-facing slice.
- UX/accessibility risks: relative deadlines intentionally do not show a computed urgency badge because the sample has no authoritative send timestamp. Users must interpret the clearly named receipt anchor. Exact-date urgency remains separately covered by its deterministic gate.
- Current-run evidence: `docs/ux-evidence/2026-08-03-safe-sample-date-freshness/01-before-stale-overdue-result.png` captures the rejected stale 1280×720 state. The in-app Browser could not attach a fresh post-fix tab, so no same-browser after image is accepted. Post-fix acceptance comes from the real-Electron 400×400 window at zoom 2, which verifies the relative headline/summary/cards, source text, disclosures, focus/reflow, and absence of `已逾期`, plus the production build and full regression suite.
- Evidence limits: the deterministic preview is fictional and network-denied. It does not validate arbitrary relative-language normalization, provider quality, real email timestamps, signed/notarized installation, VoiceOver speech/Rotor behavior, physical startup, older/Intel Macs, or global shortcut/real termination behavior. The exposed credential was not used, sent, logged, or persisted and must be rotated before any future live-provider run.

follow-up result: passed for permanently time-stable safe-sample deadlines, exact source grounding, dependency-safe action order, zero automatic side effects, restored renderer-entry reserve, and the stated visual-evidence limitation.

## 2026-08-03 Command+Q safe exit follow-up

- Numbered journey and health:
  1. **Quit from a completed Result — previously unreliable, now healthy.** The macOS application menu owns an explicit `Command+Q` accelerator whose handler enters the same renderer-owned consequence guard as every other application-quit request. A real macOS `Command+Q` opens the guard over the preserved Result instead of silently doing nothing or bypassing review.
  2. **Understand what would be lost — healthy.** The first alertdialog names the current unsaved/session work and puts the non-destructive return action in focus. App-level quit remains the only foreground layer while lower Settings and capture decisions retain their state.
  3. **Cancel safely — healthy.** A real Escape closes only the quit decision, keeps the Electron process alive, and returns to the visually unchanged Result. The current request is settled; a later quit receives a new request rather than reviving the old one.
  4. **Review the system-clipboard consequence — healthy.** First confirmation remains fail closed when the main process reports a live opaque clipboard consequence. The second layer says that Slipstream will preserve the current system clipboard and that the user can return to check or replace it manually; only the exact current consequence can authorize the final settlement.
  5. **Exit after explicit confirmation — healthy within the stated boundary.** A real second confirmation closes the process. The isolated native fixture separately proves guarded native terminate dispatch, one-dialog listener-ready replay, mismatch rejection, cleanup completion, `before-quit`, and `will-quit` ordering.
- Race repair: an application-quit request arriving before React's passive subscription remains pending in a sender-bound registry. After the trusted main-frame renderer signals listener readiness, main replays that existing request without rotating or exposing its ID. Replaying an already-active request is a strict renderer no-op; the runtime proof waits for the replay and requires the original dialog node to remain connected, unique, and textually unchanged.
- Strengths: native macOS expectations now lead into the product's existing loss-prevention model; cancellation is useful; clipboard behavior is described before exit; early-start input is recoverable; repeated ready signals cannot flicker or duplicate the decision; and stale, mismatched, cross-sender, or already-settled decisions remain invalid.
- Evidence strength: physical macOS coverage proves the real `Command+Q` accelerator path, real Escape cancellation, the two confirmation layers, and final process exit. Deterministic fixture coverage proves the guarded native terminate and replay/cleanup contract, but its synthetic Meta+Q input does not activate the macOS menu accelerator; its menu/accelerator checks are static configuration evidence rather than causal input evidence.
- Fixture and build hardening: the contrast-preference runtime waits for reset-dialog focus settlement and explicitly reaches the safe cancel action, removing its next-frame race with the intentionally forced-color-adjusted danger control; three focused repetitions pass. A separate delayed-reply Undo sample exposed a load-sensitive one-shot selector after the Undo itself had succeeded. The native proof now requires the restored Result footer and correct reply consequence for three identical animation frames and records observed statuses on failure; three complete runtime matrices pass after repair, with the ten-second product Undo lifetime unchanged. After both macOS architecture archive sets exist, release staging deletes only the basename-guarded `mac` and `mac-arm64` unpacked directories before atomic publication. Pending paths are registered before each copy so partial ENOSPC writes are removed. A package-configuration fixture proves the unpacked directories are removed while an archive sentinel remains.
- Current-run evidence is in `docs/ux-evidence/2026-08-03-command-q-safe-exit/`. Four screenshots record the preserved Result, the first risk dialog, the Result after Escape, and the clipboard-consequence layer. The final candidate tree passes complete `npm test`, full ESLint, Swift OCR typecheck, renderer budget checks, strict dependency-tree validation, unsigned x64/arm64 DMG/ZIP builds, checksums, package/Info.plist/ASAR artifact checks, and full plus production-only audits at 0 vulnerabilities. Production entry JavaScript is 478,744 bytes, leaving 11,256 bytes of working-budget headroom.
- Release and evidence limits: the current artifacts are ad-hoc validation builds, not distributable release artifacts. The machine has no Developer ID Application identity or notarization credentials, GitHub CLI is unauthenticated, the worktree is not a clean tagged release commit, and the changelog remains `Unreleased`; distribution trust therefore fails and nothing was published. The journey does not replace real VoiceOver speech/Rotor/interruption acceptance, signed/notarized installed-app Gatekeeper/translocation testing, older/Intel Mac acceptance, arbitrary user state, or a network-backed model-quality run. The exposed credential was not used, sent, logged, or persisted and must be rotated before any future live-provider test.

follow-up result: passed for guarded macOS `Command+Q`, early-request listener-ready replay, strict active-replay no-op, useful Escape cancellation, explicit clipboard-consequence confirmation, final process exit, cleanup ordering, and zero-audit dependency locks within the stated physical/fixture split; public release remains blocked by external signing, notarization, repository-authentication, and clean-tag prerequisites.


## 2026-09-09 — 中文专业阅读定位

Same 520×680 native capture surface, dark theme: [before](../docs/images/01-reading-home-before.png) → [after](../docs/images/02-reading-home.png). The installed old surface was inspected through native accessibility and screenshot capture before editing; the matching before fixture uses the installed renderer.

- Reading copy identifies textbooks, papers and specialist articles. Screenshot reading is the first primary action and shows the actual shortcut.
- Removed the redundant three-step strip from the reading home and shortened text input so “开始阅读” is fully visible at the default window size.
- The card-box label is visible at ordinary widths; at a 200px CSS viewport its icon retains the full accessible name without overflowing its button.
- Pasted text opens the actual independent reading-card manager. Failed handoff retains the draft; arbitrary IPC exceptions are not shown to the reader.
- No-image cards exclude the image tab from keyboard navigation. Compact lookup panels keep the explicit save action visible.
- The first-use surface presents professional reading and basic translation with accurate destination and capability copy.
- Chinese README is the default, English is linked, and historical action contracts are retained as compatibility references.

Evidence: [concept explanation](../docs/images/03-reading-concept.png), [local card box](../docs/images/04-reading-card-box.png), [200% home](../docs/images/05-reading-home-200.png), [first use](../docs/images/06-reading-setup.png). Screenshot text and responses are authored fixtures in the actual UI. `check:reading-home` blocks external requests and uses temporary profile/card directories; it verifies the interaction rather than model accuracy.

Validation completed: all npm test component gates and ESLint pass after scoped reruns. The historical Saved Terms production harness now reaches its workspace through an explicit temporary session recovery; the reading-home harness covers the default native card-box path. Final signed preview installed at the stable user Applications path; its ASAR sources and production entry match the verified workspace. Native UI confirmed the new home, sample text before submission, and source transfer after starting reading.
