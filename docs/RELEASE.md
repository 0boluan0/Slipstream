# macOS release checklist

## Source gate

- Working tree and dependency lock are intentional.
- Version and changelog are updated.
- `npm ci`, `npm run check:runtime-dependencies`, `npm run check:dev-dependency-security`, `npm run check:ocr-environment`, `npm run check:ocr-runtime`, `npm run check:file-provider-conflicts`, `npm test`, lint, renderer build, OCR language/review/static checks, and package-config validation pass.
- `check:startup-benchmark-mode` passes inside `npm test`, proving the retired production-integrated timer and its flag/environment activation path remain absent. Do not publish the former seven-sample numbers.
- `check:renderer-startup-harness` passes inside `npm test`. `npm run measure:renderer-startup` is a separately identified, non-packaged full-contract harness; `measure:renderer-startup:smoke` validates plumbing only and must never be cited as performance evidence.
- The reviewed 2026-07-31 full-contract run recorded 40 samples after five warmups per scenario. First-use semantic DOM receipt was p50 240.150 ms / p95 241.641 ms / max 241.963 ms; returning capture was 240.546 / 241.956 / 244.616 ms. Spawn-to-`did-finish-load` receipt p95 was 201.751 ms and 200.704 ms. Unexpected adapter calls were zero. FCP was unavailable in all 40 samples of each scenario.
- This baseline is hidden, same-machine, warm-filesystem, fresh-process/profile, direct-exec and ad-hoc-signed evidence. It is not compositor-visible, Finder, Gatekeeper, quarantine, translocation, first-install, signed/notarized installed-app, or release-threshold evidence. Do not define an absolute threshold until a visible signed-app pass and representative older/Intel hardware acceptance exist.
- `check:startup-first-interaction` verifies that formal windows are not shown before `did-finish-load`, production `loadFile` failure exposes only a privacy-safe native Retry/Quit decision, and fresh setup/returning capture assign focus to the recommended CTA or source textarea only after yielding to modal, recovery, existing-focus, and foreground ownership.
- `check:capture-ingress-readiness` verifies that an early shortcut cannot show, focus, or send directly to an unsubscribed renderer. The main-owned queue is sender-bound, memory-only, latest-wins, and exactly-once after a trusted listener-ready handshake; delivery failure remains retryable, navigation/crash invalidates readiness, a replacement renderer cannot inherit captured text, and passive clipboard monitoring does not foreground the app.
- `check:startup-evidence-harness` keeps the separate screenshot helper out of formal artifacts and statically enforces its private profiles, fixed environment, hidden window, network/capability denials, fresh-output policy, focus targets, and DPR geometry contract. Its optional runtime gate and publishing command are evidence tooling, not release-performance gates.
- When claiming live DeepSeek compatibility, run `npm run check:deepseek-live` with `DEEPSEEK_API_KEY` supplied only to that process. This network gate is deliberately outside `npm test`: it sends only the fixed fictional source with built-in `action-brief.prompt.v2`, exercises the production `processText` call, parser, and `ActionBriefV1` validator, and then requires the additional probe-specific grounding assertions. The candidate/schema remain v1. It does not cover renderer UI, IPC, `safeStorage`, or the packaged application and is not an arbitrary-input quality guarantee. Never save the credential in the repository, fixture, shell command, log, or release artifact.
- `check:model-quality-benchmark` passes inside `npm test`. It validates all ten fictional administrative cases, schema-valid golden briefs, bilingual action semantics, exact evidence, and deliberate mutations for missing, invented, inverted, misdated, prohibited, conditional, and wrong-reply outputs. This is offline scorer/corpus evidence, not a live model result.
- Before making a broader DeepSeek quality claim, run the complete `npm run check:deepseek-quality-live` corpus with a newly rotated `DEEPSEEK_API_KEY` supplied only to that process. A subset must remain labeled partial. The runner uses production `processText`, requires substantially Chinese translation output, and reports only metadata, scores, and bounded failure codes; source text, raw model output, and credentials must never enter its output. This still does not prove arbitrary inputs, renderer IPC, Keychain storage, or packaged-app interaction.
- The real-process Electron UI fixture gate passes with a new empty profile inside a unique per-run temporary root. Every child process receives that root explicitly, reports that Electron `userData` and `sessionData` resolve inside it, and must remove its own fixture directory before the root can be closed. The gate neither snapshots nor depends on the live normal profile, so it remains safe when another Slipstream instance is running. Fixture startup remains development-only, loopback-only, and fail-closed; its launcher and preload are not included as production application entry points.
- The production renderer and main process carry no development fixture implementation. Build-time alias checks select only the preload bridge for renderer builds; formal `main.js` rejects packaged fixture activation before loading the excluded development proof module. Package configuration admits only `dist/renderer`, excludes fixture modules and source maps, rejects positive `scripts` globs and sibling `dist` trees, and allows exactly `VisionOCR.swift` plus `ocr-swift-runner.sh` as OCR source resources. A clean fresh-install unsigned x64/arm64 build passes `check-release-info` against both actual ZIP/ASAR payloads, including exact final runtime-script allowlists and fixture-marker scans.
- The production preload exposes no clipboard-clear channel. Its VM contract proves the retired conditional and unconditional clear names are rejected, while application clipboard writes remain limited to the single owned write path.
- `check:settings-transition-dialog` passes inside `npm test`. Unsaved connection drafts and active provider validation share one `aria-modal` decision contract and resolve `draft → connection → perform`; the layer restores exact prior `inert`/`aria-hidden` values, owns capture-phase Escape/Tab and `focusin`, yields to AppQuit, blocks same-frame duplicate submission, focuses busy/error/completed states, and restores the exact trigger after the settled two-frame render. Errors participate in `aria-describedby`; focused status content routes Tab/Shift+Tab to the first/last action; state rings use opaque semantic colors; spinner motion stops under Reduced Motion. Actions are at least 44px high and use one column at 620px or below.
- `check:settings-accessibility` passes inside `npm test`. Settings exposes one labelled `main`/`h1`, its basic-translation explanation begins collapsed, and each custom ARIA radio group has one roving tab stop plus wrapping Arrow keys and Home/End navigation that focuses and activates the destination.
- `check:screen-reader-semantics` passes as a targeted gate. Its static contract maps ActionBrief `en`/`zh`/`mixed`/`unknown` to valid `en`/`zh-CN`/`mul`/`und`, rejects raw enum values in HTML language attributes, requires segmented bilingual names, preserves the deadline action cue, verifies first-use and capture→processing focus ownership, requires one empty-first bounded processing live region with silent elapsed seconds, and requires the Result conclusion, evidence workspace, and footer actions to share one heading-labelled non-nested `main`. Its isolated hidden real-Electron run traverses Result, reply, Saved Terms, setup, capture handoff, and processing with Chromium's Accessibility Tree, a sandboxed mode-`0700` temporary profile/session, stubbed clipboard writes, denied permissions, stripped secret-bearing environment, and zero network-trap requests. Both the default no-write mode and reviewed evidence-output mode pass; the manually inspected captures are `docs/ux-evidence/2026-07-30-result-processing-accessibility/01-result-named-main.png` and `02-processing-status.png`. This is DOM/Chromium AX evidence, not real VoiceOver pronunciation, speech order, interruption, Rotor, or announcement-queue acceptance; enabling VoiceOver requires explicit approval because it changes a global macOS accessibility setting.
- The accessibility slice did not use, enter, persist, log, or place the supplied API credential in fixture state or evidence. Any later network-backed provider-quality run must use a newly rotated credential supplied only to that process; this slice makes no live-provider claim.
- The final accessibility tree was copied into a clean temporary directory while preserving the workspace-root structure, then received a fresh lockfile installation with `npm ci`. That copy passes complete `npm test`, full lint, and `npm run build:renderer`; the build reports only the existing non-fatal >500 kB chunk advisory.
- `check:reduced-motion` passes inside `npm test`. Its static contract prohibits renderer code from forcing literal smooth scrolling outside the shared preference helper. Its isolated real-Electron probe uses Chromium media emulation to prove ordinary `smooth` and Reduced Motion `auto` behavior on the production deadline path, plus the active universal CSS motion suppression, sandboxed temporary profile, loopback-only renderer, stubbed clipboard, stripped secret-bearing environment, and zero network-trap requests. Evidence and Settings-recovery call sites share the same policy but are not all activated by this runtime probe.
- `check:contrast-preferences` passes inside `npm test`. The static contract requires ordinary light/dark filled-control text contrast of at least 4.5:1, light/dark enhanced text and boundary thresholds, the success/focus/fill/scrim token set, blur-free overlays, three-pixel focus, forced system colors, dynamic evidence remapping, and no broad forced-color opt-out or semantic background paired with hard-coded white. Its isolated real-Electron probe explicitly sends both `prefers-contrast` and `forced-colors` features for normal, more, and forced states, then traverses Result, Settings, and reset with native Tab modality. All nine focus targets must remain visible with 3px outlines; enhanced overlays remove blur; boundaries, text, and horizontal containment pass; the mode-`0700` profile is removed; and external/trap request counts remain zero. Optional screenshot output is restricted to an existing reviewed `docs/ux-evidence` directory.
- The hidden real Electron 43 fixture passes the Settings transition scenario at 520×680 and a dedicated 400×400/200% scenario. The latter verifies eight draft/error/completed focus paths, full four-pixel ring geometry, and zero horizontal dialog overflow. Both continue proving per-run isolated `userData`, demo preload, blocked external network access, sanitized secrets, and cleanup without reading or snapshotting the live normal profile. This is automated native interaction evidence, not visible installed-app acceptance.
- A separate current-run visible real Electron development-fixture audit accepts the same transition path at exactly 520×680 and after a native resize to 400×400. Eleven inspected before/after captures prove complete draft, running, stop-error, late-completion, minimum-window, and reverse-focus states. Warning-action contrast is 4.82:1 in light and 6.38:1 in dark after repair; the larger copy, 44px actions, and focused controls remain contained. This is visible native development evidence, not signed/notarized installed-app acceptance or VoiceOver certification.
- A second current-run visible audit exercises the bounded Settings transition at exactly 400×400 and 200% Chromium scaling. Thirteen accepted captures show the 100% reference, the pre-fix offscreen-focus failures, and the repaired safe, destructive, stop-error, retry, and completion targets with complete rings. Later current-run passes remove entry/header/capture clipping and accept completed results, Saved Terms, the full Settings shell, and simultaneous recovery/capture/shortcut/monitoring status surfaces. Evidence: `docs/ux-evidence/2026-07-28-settings-transition-text-scale/`, `docs/ux-evidence/2026-07-28-app-shell-text-scale/`, `docs/ux-evidence/2026-07-28-result-workspace-text-scale/`, `docs/ux-evidence/2026-07-30-settings-shell-text-scale/`, and `docs/ux-evidence/2026-07-30-stacked-status-text-scale/`.
- The current first-use/capture 200% pass uses a real visible Electron development fixture plus a dedicated native gate at exact 400×400 bounds and zoom factor 2. It accepts first-use choices, processing/privacy copy, every global header action, empty capture, the fictional safe sample, explicit submission readiness, and keyboard focus with zero horizontal loss and complete rendered rings. The fixture remains isolated from normal user data, real clipboard access, credentials, external networking, and provider requests.
- The current completed-result/Saved-Terms 200% pass uses the same exact native bounds and scaling. Visible before/after evidence is paired with an ordered trusted-input path: 19 real clicks exercise pane/evidence round trips, four self-reported completion check cycles, and Saved Terms open/preview/cancel; two trusted Tabs and Escape then prove keyboard containment, and two final real clicks prove deadline open/close with natural pointer focus. An out-of-sequence probe is rejected. Result-order, processing, footer, and remaining drawer controls are separately focused and measured for visibility, reachability, and complete keyboard rings. Together the 21-click/three-key path accepts complete source evidence, modal isolation, focus restoration, stable disclosure identity, and zero horizontal loss. Eleven side-effect counters and the blocked renderer-network trap remain at zero.
- The Guided Reply 200% gate uses a separate fixed-result run at exact 400×400 native bounds and zoom factor 2, yielding a measured 200×200 CSS viewport. It must keep the drawer as one vertical scroll surface, place the footer statically after content, expose no page/drawer/footer horizontal overflow, keep every measured target at least 32×32, and preserve the two independent progress-conflict and placeholder blockers. Its exact trusted sequence is six pointer actions, two fixed fixture-owned edits, nine Tabs, and one Escape, plus one rejected out-of-sequence edit. Copy is allowed only after both blockers settle; the one in-memory clipboard write must become visibly outdated after an edit; Close and Escape must restore the exact Result trigger; reopen must retain the edited state; all nine focus stops and rings must stay inside the modal; and processing, screenshot, provider, credential, quit, real-clipboard, and external-network side effects must remain zero. Visible in-app-browser captures stop at 240×200 and must not be cited as the exact 200×200 source.
- The same isolated native gate passes three fixed recovery journeys: all supported runtime-degradation warnings remain visible without collapsing capture; corrupt settings stay blocked until the two-step archive-and-fresh confirmation completes, then expose only a safe basename and focus the success notice; and an unreachable provider fails once, succeeds on the explicit second test, preserves the saved configuration, and focuses each new result. Inputs are enum-whitelisted, request counts are exact, and the blocked network trap remains at zero. Hidden interaction evidence still does not replace visible screenshot or VoiceOver acceptance.
- The hidden real Electron fixture also passes the delayed reply-write scenario: copy enters pending immediately; editing, closing, late settlement, Undo, and exact-versus-edited reconciliation produce `outdated`, `retained`, or `copied` without a second write. A separate application-transaction scenario covers result, action-list, source-link, all saved-term scopes, support-diagnostic, and recovery-command routes under the same global lock, then proves cross-view retained consequence state, no automatic clipboard mutation, an explicit later replacement write, and exact manual-overwrite acknowledgement. Both scenarios use an in-memory clipboard and must continue proving zero real clipboard, external-network, normal-profile user-data, or secret access.
- `check:full-data-reset` verifies the preserve-only two-phase reset protocol: a cryptographically random sender-bound ticket expires after 30 seconds, is one-time and replay-safe, blocks conflicting clipboard writes while live, revalidates the current opaque clipboard consequence before persistent deletion, aborts on renderer loss, and lets a session-cleared partial retry continue without repeating the renderer purge or promising that session data still exists.
- Review development-tool advisories separately; never use a forced audit fix that introduces breaking build-tool upgrades into a release. The lockfile now resolves range-compatible `brace-expansion@1.1.18` for `minimatch@3`, `2.1.4` for `minimatch@5`/`9`, and `5.0.9` for `minimatch@10`, plus `fast-uri@3.1.5` and root development `undici@7.29.0`. `check:dev-dependency-security` verifies exact lock paths and integrity, strict `npm ls` validity, legacy APIs, ordinary brace behavior, and bounded expansion. Both full `npm audit` and `npm audit --omit=dev` must report 0 vulnerabilities before release.
- Packaged application artifacts must include no screenshots, source emails, API keys, or local audit evidence; repository documentation may retain explicitly reviewed fictional UX evidence outside the packaged app.
- Saved-term v2 backups contain only terms, explanations, strict type enums, and the provenance labels present at export time; evidence excerpts, IDs, timestamps, settings, and credentials remain excluded. V2 `original`, `inference`, and `official` labels therefore import as `unknown`, with the exact downgrade count shown before confirmation, and lower-trust imports cannot replace stronger local records. V1 imports fail closed to `other / unknown`, and import previews remain non-mutating until explicit confirmation.
- Custom-provider location is derived from the validated endpoint instead of the provider name: accepted HTTP loopback is shown as a compatible service on this Mac, accepted public HTTPS is online, and ambiguous/invalid values block analysis. The same bounded location snapshot follows the active task, result details, interruption recovery, cancellation/quit risk, tray, and support diagnostics without retaining the URL. Unsafe legacy/runtime Ollama values are cleared or fail closed, and no unknown Ollama state may be labelled “this Mac.”
- Formal custom and Ollama analysis revalidate immediately before submission and reject every redirect before a second hop. Public custom HTTPS requests use a safety-checked pinned DNS result; Ollama and “service on this Mac” custom analysis accept only HTTP loopback. Custom non-2xx bodies are discarded before parsing; successful responses require identity encoding and a 4 MiB declared/streamed byte cap. Loopback UI copy explicitly warns that the compatible service can still network, forward, retain, or charge under its own configuration.
- Support diagnostics remain preview-before-copy, never auto-send, and exclude credentials, endpoints, source text, term/evidence content, clipboard content, and arbitrary model markers. Interrupted-task recovery stores only allowlisted provider/location values and names the historical destination without retaining connection fields.
- Clipboard monitoring remains off by default; enabling requires destination-specific confirmation, the active destination and off action stay visible on the main task surface and macOS menu, and a new copy cannot silently replace a task, draft, verification, or completed result. Only the latest waiting copy remains in renderer memory, the native menu receives no text, and processing it requires an explicit user action after active work settles. Closing discards the waiting copy, affects future copies without misrepresenting an active task as cancelled, and failed enable/disable writes preserve and explain the last confirmed state without leaving a second generic retry path.
- Option+C and the screenshot shortcut cannot silently supersede active analysis or official verification. Shortcut text waits for an explicit process-or-ignore action; screenshot intent waits for a stop-or-continue choice; frame selection starts only after the captured analysis, official-verification, provider-test, and screenshot tasks actually settle or complete naturally. The main cancellation handler uses a bounded settlement wait, returns false while any captured task still runs, leaves official lookup cancellation retryable instead of stuck, and the screenshot handler rejects busy work without aborting it.
- Option+C's empty-surface 400 ms handoff is source-bound and one-shot. Editing, edit-then-undo, manual submission, replacement, clear, paste, screenshot, foreground ownership, hiding, or active processing revokes the delayed capture. An edit leaves the latest draft visible, announces that it was not sent, and requires Generate or Command+Enter; the stale captured original cannot be submitted or queued later.
- Option+C and the screenshot shortcut cannot run invisibly behind Settings. Ordinary settings returns to the task before capture; unsaved connection input requires an explicit keep-or-discard decision; provider validation requires confirmed cancellation; continuing settings leaves capture visibly pending; guided setup does not leave its gate to start capture; empty clipboard errors stay in Settings; and the main screenshot handler also rejects provider-validation races.
- Option+C and the screenshot shortcut cannot bypass another foreground decision. Unsaved source correction and drafts, same-window recovery, the saved-term library, settings confirmation, app quit confirmation, and the clear-undo window retain focus and state. Clipboard actions name the correction, draft, or undo consequence and return safe focus to the owned control; modal decisions keep processing disabled. Screenshot intent contains no source or image data. Neither request auto-starts after listener or state changes, and stale undo-specific actions disappear when the undo window expires.
- Option+C and the screenshot shortcut expose their actual macOS registration result. The fresh screenshot default is Option+Shift+S. Existing bare F1–F24 settings remain valid but must carry the Apple-keyboard Fn/Globe warning and a safe recommended-combination action. Startup failures remain visible on the task surface and route to the affected field; Settings distinguishes enabled, caution, unavailable, and checking states; conflict or invalid-accelerator changes restore the prior registration before reporting failure; interface capture buttons remain usable; and the preview-before-copy support summary stays synchronized without adding source or clipboard content.
- Shortcut editing records a direct key combination instead of requiring accelerator syntax. Modifier-only, unsupported, unsafe unmodified, duplicate, invalid, and external-conflict outcomes stay distinct; macOS-facing labels match the visible keys; Escape cancels; Tab cancels and advances focus; and every failed change leaves the previous working shortcut intact.
- A failed, incomplete, malformed, schema-invalid, or migration-failed settings startup remains blocked and cannot fall through to first-run setup, activate normal persistent runtime, or persist defaults. Validation and migration occur against a private staged copy; the original is replaced only after success. Retry is safe. Archive-and-start-fresh requires explicit confirmation, creates a local mode-`0600` recovery archive without automatic re-import, restores the original if fresh creation fails, and full data reset removes recognized archives.
- `check:storage-startup` is included in `npm test` and must pass. It uses the lockfile-pinned real `electron-store@8.2.0` and temporary filesystem profiles to cover malformed JSON, schema and migration failure, original-byte preservation, private permissions, retry, recovery rollback, fresh-store startup, and full-reset archive cleanup. Electron and `safeStorage` are stubbed, so this is storage/startup-logic evidence rather than native Electron, Keychain, packaged-app, or visible-UI acceptance.
- Clear undo snapshots remain memory-only, expire after ten seconds, disappear on new input, and never restore official-verification approval. An edited guided reply must restore its exact text, real-world status, progress exception, and cursor selection.
- Same-window interruption recovery remains bounded to temporary session storage, expires after 30 minutes, clears on restore/discard/explicit clear/window-session end, never becomes history or sync data, excludes settings drafts, secrets, clipboard authority, copy receipts, request/task/version identities, and full reply-model content, and never resumes tasks or official-lookup approvals automatically. An unsent reply may restore its bounded text, status, and selection, but any stale-checklist exception must be confirmed again and the restored reply must begin as not copied.
- The installed macOS application menu retains standard app, editing, and window roles but excludes Reload, Force Reload, and Toggle Developer Tools, so native commands cannot bypass task and Settings guards.
- Hidden analysis and official-verification tasks expose only generic menu-bar status and completion notifications; source text and analysis content never appear in notifications.
- The post-submit processing surface contains no raw source excerpt. It may expose only the source type and retained character count, must keep the destination disclosure visible, and must restore the complete source after confirmed first-analysis cancellation.
- Every app-provided renderer clipboard write must pass through the single App-owned operation and notice coordinator; only `App.jsx` may invoke the production clipboard-write channel. Task outputs, source links, saved-term scopes, support diagnostics, and recovery commands must acquire synchronous ownership before IPC, block competing writes, settle only their own request, preserve a still-live prior consequence after replacement failure, and keep notice state free of copied plaintext. No renderer or preload path may read, clear, or overwrite the system clipboard automatically. Pending operations and opaque consequence state protect quit and full reset across view changes; both flows offer preserve-only confirmation and fail closed on expired, malformed, mismatched, or stale settlement. A reply copy additionally settles against its originating task generation and non-plaintext content identity: same-version success is copied, edit-before-settlement is outdated, and completion after task exit is retained. Undo does not write again and reconciles exact restoration to copied or edited restoration to outdated. Dismissal hides presentation without discarding settlement or consequence state; TTL expiry removes it. Guided replies remain uncopyable while generated placeholders are unresolved. Official-source open actions report pending, confirmed handoff, and retryable failure separately, and a browser handoff must never be worded as semantic verification. Source open/copy controls remain at least 32px high, keep a visible keyboard focus ring, wrap without compressing source metadata, and align with the content edge on narrow cards.
- Deadline urgency uses only a validated source `calendarDate` or a genuine timezone-qualified `normalizedAt`; it never parses natural-language display text or fabricates an end-of-day instant. The exact source date remains visible beside today/tomorrow/remaining/overdue text, invalid dates fail closed, and an open result refreshes after local midnight.
- When several grounded deadlines exist, the result header must prefer the earliest safely comparable date within the strongest user-action priority group: mandatory user action, other user action, linked non-user action, then unlinked date. An earlier institution-only date cannot displace a mandatory user deadline. Incomparable candidates preserve source order, the total deadline count stays visible and actionable at narrow widths, and opening the summary exposes every date's urgency, condition, provenance, and source evidence.
- Action completion remains explicitly self-reported and reversible. The `x / y` summary must not imply external verification; completed actions retain their source evidence; copied checklists preserve pending/completed labels; Settings navigation, clear undo, and bounded same-window interruption recovery keep the same record; a genuinely new result resets it; and guided replies still require a separate real-world status choice.
- When every generated action is marked complete, the result must expose a distinct self-reported completion state without weakening source evidence or claiming external verification. A completed reply action changes the requirement indicator and repeat-reply control without deleting the source requirement; the analysis disclosure says `处理完成`; and `完成并返回` becomes the primary exit with the existing ten-second memory-only undo. Unmarking any action must immediately restore active-task labels and the ordinary clear action. Undo must restore the full result and completion record without restoring official-lookup approval.
- A guided reply that claims completion must compare its status with mandatory user actions before the reply, excluding the reply action itself, optional actions, and institution actions. A mismatch keeps copy disabled until the user chooses the in-progress status or explicitly confirms the checklist is stale; that confirmation is ephemeral, never marks actions complete, and resets on status change or drawer reopen. A consistent pre-reply record must not gain this extra gate, and the sticky copy footer must keep every remaining blocker visible.
- Completed actions may hide their expanded evidence only behind a named, keyboard-visible disclosure that reports the evidence count. The next unfinished action must remain readable, expanding one completed item must not expand every item, and removing a completion mark must immediately restore its evidence.
- Action steps that depend on another generated or prepared item must carry valid direct prerequisite IDs and render in a stable executable order. Missing, self, duplicate, or cyclic references cannot enter a valid brief. The result and copied checklist must name the prerequisite step numbers; marking a dependent step complete while a prerequisite is not marked must warn for review without silently changing the user's self-reported record.
- First-run full analysis remains disabled until the current saved provider and model complete metadata discovery and the fixed fictional `action-brief.prompt.v2` probe through the production model call, parser, and `ActionBriefV1` validator, then satisfy the extra checks for grounded translation, deadline, material, actions, terms, and process context. Valid JSON alone, cancelled/stale output, or a structurally plausible but probe-incomplete brief cannot unlock activation. The candidate and schema remain v1-compatible, and stored prompt v1 remains accepted.
- Runtime provider failures preserve the task and last valid result, expose reason-specific credential/model/connection/transient recovery, focus the exact settings control, and keep the retained-task notice visible. Replacing a credential revokes full-analysis readiness until the fixed fictional structured check passes again; returning restores the source for retry and the completed result must report the provider's true local/online destination.
- First-use free translation returns only a translation-only brief in production and deterministic demos: action, material, deadline, reply, evidence, and verification fields stay empty and the boundary is visible. Opening full-analysis setup preserves the current task; leaving incomplete setup returns to that translation, prevents processing, and offers focused continuation. After successful enablement, rerunning the preserved source uses the selected full-analysis backend.
- Free-translation fallback keeps Google automatic source detection but gives MyMemory a concrete inferred English/Chinese language pair; deterministic fixed-text checks with injected transport must succeed in both directions when Google is forced to fail. Those checks prove fallback control flow, not current Google/MyMemory network compatibility; any live claim requires a separate opt-in network gate. The submit and in-progress surfaces must say that the complete source goes to Google first and to MyMemory only after an unusable Google response. Failure recovery names both services, preserves the source and prior valid result, keeps retry primary, and never routes to a provider test that does not exist. Choosing another processing method opens and focuses the local-versus-online analysis decision; returning leaves the original retryable, and a transient fixture must complete successfully on retry.
- Moving from a configured online model to free translation names the saved credential and requires an explicit keep-or-delete choice. Keeping never transfers the credential to Google/MyMemory; deletion has an independent confirmation, clears every connection value for the selected provider, and preserves the task. A partial write leaves setup unconfigured with a targeted retry, and direct API-key deletion cannot be triggered by one click or an Escape intended only to cancel confirmation.
- Explicit draft discard must reset the visible API Key, endpoint, and model editors as well as the parent dirty state. The hidden native save-failure scenario confirms that a failed requested switch keeps the saved provider authoritative, keeps its validation action coherent, and exposes the retry transaction. When a custom-origin save revokes its previous stored key, a non-empty replacement key still visible in the editor must remain dirty and keep return, provider-switch, and capture transitions behind the draft guard.
- A failed replacement source must remain separate from the older valid result. An untouched retry preserves its capture and truncation metadata; a reviewed correction that survives returning to the older result must be retried as manual text without stale OCR metadata, while an empty correction must reopen review rather than silently submitting the original source.
- During provider validation, API Key, endpoint, model, and full-data reset controls stay locked until the owned task actually settles. A renderer run token prevents an older validation result from replacing a newer one; completion after a stop error replaces the stale warning with the retained completed result, and `查看验证结果` focuses that real result.

## Signing and notarization

- Install a valid `Developer ID Application` identity.
- Provide App Store Connect API credentials, `APPLE_ID` plus an app-specific password and Team ID, or a validated `notarytool` Keychain profile.
- Run `npm run release:signed`.
- Never replace a failed Developer ID signature with an ad-hoc signature.
- The final DMGs are Developer ID signed and verified, submitted to Apple, stapled and validated, then receive freshly generated blockmaps before publication.
- Both release commands stage the complete build under the system temporary directory, outside synced/File Provider folders, and publish artifacts only after both architectures finish. As soon as each architecture's archives exist, the builder removes only its basename-guarded reproducible unpacked app directory to bound peak disk use; every `.pending` destination is registered for cleanup before its copy starts, so an interrupted or ENOSPC copy cannot leave a partial candidate behind.
- Ad-hoc builds use a local-only library-validation exception so Electron can launch without a Team ID. The signed distribution gate rejects that exception.

## Artifact gate

- arm64 and x64 DMG/ZIP files exist and match `SHA256SUMS.txt`.
- Packaging inputs and the release directory must be free of lexical File Provider conflict-copy names. `afterPack` scans the complete app tree and ASAR; release inspection independently scans raw ZIP central-directory entries, each extracted ZIP/app/ASAR, each read-only mounted DMG/app/ASAR, and the final release directory. Architectures are extracted and removed one at a time, and the gate finishes by recomputing artifact hashes and rescanning the release directory.
- DMG inspection detaches every mounted image before continuing. `scripts/check-release-artifacts.js` allows only four detach attempts, 250 ms apart, and accepts a disappeared mount point as already detached; a mount that remains after the bounded retry still fails the gate.
- Both apps have hardened runtime and a Developer ID authority/team identifier.
- The app and DMG both contain valid stapled notarization tickets.
- Gatekeeper accepts both architectures.
- Packaged OCR acceptance statically verifies the exact arm64 or x86_64 Mach-O slice in both packages. On the host architecture only, `check-release-info` must execute the reviewed fixed fictional image and require its exact text, source hash, ordered 4-block contract, and confidence floor, then separately execute the missing-image negative case. Do not describe this as runtime execution on both target architectures.

## Latest signed release evidence

- On 2026-08-15, the exact v1.0.0 source candidate completed a fresh dual-architecture signed build outside the iCloud/File Provider workspace. Apple accepted the x64 and arm64 application submissions and both final DMG submissions. The final DMGs passed code-signature verification, ticket stapling and validation, Gatekeeper distribution checks, checksum generation, complete archive/content inspection, and the final release-manifest recheck. These exact DMGs are the public v1.0.0 release candidates.

## Historical unsigned validation evidence

- On 2026-08-04, the exact current tree completed a fresh `npm run release:unsigned` with exit code 0 in a private non-synced validation tree. Fresh `local-adhoc` arm64/x64 DMG and ZIP artifacts passed checksums, package configuration, production audit at 0 vulnerabilities, complete `npm test`, full ESLint, history non-retention, raw ZIP/app/ASAR and read-only DMG inspection, code-signature/build-identity/entitlement checks, and the final manifest recheck. Both OCR binaries had the exact target slice; the host-arm64 packaged runner recognized the fixed fictional image as 4 blocks at 1.000 confidence, and missing-image handling passed independently. The byte-identical artifacts and `SHA256SUMS.txt` were copied to `slipstream/release`. This is a verified local ad-hoc candidate, not a public release: no Developer ID signature, notarization, Gatekeeper distribution acceptance, clean exact-version tag, or GitHub publication is claimed.
- On 2026-08-04, the build-identity tree passed focused identity/support/package checks, complete `npm test`, full lint, renderer build, production audit at 0 vulnerabilities, and `npm run release:unsigned` in an isolated final tree. Fresh arm64/x64 DMG and ZIP candidates matched checksums, mounted or unpacked successfully, launched the host-architecture packaged OCR runner through its expected missing-image error path, and declared the same `local-adhoc` identity; artifact inspection proved that identity matched each actual ad-hoc signature. This did not prove positive OCR recognition or target-architecture execution for both packages. `check:signing`, `check:notarization-env`, and `check:distribution` then failed as designed because the machine has no Developer ID identity, notarization credentials, or signed artifacts. These candidates are local smoke evidence, not a public release.
- Later on 2026-08-04, the current cold-start capture-ingress tree passed focused behavior/IPC/startup gates, complete `npm test`, full lint, renderer build, package-config validation, and full plus production audits at 0 vulnerabilities in the private validation tree. Entry JavaScript was 482,501 bytes with 7,499 bytes of working-budget headroom. No fresh DMG/ZIP was built because available disk space was below 1 GiB, so the earlier artifact evidence does not certify this later code slice.
- On 2026-08-03, the production renderer began selecting a fixture-free IPC adapter at Vite build time while Vite serve retains the deterministic development adapter. The entry fell from 506,899 B / 148,685 B gzip to 487,656 B / 142,922 B gzip, removing the >500 kB warning without changing the stable lazy-workspace or eager-CSS recovery graph. The artifact gate now enforces a 500,000-byte entry budget and rejects representative preview/native-fixture sentinels. The complete native Electron development-fixture runtime, startup isolation/first-interaction checks, production renderer build, and two-sample smoke plumbing pass; the smoke remains ineligible for a performance or release claim. Same-viewport evidence is in `docs/ux-evidence/2026-08-03-production-renderer-boundary/`.
- On 2026-08-03, the renderer-only `@phosphor-icons/react@2.1.10` package was moved from production dependencies to pinned development dependencies, and `check:package-config` now fails if that classification regresses. A source-only `/tmp` copy completed fresh `npm ci` in about 3 seconds and the full unsigned x64/arm64 DMG/ZIP `npm run build` in 101.62 seconds. Both architecture packages completed; the only renderer build advisory remained the non-fatal 506.89 kB entry chunk. The 826 MB temporary install and artifacts were moved to the Trash after inspection and were not synchronized into the workspace.
- On 2026-07-31, the isolated renderer harness completed the full 40-plus-five-per-scenario contract with zero unexpected adapter calls. The exact machine-readable result is `docs/ux-evidence/2026-07-31-startup-readiness-performance/04-renderer-hidden-startup-baseline.json`. The same directory contains three reviewed 520×680 startup-state captures and a proof of exact Retina geometry, intended focus, isolated profiles, hidden windows, and zero unexpected adapter calls. `measurementContractSatisfied` is true and `releaseClaimEligible` is false; no visible startup, FCP, installed-app, Gatekeeper, or release-threshold claim is made. The exact final tree passes complete `npm test`, full-repository lint, and the renderer build; this is still not a signed/notarized release gate or visible-startup acceptance.
- On 2026-07-28, the exact application tree after the custom-endpoint trust-boundary, stale full-activation retry, and irreversible screenshot-stop fixes was copied into a local `/tmp` verification directory and the lockfile-pinned dependencies were freshly installed there.
- The first full pass correctly failed on one obsolete static assertion for the prior failed-settings representation. After that contract was updated, complete `npm test`, full lint, renderer production build, the scoped development-dependency security gate, and the runtime dependency audit passed; the runtime audit reports 0 vulnerabilities. `release:unsigned` then passed for x64 and arm64 DMG/ZIP creation, regenerated checksums, the complete `check:release`, history non-retention, Info.plist checks, and artifact inspection.
- The verified `index-ngyx_oTz.js` renderer and canonical release files were synchronized back to the workspace. All four DMG/ZIP entries pass `SHA256SUMS.txt`, and the workspace artifact inspection passes after synchronization. The isolated verification directory was moved to the Trash after the checks.
- After the visible first-use/Settings accessibility pass, a separate current-tree verification again passed the complete test chain, lint, renderer production build, package configuration, production dependency audit at 0 vulnerabilities, and full unsigned x64/arm64 ZIP/DMG build with checksums, Info.plist, history, and artifact inspection.
- Current-run development-fixture screenshots were inspected for first use, basic-translation readiness, initial Settings, and keyboard selection at 820×720, then for unified Settings transitions at the exact 520×680 capture geometry and supported 400×400 minimum. The transition pass includes pre/post comparison and direct Shift+Tab focus evidence.
- A later current-run pass inspected the Settings transition at 400×400 with Chromium zoom factor 2. Its native gate and visible evidence accept the bounded dialog focus path. Subsequent current-run passes accept first use, capture, completed results, and Saved Terms at the same exact geometry, including the previously clipped entry header, privacy handoff, safe sample, action/source evidence, footer controls, and drawer search/backup focus paths.
- On 2026-07-30, an isolated credential-save-retry pass accepted the failed-save → global-retry → provider-test journey with fictional connection data, exactly two save attempts, one success, one success live owner, useful focus recovery, and zero external requests. Current-run before/after screenshots were inspected, and a clean local copy with fresh lockfile dependencies passed complete `npm test`, full lint, and `npm run build` for ad-hoc x64 and arm64 macOS artifacts. This does not claim real VoiceOver output, Developer ID signing/notarization, live provider quality, or use of a real credential.
- A later 2026-07-30 Saved Terms import pass accepted the preview → trust review → cancel/commit → close/reopen journey. Current-run 520×680 evidence shows focus moving from premature confirmation to the complete trust-review perimeter; DOM/Chromium AX proves Confirm descriptions and one exact outcome owner; and the native 200%-scale fixture proves warning order, complete ring geometry, unchanged data on cancel, and useful focus return. Complete `npm test`, full lint, and the ad-hoc x64/arm64 build pass. This does not claim real VoiceOver, arbitrary real-file imports, Developer ID signing/notarization, live provider quality, or use of a real credential.
- A later 2026-07-30 Result-disclosure pass inspected eight current-run 520×680 fixture disclosures and their before/after comparisons. Chromium exposes the repaired H1 → H2 → H3 hierarchy, stable trigger/panel IDREFs, deadline and processing-detail open/close with focus retained, one labelled processing-details region, no common-panel regions, and zero horizontal overflow; the shared component also covers the conditional ninth warnings disclosure. An isolated 400×400/200% native Electron path passes twice with 24 ordered inputs: 21 trusted clicks, three trusted keys, one deliberate rejection, complete Saved Terms keyboard-ring evidence, and a final natural-pointer deadline collapsed → open → collapsed cycle on the same trigger/panel with two fictional cards and zero application side effects. Narrow reflow reduces the representative disclosure trigger from 150×89 to 150×42. The isolated source copy passes complete `npm test`, full lint, and `npm run build`; ad-hoc x64 and arm64 artifacts complete with only the existing non-fatal >500 kB renderer chunk advisory. This uses fictional fixture data and makes no claim about real VoiceOver Rotor output, real network/API activity, live provider quality, Developer ID signing, or notarization.
- A later 2026-07-30 Guided Reply pass inspected twelve current-run journey captures plus exact-size 240×200 and 520×680 comparisons. The in-app browser's minimum visible viewport was 240×200; exact 200×200 CSS acceptance comes from the separate hidden 400×400/zoom-2 native gate. The repair removes the sticky-footer overlap, preserves the ordinary 520×680 hierarchy, keeps progress and placeholder blockers independent, exposes copied→outdated version feedback, and retains useful state across close/reopen. The hidden sequence accepts 18 trusted steps with one deliberate rejection, a complete nine-Tab loop, exact Close/Escape trigger return, one in-memory clipboard write, zero unrelated side effects, and zero external requests. On an isolated physical copy of the exact current source, complete `npm test`, full-repository lint, the renderer build, unsigned x64/arm64 ZIP and DMG packaging, SHA-256 checksum generation, release-artifact inspection, and `git diff --check` pass. The renderer retains one non-fatal large-chunk advisory (697.98 kB minified / 196.28 kB gzip). The evidence uses only fixed fictional data; no real credential was used or stored and no live API request was made.
- The entries in this historical section describe unsigned automated and development-fixture evidence only. The v1.0.0 signed/notarized distribution result is recorded separately above; these older entries must not be used to contradict or extend that release claim.

## Publish

- Publish only from a reviewed, clean exact-version commit after `release:signed` and `check:distribution` pass with a valid Developer ID identity and notarization credentials. Never upload the local-ad-hoc artifacts as the public production release.
- Create a version tag from the exact commit used to build.
- Attach both user-facing DMGs and `SHA256SUMS.txt` to the release. ZIPs and blockmaps remain build outputs unless an updater needs them.
- Include known limitations and privacy-impacting changes in the notes. In V1, GOV.UK is the only built-in search-discovery provider; other publishers require an eligible candidate URL and retrieved pages remain claim-neutral unless an explicit semantic assessor verifies support.
