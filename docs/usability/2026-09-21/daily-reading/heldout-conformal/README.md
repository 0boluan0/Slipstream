# Conformal Prediction 论文的真实首读与归属复验（2026-09-23）

来源：[Angelopoulos 与 Bates 的原论文](https://arxiv.org/abs/2107.07511)，本地 PDF SHA-256 为 `c69aa191d8363c25b36685db4adb7e6980e55ee39892fd3626206c16e1e0efa1`，共 51 页。本轮首次把此文件交给产品，目标是以普通读者视角检查第 4 页的预测集、校准数据和边际覆盖率：公式 (1) 应表达覆盖概率约为 `1-α`，不是对每个给定输入都成立的条件覆盖；`(X_i,Y_i)` 是校准样本，`(X_test,Y_test)` 是新测试点。测试材料和判断依据没有作为提示传入产品。

## 首次阅读：本地安装版 `c3d29e2`

在 macOS Preview 打开该 PDF，使用 Option+Shift+S 触发系统 `/usr/sbin/screencapture -i`，用真实拖框 `(490,650)→(1320,923)` 截取正文与公式。选区右侧过紧；卡片提示右边可能截断，原文确有词尾缺失。读者按提示重框为 `(475,634)→(1390,963)`。第二张卡片没有截边警告，显示“临时阅读”，没有继承之前论文的本文定义。

第二张本地识别保留了覆盖率不等式的上下界、概率、下标、分式和式号，排版为 LaTeX 数学。核对页仍标出 6 处可能不可靠的公式；英文正文中的 `i.i.d.` 被识别成 `ii.d.`。确认后，中文译文能说明预测集及边际覆盖率；点击 `valid` 的按需解释正确指出式 (1) 是边际覆盖，不推出条件覆盖。界面提供 `calibration data`、`prediction set`、`softmax scores` 等术语。一次解释与译文分别用了“边缘覆盖”和“边际覆盖”，术语仍需统一。

这份材料的首次完整体验需要重框和人工核对，**不计无须校正的独立首用通过**；覆盖率公式保留不等于全部数学内容或正文识别正确。论文中的原始像素和用户窗口记录仅留在本机，不进公开 PR。

## 本文速查归属：更新安装版 `093b296`

修复发现的问题：从阅读卡片打开独立的“本文速查”窗口并新建论文后，新建的论文只绑定了后续截图，发起操作的旧卡片仍写着“临时阅读”。修复后，用相同 PDF 字节的另一个测试文件 `conformal-reference-flow-qa.pdf` 作新来源：真实快捷键和原生拖框先出现临时阅读，在卡片内打开本文速查并新建、改名 `QA-Conformal-Reference-Flow` 后，回到原卡片立刻显示该名称；关闭卡片，对同一个 Preview 文档再拖框，新卡片自动显示这个论文。这份复拍的底边选区仍被提示可能截断，只验证归属，不计内容准确性或第二次独立首读。

当次预览安装在 `~/Applications/Slipstream 阅读预览.app`。它使用独立 bundle ID、Developer ID 签名，代码身份和打包公式 OCR 已检查；`093b296` 安装版 ASAR SHA-256 为 `fe8b6b1ab4e479a00008617ba27f17d966af57b5e5c616ac12099df9d0f8032a`。它没有经过公证，也没有公开发布。

## 新安装版 `cbec0f7` 的同篇复拍

放大的 Adam 样例在较慢的 CI 机器上曾出现缺失公式的纯文字结果；原有 25 秒短限时可能是触发原因。新版按检测出的公式数量延长处理时间，最长 60 秒。公开 10 项、本地完整 19 项公式图像测试及随包 3 项离线识别通过。它仍是本地预览，没有公证或正式发布。

用同一 PDF 的 QA 副本，在 Preview 第 4 页重新原生拖框 `(475,670)→(1390,950)`：本地识别 7 处公式，其中 3 处标为需核对；式 (1) 的上下界、下标、分式和式号仍正确，`i.i.d.` 这次读对。译文 3/3 段完成；点击 `marginal coverage` 后，解释准确说明它对校准集和测试点的随机性取平均，不保证某个固定输入的条件覆盖。此次更合适的选区没有触发截边提示，但“Using f”漏掉原文的帽号、图 1 读成图 I，依旧不能算无误识别。

卡片提出一条有原文依据的 `(X_1,Y_1),…,(X_n,Y_n)` 定义，解释为训练未见过、用于校准的 i.i.d. 图像与类别对；展开原文依据核对后，保存为 `QA-Conformal-Reference-Flow` 的本地速查。完全退出并重开安装版，对同一 Preview PDF 再次原生拖框，卡片立即显示该论文和“本文速查 · 1”，打开后仍有定义及来源。随后关闭测试卡片，未留悬浮窗。

## 同像素识别根因与离线修复（2026-09-23）

重新用安装版快捷键和系统截图器，在同一 Preview 页真实拖框 `(475,670)→(1390,950)`，立即保留应用创建的 1832×562 PNG（SHA-256 `b7805bd123b9b7f946eaa9fb7a9dc52ca91d56b255e804f065eb4cd35d5c9e72`）。原始图片仅在本机 `/tmp/slipstream-daily-reading/debug-conformal-ocr/` 调试，不随源码提交。直接用 `screencapture -R` 截同一区域产生 1830×560 图片（SHA-256 `0c1f1d1117e8e72b911c9791a8a3491497fe1fb1df1f962a0528644a641722eb`）；它能识别第二个 `\hat f`，不能代替产品真实截图的判据。

真实截图的整图公式布局检测给第二个 `\hat f` 打分 0.2604，旧阈值将其丢弃，因而原文和译文前置识别都只见 `Using f`。专用数学识别器在原裁切及两种加白边裁切上三次一致读成 `\hat f`，最低置信度仍超过 0.9999。修复只让这类小型单字母重音在三次数学识别完全一致时进入 LaTeX；宽幅截图再对左右重叠半页做检测，恢复另一张完全自拟的 1840×500 排版样例中分数仅 0.143 的相同遗漏。该弱候选继续标记需人工核对，不把检测器分数伪装成高置信度。宽幅纯文字 `f` 样例没有被添帽号。

同张截图中，Vision 整行读作 `See Figure I`，加边距读法为 `See Figure 1`，按字符位置裁出 `See Figure` 及其后一个字形时再次读成 `See Figure 1`。仅在两种整行读法除 `I/1` 外完全一致、位于同一行，且紧裁 OCR 再确认数字时才替换。离线完整 OCR 现在给出 `Using $\hat{f}$`、`See Figure 1` 和 8 处公式（4 处需核对），之前为 7 处（3 处需核对）。自拟图片的正例、普通字母反例、本地图像测试以及完整 `npm test` 均通过。随后将修复打进独立的签名阅读预览安装版，使用 Preview 第 4 页、Option+Shift+S 和系统截图器重新拖框；卡片同样显示 8 处公式、4 处需核对，恢复了帽号和 `Figure 1`，译文 3/3 段完成。此处是同篇同像素回归，独立首用通过数仍为 0。
