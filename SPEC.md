# Slipstream 产品规格

## 定位

**读懂原文，留下概念。**

Slipstream 面向阅读英文教材、论文和专业文章的中文母语者。用快捷键框选正在读的内容，把中文译文贴在屏幕旁；按需解释专业概念，并将值得留下的解释和原文存入本地卡片盒。

## 核心体验

1. 截图阅读是首页的主要入口，显示实际配置的快捷键。粘贴和已复制文字进入同一套独立阅读卡片。
2. 中文译文优先，按段保留英文对照。术语解释由读者主动展开，不阻塞继续阅读。
3. 推荐聚焦原文中值得解释的专业概念，允许零个，不按数量补齐。多个候选由同一服务额外做一次只删不增的短复核；译文先显示，可复制和手动查询，复核失败保留译文并提示选词查询。原文选词查询补充自动推荐。
4. 每次解释包含概念含义和它在本段中的作用，并区分原文明确定义、根据用法推断与通用释义。原文依据按需展开；模型输出须经过长度、原文引用与词边界校验。来源标记不能代替对解释内容的核对。
5. 临时阅读卡片可以移动、缩放、置顶、收起、独立关闭。展开解释为自动适配的小卡片增加空间，关闭后恢复，尊重读者手动调整的尺寸与位置。字号作用于译文、概念解释与本段用法；换词时从解释开头显示。不同卡片的请求和取消互不混淆。
6. 点击保存后，概念卡片才写入系统文稿目录，包含解释、原文、个人笔记与 Markdown 关联。卡片盒提供搜索、编辑、关联及反向关联。同一术语在完全相同的原文中再次查询时，优先读取已保存的概念解释，明确标注本地来源并可打开编辑；不同原文重新解释。卡片身份保留大小写和数学 Unicode 字形。
7. LaTeX 在译文、解释与卡片中本地渲染并保留可复制的源格式。数学 OCR 先核对，独立公式保留原文。支持的视觉服务只在明确选择公式识别后接收当前截图。
8. 本文速查按论文或章节保留。截图按前台文档来源找回已绑定的阅读；首次遇到的文档或无法识别来源时先作为临时阅读，读者明确选中或新建后再绑定。粘贴文字沿用当前阅读，已有卡片保留各自归属。翻译时提取原文明确定义的符号、缩写及本文约定，显式留下后写入独立本地文件；再次出现时优先本地查询，保留原文依据及同名不同义。
9. 本文速查支持手动记录、搜索、编辑、删除撤销和跨启动继续阅读。大小写、上下标及符号修饰保持区别，公式在查询、编辑与复制中保留 LaTeX。

## 入口与文案

- 仓库默认 README 使用简体中文，英文版本从顶部切换。
- 首页说明阅读用途，突出“截图阅读”；文字提交称为“开始阅读”；顶部“卡片盒”打开本地概念卡片。
- 首次设置提供“专业阅读”和“基础翻译”。专业阅读配置模型；基础翻译清楚说明在线翻译去向及解释能力边界。
- 专业阅读设置以自拟教材段落实际测试翻译和选词解释，展示当前模型的译文、概念含义及本段用法；推荐术语允许为空。测试只发送内置材料，结果临时保留，重试或更改配置后失效，明确启用后才进入阅读首页。
- 示例采用自拟教材风格材料，载入示例与提交服务分开。
- 对外截图来自可运行界面；固定回复的示例明确标注，不充当模型质量证明。
- 公开文档标明对应的阅读版本、架构与下载入口，开发预览使用独立应用身份。

## 数据与可靠性

macOS 默认截图识字使用本机 Apple Vision；从 v1.2.0 起，正式安装包在截图入口自动执行本地专用公式识别，恢复 LaTeX 后与正文按位置合并。低质量或数学 OCR 进入编辑核对，组件失败必须说明。提交位置在发出请求前可见。模型翻译和解释均视为不可信输出，保留原文供读者核对。

公式右侧的编号与对应公式一并保留，不单独触发翻译。补白识别只用于补全原图边缘同一位置的残词，不能替换整段正文；公式识别和补读不能重复原文标点。

API Key 只在主进程使用并通过 Electron safeStorage 调用系统加密存储。渲染进程隔离、IPC 来源校验、端点与重定向限制继续有效。读取剪贴板须由主动操作或已明确开启的监听触发；监听默认关闭。

临时阅读不自动写入历史。主动保存的概念卡片包括原文，关闭浮窗和应用设置重置均保留用户文稿。外部编辑发生版本冲突时保留未提交输入，要求重新载入再决定合并。

本文速查持久保存阅读名称、当前选择、主动留下的定义和来源段落，以及读者绑定的前台窗口来源摘要。关联字段不保存原始窗口标题或 PDF 路径；从截图新建阅读时，窗口标题可作为默认阅读名称保存，读者可修改。未留下的候选仅在临时卡片内存中保留。详情见[本文速查](docs/reading-references.md)。

## 当前范围与验收

支持 macOS 上的英文转中文专业阅读，正式阅读版本从 1.1.0 开始。 Windows 11 x64 提供独立的文字阅读预览，截图识字、开机启动和自动更新暂未开放；范围与证据见[Windows 预览说明](docs/windows-preview.md)。详细行为、公式边界、已知限制与验证证据见[阅读卡片说明](docs/reading-pins.md)。

验收须覆盖：截图与文字进入同一阅读流程、首次配置、零术语、手动查询、保存后从首页卡片盒重开、公式核对与复制、窄窗口与放大文字、失败重试、隐私与权限边界。自动固定回复验证交互；真实服务检查另行记录输入、配置和限制。

历史结果与恢复机制仍使用已有结构化行动工作区。[兼容契约](docs/action-workspace-contract.md) 保留其约束，供现存数据、服务兼容性检查及回归测试使用。
